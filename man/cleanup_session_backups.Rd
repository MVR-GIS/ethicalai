% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanup_session_backups.R
\name{cleanup_session_backups}
\alias{cleanup_session_backups}
\title{Clean Up Old Session Backups}
\usage{
cleanup_session_backups(
  sessions_dir = "dev/sessions",
  days_to_keep = 7,
  dry_run = FALSE
)
}
\arguments{
\item{sessions_dir}{Character. Path to sessions directory
(default: "dev/sessions")}

\item{days_to_keep}{Numeric. Keep backups from last N days (default: 7)}

\item{dry_run}{Logical. Show what would be deleted without deleting?
(default: FALSE)}
}
\value{
Invisible NULL
}
\description{
Removes backup files older than specified number of days to prevent
backup directory bloat.
}
\examples{
\dontrun{
# See what would be deleted
cleanup_session_backups(dry_run = TRUE)

# Delete backups older than 7 days
cleanup_session_backups()

# Keep backups from last 30 days
cleanup_session_backups(days_to_keep = 30)
}

}
