% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_sbom_env.R
\name{write_sbom_env}
\alias{write_sbom_env}
\title{Write SBOM environment bundle metadata for an R package repo}
\usage{
write_sbom_env(
  lockfile = "renv.lock",
  out_dir = file.path("artifacts", "sbom"),
  overwrite = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{lockfile}{Character. Path to \code{renv.lock} (default \code{"renv.lock"}).}

\item{out_dir}{Character. Output directory for the SBOM bundle.
Default \code{file.path("artifacts", "sbom")}.}

\item{overwrite}{Logical. Overwrite existing bundle files? Defaults to \code{TRUE}.}

\item{quiet}{Logical. Suppress informational messages? Defaults to \code{FALSE}.}
}
\value{
Character vector of written file paths (invisibly).
}
\description{
Writes a lightweight environment manifest and copies the specified \code{renv.lock}
into an SBOM artifact bundle directory (default: \code{artifacts/sbom}).
}
\details{
This function is intended to be called from CI after \code{renv::restore()} and SBOM
generation steps (e.g., Anchore SBOM Action). It does not generate the SBOM
itself; it creates additional evidence that documents the environment associated
with the SBOM artifact.

Files written:
\itemize{
\item \verb{<Package>_env_sha-<shortsha>.txt}: environment manifest (Package/Version, git SHA,
R version/platform, timestamp, lockfile hash)
\item \code{renv.lock}: copy of the lockfile used
}
}
