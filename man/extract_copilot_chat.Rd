% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_copilot_chat.R
\name{extract_copilot_chat}
\alias{extract_copilot_chat}
\title{Extract GitHub Copilot Chat from Export Zip}
\usage{
extract_copilot_chat(
  zip_path,
  session_date = Sys.Date(),
  topic = NULL,
  sessions_dir = "dev/sessions",
  backup = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{zip_path}{Character. Path to the exported Copilot chat .zip file}

\item{session_date}{Character or Date. Date for the session (default: today).
Format: "YYYY-MM-DD" or Date object}

\item{topic}{Character. Optional topic suffix for filename (default: NULL)}

\item{sessions_dir}{Character. Path to sessions directory
(default: "dev/sessions")}

\item{backup}{Logical. Create timestamped backup before overwriting?
(default: TRUE)}

\item{quiet}{Logical. Suppress informational messages? (default: FALSE)}
}
\value{
Character. Path to extracted session file (invisibly)
}
\description{
Extracts the main chat markdown file from a GitHub Copilot export zip
archive and saves it to dev/sessions/ with date-based naming. Supports
incremental updates throughout the day via backup and timestamp tracking.
}
\details{
The function is designed for incremental updates throughout a work session:
\itemize{
\item First call: Creates session file
\item Subsequent calls: Overwrites with latest export, creating backup
\item Backups stored in dev/sessions/.backups/ with timestamps
\item Original filename preserved for easy reference
}
}
\examples{
\dontrun{
# Call multiple times throughout the day - automatically handles updates
extract_copilot_chat("~/Downloads/copilot_export.zip")
# ... work for 2 hours ...
extract_copilot_chat("~/Downloads/copilot_export.zip")  # Creates backup
# ... handle brushfire ...
extract_copilot_chat("~/Downloads/copilot_export.zip")  # Another backup

# Disable backups (not recommended for active sessions)
extract_copilot_chat("~/Downloads/copilot_export.zip", backup = FALSE)

# Specify topic for multi-topic days
extract_copilot_chat(
  "~/Downloads/copilot_export.zip",
  topic = "data_validation"
)
}

}
