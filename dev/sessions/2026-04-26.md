# Copilot Chat Conversation Export: SBOM

**User:** @mpdougherty
**Thread URL:** https://github.com/copilot/c/09878871-f021-4761-acb0-1e570cfab3f4

## @mpdougherty

# This session will be based on the `MVR-GIS/MVR-User-Guide`
# repo `main` branch. Read `dev/instructions/CHAT_INSTRUCTIONS.md` 
# and follow the instruction modules listed under "Selected 
# instruction modules (read in order)".

## Copilot

Got it. I’ve read and will follow the repo’s selected instruction modules (in order):

1) `dev/instructions/chat-manual.md` (manual/review-first; **read-only only**, no PRs/agent sessions/writes)  
2) `dev/instructions/goals.md` (authoritative, evidence-based, reproducible)  
3) `dev/instructions/r-package.md` (R Packages 2e best practices when applicable)  
4) `dev/instructions/user-manual.md` (reproducible technical manual standards)

Before we do anything else: what’s your first task/question for `MVR-GIS/MVR-User-Guide` on `main`?

## @mpdougherty

I'm the tech lead for an Army NIPR data science team that focuses on R, Python, and SQL using Github. Army has directed us to comply with Army Directive 2024-02 and the Unified Network Plan 2.0. These specify the adoption of SBOM. Describe a plan for us to implement SBOM using only no cost open source software.

## Copilot

Implement SBOM as a **repeatable, automated supply‑chain “inventory + monitoring” capability** across your R/Python/SQL repos, using **open SBOM standards (CycloneDX or SPDX)** and **no-cost open source tooling**, then **store, govern, and continuously monitor** SBOMs as part of your GitHub workflows.

This aligns with the Army’s direction toward modern software practices (CI/CD, DevSecOps) ([army.mil](https://www.army.mil/article/274356/army_announces_new_policy_to_drive_adoption_of_agile_software_development_practices)) and with the Army’s SBOM policy expectations around **collection, secure storage, management, and continuous monitoring** of SBOMs ([api.army.mil](https://api.army.mil/e2/c/downloads/2024/09/26/36c0ec0e/asaalt-software-bill-of-materials-policy-signed.pdf)), while supporting Unified Network Plan 2.0’s emphasis on standardized, centrally managed approaches and Zero Trust/data-centricity ([api.army.mil](https://api.army.mil/e2/c/downloads/2025/03/04/0b7f95c5/army-unified-network-plan-2-0.pdf)).

---

## 1) Governance: define “what an SBOM is” for your team (Week 1)

**Decisions you make once, then automate:**

1) **SBOM format**
   - **CycloneDX (recommended)**: strong ecosystem and first-class support in Dependency-Track ([owasp.org](https://owasp.org/www-project-dependency-track/?utm_source=openai))  
   - SPDX is also acceptable; choose if your downstream consumers mandate it.

2) **SBOM scope**
   - Minimum: application/library dependencies (direct + transitive).
   - Add-on (later): container base images, OS packages, git submodules, etc.

3) **SBOM “moments”**
   - **Every merge to main** (continuous)
   - **Every tagged release** (baseline “delivered SBOM”)
   - Optional: nightly full rebuild for drift detection.

4) **SBOM handling rules**
   - SBOMs are **artifacts**, not hand-maintained documents.
   - SBOMs must be **versioned to a build** (commit SHA, tag, build time).

**Deliverables**
- A short internal SOP: “SBOM Policy for Data Science Repos”
- A repo template (GitHub Actions workflow) that every project can reuse

---

## 2) Tooling stack (all no-cost open source)

### A. Generate SBOMs (per language/ecosystem)
Use a **generator per project type** (don’t force one tool to do everything):

- **Python**
  - `cyclonedx-python` (generates CycloneDX from Python environments / lockfiles)
- **R**
  - Use SBOM generation that reads from `renv.lock` (ideal if you standardize on `renv`)
  - If you don’t use `renv` yet, SBOM quality will be weaker; plan to adopt `renv` as a prerequisite for good SBOMs
- **Containers / mixed projects (optional but powerful)**
  - `syft` to generate CycloneDX/SPDX SBOMs from a repo, filesystem, or container image (useful when Python+R are packaged together)

### B. Store and monitor SBOMs centrally
- **OWASP Dependency-Track** (open source) as your SBOM “system of record” and dashboard ([owasp.org](https://owasp.org/www-project-dependency-track/?utm_source=openai))  
  - It consumes CycloneDX SBOMs and can continuously analyze risk/vulnerabilities across a portfolio ([owasp.org](https://owasp.org/www-project-dependency-track/?utm_source=openai)).

**Why this matters:** Army SBOM policy isn’t just “create SBOMs”; it emphasizes collecting them, securely storing/managing them, and **monitoring them over time** ([api.army.mil](https://api.army.mil/e2/c/downloads/2024/09/26/36c0ec0e/asaalt-software-bill-of-materials-policy-signed.pdf)).

---

## 3) Operating model in GitHub (how your team will work)

### Repo-level standard (each repo gets the same pipeline)
1) **Build/resolve deps**
   - Python: require a lockfile (`requirements.txt` pinned, or `poetry.lock`, etc.)
   - R: require `renv.lock` for anything that runs code
2) **Generate SBOM**
3) **Attach SBOM to the build**
   - Store as GitHub Actions artifact
   - Optionally commit to a dedicated `sbom/` folder only if policy requires (I’d prefer artifacts + release assets to reduce churn)
4) **Upload SBOM to Dependency-Track**
   - Dependency-Track becomes the place you query: “Where is package X used?” and “What’s affected by CVE‑YYYY‑ZZZZ?”

### Release-level standard (tagged releases)
- Publish SBOM alongside release artifacts (zip, container image, etc.)
- SBOM filename convention includes repo + version/tag + commit

---

## 4) Rollout plan (phased, low-risk)

### Phase 0 — Pilot (Weeks 1–2)
Pick **1 Python repo** + **1 R repo**:
- Add lockfile discipline (R: `renv`; Python: pinned requirements/lock)
- Add GitHub Actions job to generate CycloneDX SBOM
- Stand up Dependency-Track (docker-compose) in an approved environment
- Upload SBOM automatically on merge to `main`

**Exit criteria**
- SBOM generated on every merge
- SBOM visible in Dependency-Track
- Team can answer: “Which apps have `openssl` / `requests` / `sf`?” in minutes

### Phase 1 — Standardize templates (Weeks 3–4)
- Create reusable workflow templates:
  - `sbom-python.yml`
  - `sbom-r.yml`
  - `sbom-container.yml` (optional)
- Create a one-page checklist for repo owners:
  - “Do you have a lockfile?”
  - “Does SBOM job pass?”
  - “Is Dependency-Track receiving SBOMs?”

### Phase 2 — Scale out + enforce (Weeks 5–8)
- Roll to all repos
- Add lightweight enforcement:
  - PR checks require “SBOM job succeeded”
  - Block merge if dependency resolution is not reproducible (missing lockfile)

### Phase 3 — Continuous monitoring + incident workflows (ongoing)
- Define what happens when Dependency-Track flags:
  - critical CVE in a dependency
  - unacceptable license
  - stale/out-of-date component
- Tie into your existing ticketing/issue process (even if it’s just GitHub Issues internally)

---

## 5) “Definition of done” and verification

**Per repo**
- Reproducible dependency manifest (lockfile)
- SBOM generated automatically (CI)
- SBOM stored (artifact + release asset)
- SBOM uploaded to Dependency-Track

**Per team**
- Dependency-Track has an inventory of all applications
- Monthly (or sprintly) review of “top risk” dependencies
- Documented process for remediation and re-issuance of SBOMs

---

## 6) Clarifying questions (so I can tailor this to NIPR realities)
1) Are your workloads primarily **packages**, **scripts**, **Shiny apps**, or **containers**?
2) For R, are you already using **`renv`** everywhere (or willing to make it a standard)?
3) Do you have an approved place to run a small internal service like Dependency-Track (VM or container host), or must everything stay purely in GitHub artifacts?

If you answer those, I’ll propose **3–5 implementation options** (ranked) that fit your constraints (air-gapped-ish processes, approvals, container use, release cadence) and include a concrete checklist for the first pilot repo.

## @mpdougherty

As the tech lead for this project, my first data science language is R, so we'll pilot the process with it. Let's focus on traditional R project based repos. We've already adopted `renv`, so adopting the `sbom` R package is straight forward. We have not been governance on how the SBOM artifacts will be aggregated, so let's focus on establishing continuous integration so we are ready for that later. I suspect the format we should target will be CycloneDX. Are these answers enough to streamline the plan you presented?

## Copilot

Yes—those answers are enough to streamline the plan into an **R-first, `renv`-based, CycloneDX SBOM pipeline** with CI that produces consistent SBOM artifacts now, while keeping aggregation/portfolio monitoring as a later decision.

Below are **4 options** (ranked by confidence / least assumptions) for establishing CI SBOM generation in “traditional” R repos.

---

## Option 1 (highest confidence): GitHub Actions generates CycloneDX SBOM from `renv.lock` on PR + main
**What it is**
- A GitHub Actions workflow that:
  1) restores packages via `renv`
  2) generates a CycloneDX SBOM (using the `sbom` R package)
  3) uploads SBOM files as workflow artifacts (and optionally as release assets later)

**Pros**
- Immediate CI readiness; no dependency on future aggregation decisions.
- SBOM is tied to commit SHA + run ID (good audit trail).
- Scales across many repos with a shared template.

**Cons**
- You must decide artifact naming + retention now (minor governance, but local).

**Tradeoffs**
- Slight CI time increase (restore + SBOM generation).

---

## Option 2: Generate SBOM only on merges to `main` (not on PRs)
**What it is**
- Same as Option 1, but runs only after merge.

**Pros**
- Lower CI noise/compute.
- Still creates continuous SBOM stream for your default branch.

**Cons**
- You won’t catch SBOM-generation breakage until after merge.

**Tradeoffs**
- Faster PR iteration vs earlier verification.

---

## Option 3: Add scheduled (“nightly”) SBOM regeneration in addition to PR/main
**What it is**
- Option 1 plus a nightly job.

**Pros**
- Helps detect drift (e.g., repo changes affecting restore/SBOM).
- Useful later when you add monitoring/aggregation.

**Cons**
- More CI usage; may be unnecessary early.

**Tradeoffs**
- Extra compute for extra assurance.

---

## Option 4: Keep CI minimal: build SBOM without restoring packages (lockfile-only)
**What it is**
- Attempt SBOM purely from `renv.lock` without a full restore.

**Pros**
- Fastest.

**Cons**
- Depending on how `sbom` constructs metadata, you may lose fidelity (and you may miss errors that only appear when `renv::restore()` runs).

**Tradeoffs**
- Speed vs stronger verification evidence.

---

# Recommended approach
**Option 1**: Run on **pull_request** + **push to main**, generate **CycloneDX**, and upload as artifacts.

To make this “aggregation-ready later”, standardize these now:

### 1) SBOM output conventions (light governance)
- Output directory: `artifacts/sbom/` (or `sbom/`—pick one)
- Filenames include:
  - repo name
  - git SHA (short)
  - timestamp optional
  - format + spec
- Example:
  - `artifacts/sbom/MVR-User-Guide_cyclonedx_1.5_sha-abc1234.json`

### 2) Artifact retention expectations
- Keep at least **90 days** in Actions artifacts (or whatever your org policy allows).
- Later, aggregation can pull from:
  - release assets (best for “delivered SBOM”)
  - or a separate SBOM repo/bucket/service

### 3) Minimum metadata to include
- Commit SHA, build time, project name/version (where available)
- If your repos don’t have a formal version, use:
  - git SHA + date as build identifiers

---

# What I need from you to finalize the CI workflow pattern (2 quick questions)
1) Are these repos **R packages** (have `DESCRIPTION`) or “analysis projects” (no `DESCRIPTION`)?  
   - This affects how we set project name/version metadata.
2) Do you want the SBOM artifact to be produced for **PRs from forks** as well, or only internal PRs?  
   - Fork PRs can complicate secrets (even if we’re only uploading artifacts).

If you answer those, I’ll propose the exact CI steps (still read-only guidance) and a checklist for rolling it out consistently across repos.

## @mpdougherty

Option one looks good. This aligns with our team's preferred "feature branch" git workflow. We probably need to adopt the root folder `artifacts` to accommodate the growing number of future requirements for metadata artifacts (e.g., .dbt). This simplifies .Rbuildignore. The proposed naming convention sounds good. I'm initially focused on repos that are R packages, not general purpose analysis repos. Fork workflows do not need to be supported.  

## Copilot

Yes—this is enough to **lock in a streamlined “Option 1” plan** for R-package repos:

- **CycloneDX** target format
- **`renv`** already adopted (great: makes SBOM reproducible)
- **No fork PR support** (simplifies CI + secrets)
- **Focus on CI now**; aggregation comes later
- Standardize an `artifacts/` root for SBOM outputs and future metadata artifacts

Below is a concrete, CI-ready plan you can roll out across R package repos (manual steps only; no repo writes from me per this repo’s rules).

---

## Plan: R package SBOM in CI (CycloneDX) using `renv` + `sbom`

### 1) Repository conventions (adopt once)
**Folders**
- Create: `artifacts/sbom/` (generated in CI; normally not committed)

**Ignore rules**
- Add to `.Rbuildignore` (so the package build ignores artifacts)
- Add to `.gitignore` (so developers don’t accidentally commit artifacts)

Suggested patterns:
- `^artifacts$` (or narrower: `^artifacts/sbom$` if you expect to commit other artifacts someday)

**Naming convention**
- File: `artifacts/sbom/<package>_cyclonedx_sha-<shortsha>.json`
  - Example for this repo (from `DESCRIPTION`, Package is `MVR-USER-GUIDE`, Version `2026.04.01`):  
    `artifacts/sbom/MVR-USER-GUIDE_cyclonedx_sha-abc1234.json`

(You can also include version in filename later; SHA is the key for traceability.)

---

### 2) GitHub Actions workflow (what it should do)
Create a workflow like: `.github/workflows/sbom-r.yml` that runs on:

- `pull_request` (internal PRs)
- `push` to `main`

**Job steps (high-level):**
1) Checkout
2) Set up R
3) Restore dependencies via `renv::restore()`
4) Install the `sbom` package (as a tool dependency)
5) Generate CycloneDX SBOM into `artifacts/sbom/…json`
6) Upload the SBOM as a GitHub Actions artifact

**Why restore first?**
- It’s your strongest “quality gate”: if `renv` restore fails, you don’t have a reproducible dependency graph, and the SBOM isn’t trustworthy.

---

### 3) Define “Definition of Done” for the pilot
For each R package repo:

- [ ] `renv.lock` exists and CI can run `renv::restore()` from it
- [ ] SBOM workflow succeeds on PR and on merge to `main`
- [ ] SBOM artifact is downloadable from the workflow run
- [ ] SBOM file includes package identity (name/version) and dependency list

---

## 3–5 implementation choices you still need to decide (small but important)
Pick these now so your CI is consistent across repos:

1) **Artifact retention**: 30/60/90+ days (whatever your org allows)
2) **SBOM scope**:
   - default: R package deps from `renv.lock`
   - later: include system libs / containers (if applicable)
3) **When to produce “release SBOMs”**:
   - later: on Git tags, attach SBOM to GitHub Releases (best for “delivered SBOM”)

---

## One quick repo-specific note (from inspection)
This repo has a valid R package `DESCRIPTION` and version (`2026.04.01`), so you can populate SBOM metadata cleanly from `DESCRIPTION` without guesswork.

---

## Next step (so I can give you the exact workflow skeleton)
To avoid guessing the `sbom` package API, paste one of the following (whichever you already use internally):

- the command(s) you expect to run locally, e.g.  
  `Rscript -e 'sbom::write_sbom(...)'` (or similar), **or**
- a link/path in this repo where you already documented how you generate SBOM (I saw `gov-sbom.qmd`; I can read it and adapt the workflow around it)

If you want, I can inspect `gov-sbom.qmd` and any existing CI docs in this repo and then propose the **exact** YAML + R commands (still as manual instructions you apply).

## @mpdougherty

This looks like a good next step. Since we am starting with R packages, always default to leveraging R package metadata. Clarifications: 1. 120-day retention (I don't know what is required yet, but given the update frequency of our software anything shorter seems to short). 2. always produce from `renv.lock`. 3. GitHub Releases. After coming back to this several days later, I'm rethinking the need for an entirely seperate R package `sbom`. Since I'm currently actively maintaining the R package `MVR-GIS/reproducibleai` described at `ai-reproducibleai.qmd` I think I'd like the R-centric SBOM functions we are going to define in this chat to live in `reproducibleai`. For example the API would be `reproducibleai::write_sbom(...)`. For clarification, whatever workflow we develop here will be documented for users in the currently empty chapter `gov-sbom.qmd`. The workflow developed here for R packages will be described in the R section of that chapter after the SBOM conceptual and functional introduction section. 

## Copilot

Yes—this is a clean refinement, and it’s a good idea to put the R-centric SBOM implementation in `MVR-GIS/reproducibleai` so the CI workflow for any R package repo can call a **single stable API** (e.g., `reproducibleai::write_sbom()`), rather than every repo learning/depending directly on a separate SBOM package.

You also gave enough governance choices to “lock” the workflow behavior:

- **Retention:** 120 days (Actions artifact retention is configurable per workflow/repo settings; the workflow can request it where supported, but repo/org policy may cap it.)
- **Source of truth:** always generate from `renv.lock`
- **Release delivery:** attach SBOM to **GitHub Releases** (on tag)

And: SBOM documentation will go into the currently empty `gov-sbom.qmd` (I confirmed it’s empty aside from the chapter header: `# Software Bill of Materials (SBOM)`).

Because this repo’s base instruction module forbids writes/PRs/agent sessions, what follows is a **manual implementation recipe** you can apply.

---

## 1) What to build in `reproducibleai` (R-side API)

### Option A (recommended): `reproducibleai::write_sbom()` wraps an existing CycloneDX generator
**What it is**
- In `reproducibleai`, implement `write_sbom()` that reads:
  - `DESCRIPTION` (package name/version)
  - `renv.lock` (dependency graph)
- And produces CycloneDX JSON.

**Pros**
- Keeps `reproducibleai` thin and maintainable.
- Reuses an existing SBOM spec implementation (lower risk of spec mistakes).
- Makes it easy to upgrade CycloneDX spec version later in one place.

**Cons**
- You still depend on *some* underlying generator package(s) or a CLI.

**Tradeoff**
- “One package to call” (good) vs “one package contains everything” (hard).

### Option B: `reproducibleai` implements CycloneDX JSON itself (not recommended initially)
**Pros**
- No external SBOM generator dependency.

**Cons**
- You are now maintaining a security-adjacent file format/spec.
- Higher chance of noncompliance with spec edge cases (licenses, purls, hashes, etc.).

**Tradeoff**
- Fewer deps vs much higher maintenance/compliance risk.

**Recommendation:** Option A.

---

## 2) Minimal `reproducibleai::write_sbom()` contract (so CI + docs are stable)

Design the function so it works across all R package repos with minimal arguments:

```r
reproducibleai::write_sbom(
  lockfile = "renv.lock",
  out_dir = "artifacts/sbom",
  format = "cyclonedx_json",
  include_dev = FALSE,
  overwrite = TRUE
)
```

### Behavior rules (based on your decisions)
- **Always use package metadata**:
  - package name/version from `DESCRIPTION`
- **Always use `renv.lock`** as dependency source (never “installed packages” as truth)
- Create `out_dir` if missing.
- Output file name convention:
  - `<Package>_cyclonedx_sha-<shortsha>.json`
  - If git is unavailable (rare in CI), fall back to `sha-unknown` with timestamp.

### Inputs for git SHA in a portable way
- In GitHub Actions, `GITHUB_SHA` is available. Your function can read:
  - `Sys.getenv("GITHUB_SHA")`
- If empty, try calling `git rev-parse --short HEAD` (optional).

---

## 3) CI workflow for *any* R package repo (the “Option 1” pipeline)

You’ll have two workflows eventually:
1) **CI SBOM**: PR + push to `main` → generate artifact
2) **Release SBOM**: tag/release → generate SBOM and attach to GitHub Release

### CI SBOM workflow (PR + main)
High-level steps:

1. Checkout
2. Setup R
3. Restore from `renv.lock`
4. Install `reproducibleai` (so we can call `reproducibleai::write_sbom()`)
5. Call `reproducibleai::write_sbom(...)`
6. Upload `artifacts/sbom/*` with **120-day retention**

Key point: since you don’t support forks, you can safely use:
- `pull_request` for internal branches
- no special fork-hardening required for secrets (and you may not need secrets at all for artifact-only SBOMs)

### Release workflow (GitHub Releases)
Trigger on tags like `v*` (or your versioning convention). Steps are the same, except:
- upload SBOM file as a Release asset (or attach to the GitHub Release)
- optionally also upload Actions artifact

---

## 4) Dependency strategy (important for reproducibility + less friction)

You have two feasible patterns for installing `reproducibleai` in CI:

### Option 1 (best for internal consistency): `reproducibleai` is in `renv.lock`
- Each repo includes `reproducibleai` in its `renv.lock` (GitHub remote).
- Then `renv::restore()` installs it automatically.
- CI simply calls it.

**Pros:** one step; most reproducible.  
**Cons:** you need to add it to each repo’s lockfile.

### Option 2: install `reproducibleai` as a CI-only tool
- `renv::restore()` installs project deps
- then install `reproducibleai` from GitHub

**Pros:** doesn’t force SBOM tooling into runtime deps.  
**Cons:** adds an extra moving part (still fine if pinned to a ref/tag).

Given your “always from renv.lock” posture, **Option 1** is the cleanest long-term: SBOM generation becomes part of the reproducible environment.

---

## 5) Documentation plan for `gov-sbom.qmd`
Since `gov-sbom.qmd` is currently empty, structure it like this:

1. **Why SBOM / policy context** (non-technical, short)
2. **What we generate** (CycloneDX JSON; from `renv.lock`; tied to commits/releases)
3. **R packages: how SBOM works here** (this workflow)
   - Prereqs: `renv.lock`, CI enabled, `reproducibleai` available
   - CI: PR + push → artifact
   - Releases: tag → release asset
4. **Verification checklist**
   - SBOM created
   - Artifact uploaded and retained ~120 days (or org cap)
   - Release contains SBOM asset
5. **Troubleshooting** (common failures: restore failures, auth to install internal packages, etc.)

---

## 6) Two blocking clarifications (so I don’t guess wrong)
1) What is the **preferred install source** for `reproducibleai` in CI?
   - A) add `reproducibleai` to each repo’s `renv.lock` (recommended)
   - B) install from GitHub in the workflow (pin to a tag/commit)
2) Do you want SBOM generation to be a **required PR check** (merge gate), or informational at first?

If you answer those, I’ll give you:
- the exact GitHub Actions YAML skeleton (CI + release)
- the exact section outline + draft text for the “R packages” section in `gov-sbom.qmd`
- and a minimal spec for `reproducibleai::write_sbom()` (including what it should return, file naming, and failure modes).

## @mpdougherty

This plan looks good. Clarifications: 1. A. 2. informational. Question: I understand all of the elements of this plan except one. Help me understand the dependency for the "existing CycloneDX generator". What "underlying generator package(s) or a CLI" will this approach use?

## Copilot

For `reproducibleai::write_sbom()` you have **three realistic “no-cost OSS” ways** to produce a **valid CycloneDX JSON** SBOM without you hand-implementing the CycloneDX spec.

I’ll list them in the order I’d normally recommend for an R-first shop that already uses `renv`, and I’ll be explicit about what you’d be depending on.

---

## Option 1 (recommended): Use the `sbom` R package as the underlying generator (wrapped by `reproducibleai`)
**What the dependency is**
- Your function lives in `reproducibleai`, but internally it calls functions from the **`sbom`** package to actually construct and serialize the CycloneDX document.

**Why this is still valuable even if you “don’t want a separate sbom package”**
- You’re not “making every repo use `sbom` directly”; you’re centralizing usage behind **your** API.
- `reproducibleai` becomes the stable interface and policy layer:
  - file naming conventions
  - where to write (`artifacts/sbom`)
  - tying output to `DESCRIPTION` + `GITHUB_SHA`
  - consistent CycloneDX version choice
  - team-specific fields / metadata

**Tradeoffs**
- `reproducibleai` will *depend on* `sbom` (likely in `Imports` or `Suggests` depending on how you want to gate it).
- You will need to keep an eye on `sbom`’s maintenance status and CycloneDX spec support.

**When to choose**
- When you want the simplest R-native implementation and you’re comfortable with `sbom` as a transitive dependency even if users never call it.

---

## Option 2: Use the Syft CLI (`syft`) as the generator, orchestrated by `reproducibleai`
**What the dependency is**
- `reproducibleai::write_sbom()` shells out to a pinned `syft` binary, e.g.:
  - `syft dir:. -o cyclonedx-json=artifacts/sbom/...json`
  - or `syft ... --source-name ... --source-version ...` (exact flags depend on your chosen Syft version)

**Pros**
- Syft is widely used and can generate CycloneDX JSON from many sources (dir, image, filesystem).
- Later, if you expand SBOM scope beyond R (Python, containers), you can keep the same underlying engine.

**Cons / NIPR reality checks**
- You now have a CI dependency on an external executable:
  - you must install it in the GitHub runner (download in workflow) or
  - host it internally / cache it (often preferable in restricted environments)
- Syft may not map R/`renv.lock` dependencies as cleanly as an R-native approach unless you tune inputs; it’s great at “what’s in this filesystem/image,” but your stated requirement is “always from `renv.lock`.”

**When to choose**
- When you want a single SBOM engine across languages and expect to move quickly into container-based delivery or cross-language repos.

---

## Option 3: Use CycloneDX tooling via another language runtime (generally not recommended for your stated goals)
Examples (conceptual)
- call a Python CycloneDX tool to generate JSON from requirements/lock-equivalents (not a great fit for R)
- call a Java-based CycloneDX tool

**Pros**
- Mature ecosystems in some languages.

**Cons**
- Adds runtime complexity (Python/Java) to your R CI story.
- Doesn’t naturally start from `renv.lock`.

**When to choose**
- Rarely for R-first SBOMs; usually only if your organization standardizes on a non-R SBOM toolchain for everything.

---

# Recommendation given your constraints (“R packages”, “always from `renv.lock`”, “no forks”, “informational”)
**Wrap the `sbom` R package inside `reproducibleai`.**

That gives you:
- R-native dependency parsing aligned with `renv.lock`
- CycloneDX output without implementing the spec yourself
- a stable team-facing API (`reproducibleai::write_sbom()`) that you can keep consistent even if you swap the underlying generator later

This also means, practically:
- Each R package repo’s `renv.lock` includes `reproducibleai`
- `reproducibleai` lists `sbom` as a dependency (likely `Imports`)
- CI runs `renv::restore()` then `reproducibleai::write_sbom(...)`

---

## Two due-diligence checks before you lock this in
1) **Confirm CycloneDX version support and output schema** you want (e.g., CycloneDX 1.4 vs 1.5).  
2) Decide whether `sbom` should be in `Imports` (always available) or `Suggests` (optional; but then your SBOM workflow must ensure it’s installed).

If you want, I can make this concrete by doing one quick repo inspection step here: search this repo (or `reproducibleai`, if you point me at it explicitly) for any existing SBOM tooling references or prior attempts, and then propose the exact dependency posture (`Imports` vs `Suggests`) and the exact function signature/behavior for `reproducibleai::write_sbom()`.

## @mpdougherty

Although I am choosing to start small with R packages now, I'll soon need to support multilanguage analysis projects. I'm ok with accepting at this point the overhead of tooling not built specifically for R since it will scale better to in the next iterations to multilanguage projects. It sounds like `syft` could be distributed with the package for portability. I'm not excited about having to support a seperate `sbom` r package. I was hoping to have R support for SBOMs live in only one R package, but that may be unrealistic. Clarifications: 1. I don't know enough at this point to state a CycloneDX version or output schema. 2. see choice above to determine if this question is still relevant. 

## Copilot

Use **Syft as the underlying CycloneDX generator**, and make `reproducibleai::write_sbom()` an *orchestrator* that (a) ensures a Syft binary is available, (b) chooses a stable default CycloneDX output, (c) injects R-package metadata (from `DESCRIPTION`) and build metadata (from `GITHUB_SHA`), and (d) writes to `artifacts/sbom/…` with your naming convention. This keeps “SBOM support” centralized in **one R package** (your API), while using a generator that will scale to **multi-language repos** later.

Below is a plan for the “existing generator” dependency when the generator is **Syft CLI**, including how distribution/portability typically works and what decisions you still need.

---

## 1) What the dependency is (concretely)
Your SBOM pipeline depends on:

- **Syft CLI binary** (open source) as the *SBOM engine*
- `reproducibleai` as the *wrapper/policy layer* that calls Syft consistently

So the “underlying generator” is **not an R package**; it’s an executable.

In CI, the workflow effectively does:

1. `renv::restore()` (project deps, incl. `reproducibleai`)
2. Ensure `syft` is present (download/cached/packaged)
3. Call `reproducibleai::write_sbom()`
4. `write_sbom()` runs `syft ... -o cyclonedx-json=...`

---

## 2) How to make Syft available (3–4 feasible options)
You mentioned “distributed with the package for portability.” That’s possible, but there are tradeoffs. Here are the practical options:

### Option A (recommended for now): Download Syft in CI (pinned version) and cache it
**What it is**
- In GitHub Actions, download a specific Syft release artifact (Linux runner), verify checksum, add to PATH.
- Cache the binary between runs to reduce time.

**Pros**
- Simple; no need to ship binaries inside your R package.
- Easy to pin versions for reproducibility.
- You can update Syft centrally by bumping a version constant in `reproducibleai` (or in the workflow).

**Cons**
- Requires network access to wherever you host/download Syft from (GitHub Releases by default).
- In some NIPR setups, egress restrictions may push you to mirror artifacts internally.

**Best for**
- Getting to “working CI SBOM” quickly with clean R package boundaries.

### Option B: Vendor Syft binaries inside `reproducibleai` (your “distributed with the package” idea)
**What it is**
- Store platform-specific Syft binaries under something like `inst/tools/syft/<os>/<arch>/syft`.
- `reproducibleai::write_sbom()` selects the right one at runtime.

**Pros**
- Works without downloading during CI (useful if egress is constrained).
- Truly “one package provides the tool.”

**Cons (big ones)**
- You’re now shipping and maintaining **compiled executables** inside an R package:
  - multiple OS/arch combinations (Linux x86_64, Windows, maybe macOS)
  - regular security updates
  - repo size growth
  - internal compliance/approval questions (“why is there a binary in this package?”)

**Best for**
- Environments where downloading tools at build time is not feasible and you can accept binary lifecycle management.

### Option C: Provide Syft via a standard toolchain image (container) for CI
**What it is**
- Your CI uses a runner/container image that already contains Syft (+ R, etc.).

**Pros**
- Most reproducible at scale.
- Great for multi-language projects.

**Cons**
- Requires containerized CI and image maintenance.

**Best for**
- Your “next iteration” when you start supporting multi-language repos consistently.

### Option D: Install Syft from OS package manager
**Pros**
- Simple for some environments.

**Cons**
- Version pinning is harder; varies by runner.

---

## 3) CycloneDX version / schema choice when using Syft
It’s fine that you don’t know the CycloneDX version yet. The key is to make the choice:

- **explicit**
- **centrally configurable**
- **discoverable in output**

With Syft, you can start with:
- `-o cyclonedx-json` (Syft chooses what it supports best)
and later evolve to:
- `-o cyclonedx-json@1.5` (or similar) *if/when* you decide to pin a spec version (exact syntax depends on Syft release—this is why we pin Syft and document the output).

**Practical guidance for now**
- Pin a **Syft version** first (this effectively pins the CycloneDX dialect it emits).
- Treat that as your “schema contract” until policy demands otherwise.
- When Army/org guidance later mandates CycloneDX 1.x specifically, you update one place.

---

## 4) How Syft fits your “always from `renv.lock`” requirement
This is the one technical tension to be aware of:

- `renv.lock` is your *declared* dependency graph.
- Syft is best at discovering *what is present* in a filesystem/image.

So you’ll want to decide what SBOM represents:

1) **Declared dependencies SBOM** (from `renv.lock`) — best for “what this project depends on”
2) **Deployed artifact SBOM** (from built container / installed library) — best for “what actually shipped”

You can still use Syft and stay aligned with `renv.lock` by generating SBOMs from a **materialized library** that is produced by `renv::restore()` (e.g., `renv/library/...`). In other words: restore from lock → Syft scans the resulting library directory.

That yields an SBOM that is still *governed by* the lockfile but produced using a cross-language engine.

**Definition-of-done implication:** SBOM generation inherently validates `renv::restore()`.

---

## 5) What this means for your earlier “Imports vs Suggests” question
If you use Syft, you likely **don’t need the separate `sbom` R package at all**, so that specific `Imports`/`Suggests` question becomes irrelevant *for SBOM generation*.

You will, however, have a new dependency decision:
- Does `reproducibleai` *manage Syft installation* (download, verify, cache), or does the workflow do it?

I recommend:
- **Workflow** installs Syft (Option A), pinned.
- `reproducibleai` just checks `syft` is available and errors with a clear message if not.

That keeps your R package light and avoids shipping binaries—while still enabling a later move to vendoring if needed.

---

## 6) Two small decisions to proceed (so the workflow + docs are crisp)
1) **SBOM “target” for Syft to scan in R-package repos**  
   Choose one:
   - A) scan the repo directory (captures DESCRIPTION, maybe misses installed deps)
   - B) scan the restored `renv` library directory (recommended for dependency completeness)
2) **Syft acquisition approach for NIPR reality**  
   Choose one:
   - A) download Syft from GitHub Releases in CI (fastest start)
   - B) mirror Syft internally and download from your internal source

If you answer those two, I’ll draft:
- the exact GitHub Actions workflow skeleton (PR + main + release)
- the section text to put into `gov-sbom.qmd` under the R packages subsection
- the intended behavior contract for `reproducibleai::write_sbom()` (inputs/outputs/errors), assuming Syft is the generator.

## @mpdougherty

Thank you for these tradeoffs. I see the safety of Option A. Decisions: 1. SBOM target = B. SBOM artifact needs to document as env. 2. Syft aquisition = A. 

## Copilot

Proceed with **Syft download in CI (Option A)** and generate the SBOM by scanning the **materialized `renv` library** (Target B). That gives you an SBOM that is (a) governed by `renv.lock`, (b) multi-language-scalable later, and (c) produces an SBOM that reflects the **actual restored environment**.

Below is the concrete, manual implementation blueprint you can apply now, plus the text structure you can put into `gov-sbom.qmd`.

---

## A. CI workflow design (R packages; PR + main; informational)

### Triggers
- `pull_request` (internal feature branches)
- `push` to `main`

### Steps (required order)
1) **Checkout**
2) **Setup R**
3) **Restore from `renv.lock`**
   - `renv::restore()` with a clean library (CI default)
4) **Install Syft (pinned)**
   - Download a fixed Syft release for Linux runner
   - Put it on `PATH`
5) **Generate SBOM by scanning the restored `renv` library**
   - Determine the active renv library path (after restore)
   - Run Syft against that directory
6) **Write output to `artifacts/sbom/`**
7) **Upload workflow artifact** with **120-day retention**
8) (Optional) **Attach a minimal “environment manifest” alongside SBOM**
   - e.g., `sessionInfo()`, `renv::status()`, `renv.lock` copy, R version

### Key implementation note: “SBOM artifact needs to document as env”
To make the SBOM artifact package reviewable/auditable as “the environment,” treat the artifact as a **bundle**, not only the SBOM file:

**Artifact bundle contents (recommended minimum):**
- `artifacts/sbom/<pkg>_cyclonedx_sha-<shortsha>.json` (the SBOM)
- `artifacts/sbom/<pkg>_env_sha-<shortsha>.txt` (environment summary)
- `artifacts/sbom/renv.lock` (exact lockfile used to restore)

This makes later aggregation/triage much easier (“what did this SBOM represent?”).

---

## B. Release workflow design (GitHub Releases)

Trigger on tags (pick one convention):
- `v*` tags (e.g., `v2026.04.01`), or
- tag equals `DESCRIPTION` version (some teams do `2026.04.01`)

Steps are the same as CI, but instead of only “upload artifact,” also:
- **upload SBOM JSON (and env text) as Release assets**

This satisfies “delivered SBOM” expectations without needing central aggregation yet.

---

## C. The `reproducibleai::write_sbom()` responsibilities (Syft-based)

Since Syft is installed by the workflow, keep `reproducibleai` focused on policy and consistency:

### Suggested function contract
`write_sbom()` should:
1) Read package metadata from `DESCRIPTION`:
   - `Package`, `Version`
2) Determine git SHA from `GITHUB_SHA` (or fallback)
3) Determine renv library path (after restore)
4) Ensure `syft` is available (`Sys.which("syft")`)
5) Run Syft to generate CycloneDX JSON from the renv library directory
6) Write:
   - SBOM JSON to `artifacts/sbom/...json`
   - env summary text (`sessionInfo()`, renv library path, syft version, R version, platform)
   - copy of `renv.lock` used

### What it should record in the env summary
At minimum:
- Package name + version (from DESCRIPTION)
- Commit SHA (`GITHUB_SHA`)
- R version (`R.version.string`)
- OS/platform (`Sys.info()`)
- Syft version (`syft version`)
- renv library path used for scanning
- Timestamp (UTC)
- Hash of `renv.lock` (optional but useful)

This addresses your “SBOM artifact documents the environment” requirement even if CycloneDX metadata fields vary by Syft version.

---

## D. Concrete naming convention (bundle)
Example filenames (for commit `abc1234`):

- `artifacts/sbom/MVR-USER-GUIDE_cyclonedx_sha-abc1234.json`
- `artifacts/sbom/MVR-USER-GUIDE_env_sha-abc1234.txt`
- `artifacts/sbom/renv.lock`

(Keeping `renv.lock` un-renamed is nice for tooling; optionally also include a SHA-named copy.)

---

## E. What to document in `gov-sbom.qmd` (R packages section)
Since `gov-sbom.qmd` is currently empty, you can drop in a structure like:

1) **SBOM overview**
   - what an SBOM is
   - why CycloneDX
2) **What we generate**
   - CycloneDX JSON
   - generated from `renv.lock` via restored `renv` library scan
3) **R package workflow (this section)**
   - prerequisites: `renv.lock`, GitHub Actions enabled, `reproducibleai` in lockfile
   - CI: PR + main produces SBOM bundle artifact (120 days)
   - Releases: tag produces SBOM assets on GitHub Release
4) **Verification checklist**
   - SBOM exists, env text exists, lockfile included
   - restore succeeded
   - release assets attached (for tags)
5) **Troubleshooting**
   - restore failures
   - system deps missing for package compilation (common in CI)
   - syft download/version pin mismatch

---

## Two final “don’t-guess” questions (very small)
1) Do your R packages typically require **system dependencies** in CI (e.g., `sf`, `xml2`, `curl`), or are they mostly pure-R?  
   - This determines whether your SBOM job should use `r-lib/actions/setup-r-dependencies` and apt installs.
2) Do you want the SBOM job to also run `R CMD check` (separate job) or keep SBOM generation standalone for now?

If you answer those, I can provide a ready-to-paste **workflow YAML skeleton** (CI + release) and a ready-to-paste **R-packages subsection** for `gov-sbom.qmd` that matches your conventions (artifacts folder, naming, 120-day retention, informational PR checks).

## @mpdougherty

Questions: 1. I haven't written any CI workflows myself and the few I have used have always been developed by reputable and mature package authors. So I'm not exactly sure how to answer, but my packages always import lots of packages like `sf` that you mentioned. So I guess I'd always need system dependencies. Is that correct? 2. R CMD check is too heavy at this point. Note: Since I'm new to defining CI workflows, please provide specific instructions. 

## Copilot

1) Yes—if your packages commonly depend on things like **`sf`**, you should assume you’ll need **system dependencies** in CI (Linux libraries and headers). `renv::restore()` (and/or compiling packages from source) will fail without them. So your SBOM workflow needs an explicit “system deps” step or a strategy that avoids compilation (often not possible for `sf`-class stacks).

2) Understood: keep SBOM generation **standalone** for now (no `R CMD check`), so you can get the SBOM pipeline working first.

Below are **specific, step-by-step instructions** for implementing the **informational SBOM CI workflow** (PR + push to main) using:
- `renv.lock` as source of truth
- restore + scan the restored `renv` library
- Syft downloaded in CI (pinned)
- output bundle under `artifacts/sbom/`
- upload as Actions artifact (target 120 days, noting org policy may cap it)

Because this repo’s instructions forbid me from writing files, treat this as “copy/paste and adjust” guidance.

---

## Step 0 — Pre-reqs (once per repo)
1) Ensure the repo has:
   - `renv.lock`
   - `DESCRIPTION`
2) Ensure `reproducibleai` is in the repo’s lockfile:
   - In R (locally):
     - Install it from GitHub (however you normally do), then:
       - `renv::snapshot()`
   - Verify `renv.lock` now includes an entry for `reproducibleai`

3) Add ignore rules (recommended)
- `.gitignore`: add `/artifacts/`
- `.Rbuildignore`: add `^artifacts$`

(These changes keep artifacts out of the package build and out of git history.)

---

## Step 1 — Create the GitHub Actions workflow file
Create this file in the repo:

- `.github/workflows/sbom-r.yml`

If `.github/` doesn’t exist, create it.

---

## Step 2 — Use this workflow skeleton (copy/paste)
This is a working starting point that includes a **system dependencies** install step suitable for `sf`-type packages on Ubuntu runners.

> Notes before you paste:
> - The system deps list may need adjustment based on your dependency tree.
> - I’m using Ubuntu because it’s the most common for R CI and easiest for apt packages.
> - I’m keeping it informational: it runs but doesn’t gate merges unless you later mark it required in branch protection.

```yaml name=.github/workflows/sbom-r.yml
on:
  push:
    branches: [main]
  pull_request:

name: sbom-r

jobs:
  sbom:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      # System deps for packages like sf, units, terra, xml2, curl, etc.
      # You may need to add/remove packages depending on your repo.
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libcurl4-openssl-dev \
            libssl-dev \
            libxml2-dev \
            libfontconfig1-dev \
            libfreetype6-dev \
            libharfbuzz-dev \
            libfribidi-dev \
            libpng-dev \
            libtiff5-dev \
            libjpeg-dev \
            libgeos-dev \
            libproj-dev \
            proj-data \
            proj-bin \
            libgdal-dev \
            gdal-bin \
            libudunits2-dev \
            libsqlite3-dev \
            libicu-dev \
            make \
            g++ \
            git

      # Restore packages from renv.lock
      - name: Restore renv
        shell: Rscript {0}
        run: |
          install.packages("renv")
          renv::restore(prompt = FALSE)

      # Install Syft (pinned). You should pin a version and update intentionally.
      - name: Install Syft
        run: |
          SYFT_VERSION="v1.19.0"
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh \
            | sh -s -- -b "$HOME/.local/bin" "$SYFT_VERSION"
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          syft version

      # Generate SBOM + env bundle via your package API
      - name: Write SBOM bundle
        shell: Rscript {0}
        run: |
          dir.create("artifacts/sbom", recursive = TRUE, showWarnings = FALSE)

          # This assumes reproducibleai is installed by renv::restore()
          # and provides reproducibleai::write_sbom()
          reproducibleai::write_sbom(
            lockfile = "renv.lock",
            out_dir = "artifacts/sbom",
            format = "cyclonedx_json",
            overwrite = TRUE
          )

      - name: Upload SBOM artifact bundle
        uses: actions/upload-artifact@v4
        with:
          name: sbom-r-${{ github.repository }}-${{ github.sha }}
          path: artifacts/sbom
          retention-days: 120
```

### What you will need to adjust later
- **Syft version pin** (`SYFT_VERSION="v1.19.0"`): pick one, keep it stable.
- **System deps list**: add/remove based on failures.

---

## Step 3 — Verify it works (what to do after you commit)
1) Open a PR (feature branch → main)
2) Confirm the workflow runs and produces an artifact:
   - In the workflow run, check “Artifacts” → download the SBOM bundle
3) Confirm the artifact contains (minimum):
   - a CycloneDX JSON file
   - an environment summary file
   - a copy of `renv.lock`

If the workflow fails, the failure will almost always be one of:
- a missing system library (apt package)
- a package compile failure
- a mismatch between what `reproducibleai::write_sbom()` expects and what’s installed

---

## Step 4 — How to handle system dependency failures (practical debugging)
When a restore step fails compiling a package, look for lines like:
- `fatal error: ...: No such file or directory`
- `configure: error: ... not found`

Then map common errors to apt packages:
- `gdal-config not found` → `libgdal-dev` + `gdal-bin`
- `proj_api.h` / PROJ issues → `libproj-dev` + `proj-data` + `proj-bin`
- `geos_c.h` → `libgeos-dev`
- `udunits2.h` → `libudunits2-dev`
- `curl/curl.h` → `libcurl4-openssl-dev`
- `openssl/ssl.h` → `libssl-dev`
- `xml2-config` / `libxml` → `libxml2-dev`

This is normal; most mature workflows evolve this list over time.

---

## Step 5 — Next: add the Release workflow (later, after CI is stable)
Once the CI workflow is stable, add a second workflow that triggers on tags and uploads SBOM files to GitHub Releases. (We can draft that next.)

---

## Two quick follow-ups (so I can make the instructions even more “ready to run”)
1) Are you okay standardizing on **`ubuntu-latest`** runners for SBOM generation (even if developers use Windows locally)?
2) Do you use **GitHub-hosted runners** on NIPR, or **self-hosted** runners?  
   - This matters for how reliable `apt-get` and external downloads are.

If you answer those, I’ll tighten the workflow to your environment (especially the Syft download and system dependency installation assumptions) and I’ll draft the “R packages” section text for `gov-sbom.qmd` that explains exactly what users should expect.

## @mpdougherty

Clarification: 1. ubuntu-latest is fine. 2. I'm not exactly sure what this question is asking. Users are usually on NIPR, but unsensitive, releasable, public domain repos are hosted on GitHub. Question: I'm confused about which repo this work should take place. Let me clarify my intentions for target deployment of this proposed new functionality. All new R package projects in our department should implement the SBOM requirement defined here. My thought was that there would be a set of `reporducibleai` functions that would configure a new R project to implement this requirement. Similar to the pattern of the `usethis` R package, I'd imagined a `reproducibleai::use_sbom()` function that could be called from within any arbitrary R package project repo that would configure that repo to generate SBOM artifacts. Since `reproducibleai` is an R package, I'd like to deploy this functionality for itself, in addition to being able for it to define it for other R package repos. Is this functionality doable?

## Copilot

Yes—this is doable, and your instinct to mirror the **`usethis` “use_*()” pattern** is sound. The clean architecture is:

- Implement the “project configuration” helpers in **`MVR-GIS/reproducibleai`** (because that’s your reusable toolbelt).
- Those helpers **write files into whatever repo you run them from** (the target R package repo), e.g. add `.github/workflows/sbom-r.yml`, update `.gitignore`, update `.Rbuildignore`, and ensure `renv` includes `reproducibleai`.
- Then you dogfood it: run `reproducibleai::use_sbom()` inside the `reproducibleai` repo itself so it is SBOM-enabled too.

That said, this chat session is scoped to `MVR-GIS/MVR-User-Guide` and has a strict **no-writes** rule. So I can:
- design the approach,
- show what files would be created/edited,
- draft the documentation you’ll paste into `gov-sbom.qmd`,
- and outline the `reproducibleai` functions and their behavior,

…but I cannot actually implement or commit the changes in either repo from here.

---

## What you asked about “which repo”
There are **two different repos with two different roles**:

1) **`MVR-GIS/reproducibleai`** (tooling repo)
   - Contains:
     - `write_sbom()` (generates the SBOM bundle, calls Syft)
     - `use_sbom()` (configures a repo to generate SBOMs in CI)
   - This is where the **new functionality is developed and maintained**.

2) **Each R package repo (including `reproducibleai` itself)** (consumer repos)
   - Receives the configuration:
     - `.github/workflows/sbom-r.yml`
     - ignore rules
     - (optional) documentation pointer / badge / internal policy note
   - Each repo’s CI then produces SBOM artifacts.

3) **This repo: `MVR-GIS/MVR-User-Guide`** (documentation repo)
   - Documents the policy + how-to:
     - `gov-sbom.qmd` should explain:
       - why SBOM
       - what you generate
       - how to enable it in an R package repo via `reproducibleai::use_sbom()`
       - what artifacts to expect
       - troubleshooting

So: **implementation lives in `reproducibleai`; documentation lives here; configuration is applied per-project.**

---

## Is a `reproducibleai::use_sbom()` function feasible?
Yes. Concretely, it can do what `usethis` does:

### Responsibilities of `use_sbom()`
When run from the root of an R package repo, it should:

1) **Validate it’s an R package**
- Check `DESCRIPTION` exists.
- Read `Package` and `Version` from it (for later naming).

2) **Ensure `renv` is present**
- If `renv.lock` missing, prompt the user:
  - either initialize renv (`renv::init()`) or abort with a clear message.
- If present, proceed.

3) **Add SBOM workflow**
- Create `.github/workflows/sbom-r.yml` with your standard pipeline:
  - ubuntu-latest
  - apt system deps step
  - `renv::restore()`
  - install/pin Syft
  - call `reproducibleai::write_sbom(...)`
  - upload artifact with 120-day retention

4) **Add ignore rules**
- Add `/artifacts/` to `.gitignore` (create if missing)
- Add `^artifacts$` to `.Rbuildignore` (create if missing)

5) **Ensure `reproducibleai` is in the lockfile**
- If you want “A” (your choice): the repo should install `reproducibleai` via renv:
  - Add `reproducibleai` as a dependency (likely `Suggests` is enough for runtime, but for restore you mainly care it’s installed in the project library).
  - Then snapshot:
    - `renv::snapshot(prompt = FALSE)`
- In practice, many teams do this as:
  - install `reproducibleai` (from GitHub)
  - then `renv::snapshot()`

6) **Optionally add a small local helper**
- e.g. `scripts/sbom.R` that calls `reproducibleai::write_sbom()`
- This is optional, but can help developers run it locally.

### Output / user experience
- It should print a checklist of what changed and the next verification step:
  - “Commit these files”
  - “Open a PR and confirm the `sbom-r` workflow uploads an artifact”

---

## How does `reproducibleai::write_sbom()` fit?
`use_sbom()` configures the repo.
`write_sbom()` is what the workflow actually calls to generate:

- `artifacts/sbom/<pkg>_cyclonedx_sha-<sha>.json`
- `artifacts/sbom/<pkg>_env_sha-<sha>.txt`
- `artifacts/sbom/renv.lock` (copied)

Internally, `write_sbom()` calls Syft against the **restored renv library directory** (your decision), and writes a separate env manifest so the SBOM artifact is explicitly “the environment.”

---

## Answering your runner question (what I meant)
GitHub Actions can run on:

- **GitHub-hosted runners** (GitHub provides the VM; `ubuntu-latest` is typical)
- **Self-hosted runners** (your org runs the VM/host; GitHub dispatches jobs to it)

You’re saying repos are hosted on public GitHub and are unclassified—so you’ll almost certainly be using **GitHub-hosted runners** unless your org has specifically set up self-hosted runners. That’s good: it makes `apt-get` and downloading Syft straightforward.

---

## What I suggest as the next step (choose one)
Per the “offer options, wait for selection” rule, here are **3 options** for how to proceed from here:

### Option 1 (recommended): Specify the `reproducibleai` API and generated file templates first
- We write down:
  - `use_sbom()` behavior + arguments
  - `write_sbom()` behavior + artifact bundle format
  - the exact workflow YAML template as a string/template
- Then you implement it in `reproducibleai`.

**Pros:** lowest risk; best reusability.  
**Cons:** doesn’t immediately update a specific repo until you implement.

### Option 2: Document-first in `gov-sbom.qmd`, then implement `reproducibleai`
- Fill out the user guide chapter explaining the approach and expected outputs.
- Then implement the package.

**Pros:** helps align stakeholders early.  
**Cons:** docs can drift if implementation changes.

### Option 3: Pilot manually in one repo (without `use_sbom()`), then automate
- Add the workflow + ignore rules by hand in a single R package repo.
- Once it works, encode that into `use_sbom()`.

**Pros:** fastest path to a working SBOM artifact.  
**Cons:** you’ll duplicate effort once; higher chance of inconsistent early patterns.

---

## One clarifying question (so I can draft the function signature cleanly)
Do you want `use_sbom()` to be **non-interactive by default** (CI-friendly), or is it OK if it prompts the user (like `usethis` often does) when something is missing?

If you pick an option above and answer that question, I’ll draft:
- a concrete `use_sbom()` function spec (arguments + steps + edge cases),
- the exact workflow template it should write,
- and the R-packages subsection text to paste into `gov-sbom.qmd` describing “Run `reproducibleai::use_sbom()` to enable SBOM CI.”

## @mpdougherty

Clarification: 1. Following the common `usethis` pattern, I expect the the `use_sbom()` function to always be user initiated in an interactive session and run only once during initial repo setup. Question: From the contract established at the beginning of this chat session, as I make updates to the `MVR-GIS/reproducibleai` R package "tooling" repo, are we able to work those changes as well as the "documentation" needs in `MVR-GIS/MVR-User-Guide`?

## Copilot

Yes—with one important constraint from this repo’s chat contract: **we can work on both repos in this conversation only in a “manual / review-first / read-only” way** (design, draft files, give copy/paste instructions), but **I cannot perform write operations** (no commits/PRs/branches, and no agent sessions) in either repo from here.

So the workable approach is:

- We **design and draft** the `reproducibleai` changes (new functions, templates, tests, docs) as concrete file diffs you can apply in that repo.
- We **draft and/or outline** the documentation updates for `MVR-GIS/MVR-User-Guide` (notably `gov-sbom.qmd`) that explain the workflow and how to enable it with `reproducibleai::use_sbom()`.
- You (or a teammate) applies those changes and pushes them in GitHub.

If you want me to *inspect* files in `MVR-GIS/reproducibleai` to align with its existing structure/conventions (DESCRIPTION, existing `use_*` functions, internal helpers, etc.), I can do that too—but you’ll need to explicitly confirm the second target repo and (ideally) provide the branch/ref to inspect, since our “scope: target repo” rules prefer explicit repo scoping.

---

## How to do two-repo work cleanly (recommended workflow for this chat)

### Step 1 — Confirm the second target repo for inspection
Reply with:

- Target tooling repo: `MVR-GIS/reproducibleai`
- Branch/ref: `main` (or whichever)

Once you do that, I can read files there and propose changes that match its conventions.

### Step 2 — Produce two deliverables in parallel
**Deliverable A (tooling repo):** `reproducibleai`
- `reproducibleai::use_sbom()` (interactive, run once)
- `reproducibleai::write_sbom()` (CI callable; runs Syft; writes bundle)
- A workflow template file embedded in the package (or written from R)
- Minimal tests (at least for file-writing logic; Syft invocation can be mocked)
- Package docs (README / reference man pages)

**Deliverable B (documentation repo):** `MVR-User-Guide`
- Fill in `gov-sbom.qmd`:
  - conceptual intro
  - “R packages” section describing:
    - run `reproducibleai::use_sbom()`
    - what files it creates
    - what artifacts to expect
    - troubleshooting for `sf`/system deps
  - verification checklist

---

## Important note about the “single target repo” instruction
The `chat-manual` module says “operate only on the explicitly specified target repository.” You have now explicitly asked about working on `reproducibleai` too, so the safe way to comply is:

- You explicitly name `MVR-GIS/reproducibleai` as an additional target for this session (even if the session started with User Guide).
- When we discuss file paths, we’ll always qualify which repo we mean.

---

## One question to unblock the next step
Please confirm:

**Do you want me to inspect `MVR-GIS/reproducibleai` now (read-only) to draft `use_sbom()` and `write_sbom()` in a way that matches its current code style and dependency approach?** If yes, tell me the branch/ref (usually `main`).

## @mpdougherty

Understood. Yes, please inspect `MVR-GIS/reproducibleai` now (read-only) to draft use_sbom() and write_sbom() in a way that matches its current code style and dependency approach. All work on that repo is currently in the `main` branch`. Question: Since `MVR-GIS/MVR-User-Guide` is a "documentation" repo, I requested at the beginning of this chat to follow its `dev/instructions/CHAT_INSTRUCTIONS.md`. However, since `MVR-GIS/reproducibleai` is an "R Package" repo, are you able to apply its instructions to your rules for it stored in file `dev/instructions/CHAT_INSTRUCTIONS.md`? Since this functionality for defining chat instructions based on repo purpose is a key feature of the `MVR-GIS/reproducibleai`, I'd like to confirm if this platform supports this complexity in a single chat session. 

## Copilot

Yes—this platform can support that complexity in a single chat session, and we can apply **both repos’ instruction sets**, with one practical caveat: **we must resolve conflicts by taking the most restrictive rule**.

### How instruction composition works across two repos (what’s supported)
- You asked us to follow `MVR-GIS/MVR-User-Guide`’s modules at the start (chat-manual + goals + r-package + user-manual).
- You now explicitly added `MVR-GIS/reproducibleai` and asked me to read *its* `dev/instructions/CHAT_INSTRUCTIONS.md` and modules. I did:
  - reproducibleai recipe: `c("chat-manual", "goals", "r-package")`

### Do they conflict?
Not meaningfully:
- Both repos use the same **chat-manual** base module with the same key restriction: **read-only only; no writes; no PRs; no agent sessions**.
- User-Guide adds **user-manual** overlay (docs architecture/editorial standards). That overlay is relevant when we draft `gov-sbom.qmd`.
- reproducibleai adds **r-package** overlay (R Packages 2e guidance). That overlay is relevant when we draft `use_sbom()` / `write_sbom()`.

So in this single session we can:
- operate in **manual/review-first mode** (no write operations),
- draft R package changes consistent with reproducibleai patterns,
- draft documentation changes consistent with the user-manual overlay.

The only thing we can’t do is “switch off” the User-Guide rules when talking about reproducibleai—because the conflict rule says the most restrictive constraints win, and both repos already forbid writes anyway.

---

## What I found in `MVR-GIS/reproducibleai` (read-only inspection summary)
- Package is light: no Imports currently; only `Suggests` (knitr/pkgdown/rmarkdown/testthat).
- It already has a `use_*()` function: `use_instructions()` with a clear style:
  - strong input validation
  - explicit directory creation
  - writes templates assembled as strings
  - returns written paths invisibly
- Exports currently: `cleanup_session_backups`, `extract_copilot_chat`, `instructions_available`, `instructions_recipes`, `use_instructions`.

This is a good foundation for adding:
- `use_sbom()` (repo scaffolding, like `use_instructions()`)
- `write_sbom()` (runtime helper called by CI)

---

## 3 feasible implementation options for SBOM support inside `reproducibleai` (ranked)
### Option 1 (recommended): `use_sbom()` writes workflow + ignores; `write_sbom()` shells out to Syft
**What it is**
- Add:
  - `use_sbom()` that writes `.github/workflows/sbom-r.yml`, updates `.gitignore` and `.Rbuildignore`
  - `write_sbom()` that:
    - reads DESCRIPTION for Package/Version
    - reads `GITHUB_SHA`
    - finds the renv library path
    - runs `syft` on that library dir
    - writes SBOM JSON + env text + lockfile copy into `artifacts/sbom/`

**Pros**
- Meets your “one R package for R support” goal (no separate `sbom` R package).
- Scales to multi-language later (Syft foundation).
- Matches reproducibleai’s existing “write files from templates” pattern.

**Cons**
- CI must install Syft (you chose this; acceptable).
- Some logic around “find renv library path” must be robust.

**Tradeoff**
- Slightly more complex than an R-only generator, but future-proof.

### Option 2: `use_sbom()` only; keep `write_sbom()` as a simple wrapper script in the target repo
**Pros**
- Fewer moving parts inside reproducibleai.

**Cons**
- Duplicates logic across repos; you lose the benefit of centralizing behavior.

### Option 3: Don’t add `use_sbom()` yet; publish a “copy/paste” workflow in docs
**Pros**
- Fastest to start.

**Cons**
- Inconsistent adoption; later refactor effort.

Given what you asked for, **Option 1** is the clear fit.

---

## Draft API + behavior (aligned with reproducibleai’s current style)

### `use_sbom()` (interactive, run once)
**Intent:** scaffold SBOM support into the *current working directory* R package repo.

Suggested signature:
```r
use_sbom <- function(
  dest_dir = ".",
  workflow = c("r-package"),
  overwrite = FALSE,
  quiet = FALSE
)
```

**What it should do**
1) Validate interactive session:
   - `if (!interactive()) stop("use_sbom() must be run interactively...", call.=FALSE)`
2) Validate it’s an R package:
   - ensure `DESCRIPTION` exists in `dest_dir`
3) Create directories if missing:
   - `.github/workflows/`
4) Write workflow file:
   - `.github/workflows/sbom-r.yml`
   - Use embedded template (string) with your defaults:
     - ubuntu-latest
     - apt system deps list (sf stack)
     - renv restore
     - install syft pinned
     - call `reproducibleai::write_sbom()`
     - upload artifact with retention-days: 120
5) Update ignore files:
   - add `/artifacts/` to `.gitignore`
   - add `^artifacts$` to `.Rbuildignore`
6) Message next steps:
   - “Run `renv::snapshot()` after installing reproducibleai (if not already in renv.lock)”
   - “Commit these files and open a PR”

**Return value**
- Invisibly return character vector of paths written/modified (like `use_instructions()`).

> Note: Whether it should also *edit* `renv.lock` is a substantive decision. In pure `usethis` style, it could guide the user rather than silently changing lockfiles. Since you want it run once interactively, you can choose either behavior. I’d default to **not** editing renv automatically at first—print explicit instructions instead.

### `write_sbom()` (non-interactive friendly; called in CI)
Suggested signature:
```r
write_sbom <- function(
  lockfile = "renv.lock",
  out_dir = file.path("artifacts", "sbom"),
  syft = Sys.which("syft"),
  syft_output = "cyclonedx-json",
  overwrite = TRUE,
  quiet = FALSE
)
```

**Behavior**
- Read DESCRIPTION `Package` + `Version` (and include them in env text; SBOM JSON metadata injection may be limited if Syft controls it)
- Determine SHA:
  - `sha <- Sys.getenv("GITHUB_SHA")`
  - if empty, try calling git
- Determine renv library path:
  - Prefer a renv API if available (e.g., `renv::paths$library()` if it exists in your renv version)
  - Otherwise, read `.Rprofile` renv activation? (more brittle)
- Run syft against the renv library path:
  - `syft <renv-library-dir> -o cyclonedx-json=<outfile>`
- Write env manifest text file alongside it:
  - R version, platform, syft version, sha, package version, renv library path, lockfile hash
- Copy `renv.lock` into `out_dir` (as documentation of the environment source)

---

## Answering your “can we apply both repos’ instructions” question directly
- **Yes**, because:
  - You explicitly specified both repos.
  - I can read both repos’ instruction modules.
  - There’s no conflict in permissions (both forbid writes).
  - We can draft code for reproducibleai under the r-package overlay, and draft docs for User-Guide under user-manual overlay.
- **No automatic enforcement exists** beyond what I do as the assistant; meaning: I can follow both sets, but there isn’t a platform feature that “auto-switches instruction overlays per repo” unless we explicitly read and apply them (which we just did).

---

## Next step: I can now draft the actual file contents
To draft `use_sbom()` and `write_sbom()` precisely (and consistent with your repo), I need one more read-only inspection in reproducibleai:

1) Do you already ship any templates under `inst/` (similar to `inst/instructions/`) that we should mirror for workflow templates?
2) What is currently in `inst/`?

If you agree, I’ll inspect `inst/` and `tests/` in `MVR-GIS/reproducibleai`, then provide:
- a new `R/use_sbom.R` file draft,
- a new `R/write_sbom.R` file draft,
- any helper functions (e.g., `R/utils_sbom.R`),
- and a suggested minimal `tests/testthat/` set for the file-writing logic.

## @mpdougherty

Excellent news that you can follow "both repos instructions". I like option 1. I can follow the Draft API function signatures and steps. However, before we plunge into drafting the new file contents, since we have now defined this functionality, can we perform a side quest to ensure that we aren't reinventing the wheel already developed by someone for this seemingly common cyber security task?

## Copilot

Yes—this is a good side quest. There *are* existing, mature OSS “wheels” here, and we should be explicit about what you’d be reinventing vs what’s unique about your `reproducibleai` value-add.

## What already exists (and what it does)

### 1) GitHub can export an SBOM from the Dependency Graph (SPDX only)
GitHub can export an SBOM for a repository from the dependency graph in **SPDX** format via UI or REST API ([Exporting a software bill of materials for your repository](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/establish-provenance-and-integrity/exporting-a-software-bill-of-materials-for-your-repository)). There’s also a REST endpoint: `GET /repos/{owner}/{repo}/dependency-graph/sbom` that returns SPDX JSON ([REST API endpoints for software bill of materials (SBOM)](https://docs.github.com/en/rest/dependency-graph/sboms)).

**Why this doesn’t replace your plan**
- You want **CycloneDX** (not SPDX-only).
- You want SBOM that’s tied to a build-like moment (“restored environment from `renv.lock`”) and bundled with env metadata artifacts.
- Dependency Graph coverage varies by ecosystem and detection method; you explicitly want `renv.lock` as truth.

### 2) GitHub Actions: “Anchore SBOM Action” uses Syft
GitHub documentation explicitly lists an **Anchore SBOM Action** that uses **Syft** to create SBOMs in workflows ([Exporting a software bill of materials for your repository](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/establish-provenance-and-integrity/exporting-a-software-bill-of-materials-for-your-repository)).

That means the “Syft in CI” approach is not a weird one-off—it’s a documented mainstream pattern.

**Key point:** using the action would likely remove the need to download Syft yourself in YAML.

### 3) GitHub Dependency Submission API (optional, later)
GitHub documents the **Dependency Submission API**, including that you can submit SBOMs as snapshots and that the Anchore SBOM action can both generate and submit ([Using the dependency submission API](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/secure-your-dependencies/using-the-dependency-submission-api)).

**Why it’s “later” for you**
- Submitting requires repo write permissions for the job (per the docs), which is more governance than you want right now.
- You’re focused on generating artifacts and being aggregation-ready later.

---

## So are we reinventing the wheel?
If you implement *everything* yourself (Syft install, SBOM generation commands, retention, env bundle, release assets), you’ll be duplicating what existing actions already do for the Syft install + SBOM generation portion.

But you’re *not* reinventing the wheel if `reproducibleai` provides these unique pieces:

1) **Standardized repo setup (`use_sbom()`)** across many internal repos (like `usethis`)
2) **Army/team policy conventions baked in**:
   - output paths (`artifacts/sbom`)
   - naming conventions
   - 120-day artifact retention
   - env bundle requirements (SBOM + env summary + `renv.lock`)
3) **R/renv-specific “scan the restored renv library” discipline**
   - actions tend to SBOM “the repo” or “the container”; your plan is specifically “the restored R environment governed by `renv.lock`”

That “policy + consistency layer” is the real value-add.

---

## 3 options to avoid reinventing while keeping your `reproducibleai` UX (pick one)

### Option A (recommended): `use_sbom()` writes a workflow that uses Anchore’s SBOM Action
**What it is**
- In workflow YAML, use the existing GitHub Action that wraps Syft (instead of downloading Syft yourself).
- `reproducibleai::write_sbom()` could become optional or just handle the env bundle (session info + lockfile copy), while the action generates the SBOM JSON.

**Pros**
- Maximum “don’t reinvent the wheel”
- Less CI maintenance (no curl | sh installer)
- Still consistent across repos via `use_sbom()`

**Cons**
- You depend on a third-party action version pin (still OSS, but you must pin to a commit SHA for supply chain hygiene)

**Tradeoff**
- Convenience + less code vs reliance on an external action.

### Option B: Keep your current plan (download Syft), but accept it’s duplicating some action logic
**Pros**
- No third-party GitHub Action dependency; just Syft itself.
- Clear, auditable steps.

**Cons**
- More YAML and more maintenance.

### Option C: Use GitHub’s dependency graph SBOM export for baseline + your own CycloneDX SBOM for environment
**Pros**
- Gives you SPDX from GitHub “for free” plus CycloneDX from Syft.

**Cons**
- Two SBOMs, two formats; might confuse users unless documented carefully.

---

## Recommendation
Given your goals (multi-language soon, R env discipline, consistent setup), I recommend **Option A**: keep `reproducibleai::use_sbom()` as the standardizer, but use the **Anchore SBOM Action** to generate the Syft SBOM, since GitHub explicitly documents that approach ([Exporting a software bill of materials for your repository](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/establish-provenance-and-integrity/exporting-a-software-bill-of-materials-for-your-repository)).

Then `reproducibleai::write_sbom()` can focus on the parts the action won’t do the way you want:
- “scan the restored renv library” target selection logic (if needed)
- env manifest file (sessionInfo, renv status, lockfile copy)
- naming conventions / bundle structure

---

## One decision to proceed
Do you want to pivot to **Anchore SBOM Action** in the workflow template that `use_sbom()` writes?

- **Yes** → we minimize custom tooling, maximize maturity.
- **No** → we keep the explicit Syft download/install step.

Once you choose, I’ll draft the exact `use_sbom()` template(s) accordingly and ensure the docs in `gov-sbom.qmd` describe the chosen mechanism clearly.

## @mpdougherty

I like Option A. Although Option A introduces an external dependency, it seems from the GitHub documentation provided that the industry has already implemented this workflow and that we should simply adopt it. A quick reading of the docs indicates that many of the downstream compliance and reporting steps that we need to adopt are already implemented and under active maintenance. These seem to already support multilanguage repos meaning that our other language helper functions will leverage the same CI workflow. `use_sbom()` should use the existing Anchore SBOM Action. 

## Copilot

Adopt **Option A**: have `reproducibleai::use_sbom()` scaffold a workflow that uses the **Anchore SBOM Action (Syft-based)**, and keep your R code focused on (a) repo setup ergonomics and (b) producing the **env bundle** you care about (lockfile + session/environment manifest) alongside the SBOM.

That matches GitHub’s own recommended approach for SBOM generation in Actions ([Exporting a software bill of materials for your repository](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/establish-provenance-and-integrity/exporting-a-software-bill-of-materials-for-your-repository)) and keeps you on a path that will scale to **multi-language repos** without retooling.

Below is a concrete “what to build” plan, with **specific instructions** you can implement manually.

---

## 1) What changes in our earlier design
### What moves out of R
- Installing Syft
- Running `syft ... -o cyclonedx-json`

Those become the workflow’s responsibility via the Anchore action.

### What stays in `reproducibleai`
- `use_sbom()` (interactive, run once) to write:
  - `.github/workflows/sbom.yml` (or `sbom-r.yml`—name is yours)
  - ignore rules for `artifacts/`
  - optionally a small `scripts/` helper
- A small helper like `write_sbom_env()` (or `write_sbom_bundle_metadata()`) to write:
  - `artifacts/sbom/<pkg>_env_sha-<sha>.txt`
  - copy of `renv.lock` into `artifacts/sbom/renv.lock`

You *can* keep the name `write_sbom()`, but since the action writes the SBOM itself, a name like `write_sbom_env()` is less confusing.

---

## 2) Recommended workflow behavior (what `use_sbom()` should generate)

### Triggers
- `pull_request`
- `push` to `main`
- plus a **release/tag** trigger later (optional next iteration)

### Outputs (artifact bundle)
In `artifacts/sbom/`:
- `sbom.cdx.json` (CycloneDX SBOM produced by the action; name can be standardized)
- `<Package>_env_sha-<shortsha>.txt` (written by R)
- `renv.lock` (copied by R)

Upload `artifacts/sbom/` as an Actions artifact with **120-day retention**.

### Why this is multi-language friendly
Anchore/Syft can SBOM many ecosystems from a directory or image, so later multi-language projects can use the same workflow with minimal change (maybe only the “restore environment” step changes).

---

## 3) Specific implementation instructions for `reproducibleai`

### A. Add a new `use_sbom()` function (patterned after `use_instructions()`)
Mimic the structure of `use_instructions()`:
- validate inputs
- create directories
- write file templates as strings
- update ignore files idempotently
- return written paths invisibly

Key behavioral requirements you specified:
- **interactive only**
- “run once” during setup (so default `overwrite = FALSE` is sensible)

### B. Add a helper that writes the “env manifest”
Implement something like:

- reads `DESCRIPTION` (Package + Version)
- uses `Sys.getenv("GITHUB_SHA")` if present; otherwise tries git
- writes a plain text file with:
  - Package, Version
  - SHA
  - timestamp (UTC)
  - R version, platform
  - renv status summary (at minimum confirm lockfile exists; optionally `renv::diagnostics()` if you like, but keep it lightweight)
- copies `renv.lock` into `artifacts/sbom/`

This helper is fast and doesn’t need system libraries beyond base R.

### C. Dependency posture
To keep reproducibleai lean (consistent with its current DESCRIPTION):
- add `renv` in **Suggests** (or Imports if you truly require it)
- use base functions for file IO; avoid heavy new dependencies

---

## 4) Workflow template contents (high-level)
You asked for specific instructions, but we should avoid guessing the exact action name/inputs without checking. The GitHub doc you referenced states the “Anchore SBOM Action” exists and uses Syft ([Exporting a software bill of materials for your repository](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/establish-provenance-and-integrity/exporting-a-software-bill-of-materials-for-your-repository)).

**What we should do next (evidence-first):**
- Confirm the exact action repo (`anchore/sbom-action` vs similar), its inputs, and how to output CycloneDX JSON to a known path, and whether it can target a specific directory (your restored renv library).
- Then encode that in the workflow template that `use_sbom()` writes.

I can do that confirmation using the GitHub docs tool (`support-search`) or by directly referencing the action’s README once we know the repo.

---

## 5) Documentation update plan for `MVR-GIS/MVR-User-Guide` (`gov-sbom.qmd`)
Once the workflow is set, the R-packages section should instruct:

1) Add `reproducibleai` to your project (`renv`)
2) Run `reproducibleai::use_sbom()`
3) Commit created files
4) Open PR and verify artifact bundle exists
5) (Later) tag release to attach SBOM to GitHub Releases

This aligns with your “informational” posture at first (not a merge gate).

---

## 6) One quick verification step before drafting templates
To avoid reinventing and to keep this authoritative:

### I will look up the exact Anchore action usage
If you approve, I’ll do a GitHub-docs support lookup for:
- official name of the action
- how to pin it (best practice is pin to a commit SHA)
- how to set output format to CycloneDX JSON and output path
- how to SBOM a specific directory (renv library)

**Action needed from you:** confirm you want me to do that lookup now.

If yes, I’ll do it and then come back with:
- the exact YAML template `use_sbom()` should write (ready to paste)
- the exact behavior contract for `use_sbom()` and the env-manifest helper
- the corresponding “R packages” subsection text for `gov-sbom.qmd`

## @mpdougherty

Perform the lookup to define the specific implementation of the Anchore action. Then proceed with drafting templates. 

## Copilot

Use **Anchore SBOM Action** in the workflow, and **pin it by commit SHA** (best practice for third‑party actions), then have the workflow also run a small R step to write your “environment manifest” + copy `renv.lock` into the same `artifacts/sbom/` bundle.

### What the GitHub docs confirm (authoritative)
- GitHub explicitly lists **Anchore SBOM Action** as a way to generate an SBOM in Actions, and notes it uses **Syft** ([Exporting a software bill of materials for your repository](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/establish-provenance-and-integrity/exporting-a-software-bill-of-materials-for-your-repository)).
- GitHub recommends pinning third‑party actions to a **SHA** for security hardening (while noting the tradeoff with Dependabot alerting) ([Using pre-written building blocks in your workflow](https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/find-and-customize-actions)).
- GitHub also notes Anchore SBOM Action can be used to generate an SBOM and (optionally) submit to the dependency submission API ([Using the dependency submission API](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/secure-your-dependencies/using-the-dependency-submission-api)). You’re choosing *informational only*, so we won’t submit yet.

What the GitHub docs do **not** provide is the exact action inputs (e.g., exact parameter names to set CycloneDX output path or scan target dir). For that, we should treat the action’s Marketplace/README as the source of truth. Since this chat toolset is “GitHub docs search” rather than general web, I’ll structure templates so you can fill in the few fields directly from the Marketplace “copy snippet” (which is exactly what GitHub recommends doing) ([Using pre-written building blocks in your workflow](https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/find-and-customize-actions)).

---

# 1) Finalized workflow template (ready for `use_sbom()` to write)

## Design goals (what this template guarantees)
- runs on `pull_request` and `push` to `main`
- restores the project with `renv.lock`
- generates a Syft-based SBOM via **Anchore SBOM Action**
- writes an “env manifest” + copies `renv.lock` into `artifacts/sbom/`
- uploads the whole `artifacts/sbom/` directory as an artifact (120-day retention)
- does **not** require fork support
- does **not** do dependency submission (no write perms required)

### Template (with two “fill me in from Marketplace snippet” lines)
```yaml name=.github/workflows/sbom.yml
on:
  push:
    branches: [main]
  pull_request:

name: sbom

jobs:
  sbom:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install system dependencies (sf/gdal/proj/geos/etc.)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libcurl4-openssl-dev \
            libssl-dev \
            libxml2-dev \
            libfontconfig1-dev \
            libfreetype6-dev \
            libharfbuzz-dev \
            libfribidi-dev \
            libpng-dev \
            libtiff5-dev \
            libjpeg-dev \
            libgeos-dev \
            libproj-dev \
            proj-data \
            proj-bin \
            libgdal-dev \
            gdal-bin \
            libudunits2-dev \
            libsqlite3-dev \
            libicu-dev \
            make \
            g++ \
            git

      - name: Restore renv
        shell: Rscript {0}
        run: |
          install.packages("renv")
          renv::restore(prompt = FALSE)

      # --- Anchore SBOM Action step (fill from Marketplace "copy snippet") ---
      # Replace the `uses:` ref with a pinned commit SHA per GitHub guidance:
      # https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/find-and-customize-actions
      - name: Generate SBOM (Anchore/Syft)
        uses: anchore/sbom-action@<PINNED_COMMIT_SHA>
        with:
          # TODO: Fill these from the action docs:
          # - how to set format to CycloneDX JSON (cdx json)
          # - how to set output path to artifacts/sbom/sbom.cdx.json
          # - how to target the restored renv library directory (if supported)
          # If the action can only scan the workspace, that is acceptable for v1.
          # We still record renv + session environment in the env manifest below.

      - name: Write SBOM environment bundle metadata
        shell: Rscript {0}
        run: |
          dir.create("artifacts/sbom", recursive = TRUE, showWarnings = FALSE)
          reproducibleai::write_sbom_env(
            lockfile = "renv.lock",
            out_dir = "artifacts/sbom",
            overwrite = TRUE
          )

      - name: Upload SBOM artifact bundle
        uses: actions/upload-artifact@v4
        with:
          name: sbom-${{ github.repository }}-${{ github.sha }}
          path: artifacts/sbom
          retention-days: 120
```

### Why I left the Anchore inputs as TODO
Because the GitHub docs confirm the action exists and the security guidance (pinning), but not the action’s input schema. To avoid guessing wrong (and you asked for no speculation), the correct workflow authoring process is:
- go to the Marketplace listing for **Anchore SBOM Action**
- click “Copy to clipboard” for the exact step syntax ([Using pre-written building blocks in your workflow](https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/find-and-customize-actions))
- then replace the `@vX` with a **pinned commit SHA** per the same doc.

If you paste the Marketplace snippet here, I can “lock” the final template (still without writing to the repo).

---

# 2) Draft `reproducibleai` templates + functions (aligned with current style)

From inspection, `reproducibleai` already has a strong “write template files to disk” pattern (`use_instructions()`), so we’ll mirror that.

## A) New exported function: `use_sbom()`
Responsibilities:
- interactive-only (your choice)
- writes `.github/workflows/sbom.yml` using the template above
- adds `/artifacts/` to `.gitignore`
- adds `^artifacts$` to `.Rbuildignore`
- optionally prints: “Now commit these files; open PR to verify artifact”

## B) New exported function: `write_sbom_env()`
Responsibilities:
- create `out_dir`
- copy `renv.lock` into `out_dir/renv.lock`
- write env manifest text file:
  - package name/version from `DESCRIPTION`
  - `GITHUB_SHA`
  - R version/platform
  - timestamp (UTC)
  - (optional) `renv::paths$library()` and/or `renv` version
- **No Syft calls**; that’s the action’s job now.

This gives you the “SBOM artifact documents the environment” requirement regardless of what the action includes in CycloneDX metadata.

---

# 3) Documentation draft for `MVR-GIS/MVR-User-Guide` (`gov-sbom.qmd`)
Add an “R packages” subsection that says, roughly:

- Prereqs: `renv` adopted; `reproducibleai` installed
- Run `reproducibleai::use_sbom()` once during repo setup
- CI produces an artifact `artifacts/sbom/` containing:
  - CycloneDX SBOM (generated by Anchore SBOM Action / Syft)
  - env manifest + lockfile copy (generated by reproducibleai)
- Retention: 120 days (or org cap)
- Common failures: missing system deps for `sf` stack

(I can write the exact prose once the action snippet is pinned, so the doc doesn’t contain placeholders.)

---

# 4) Next thing I need from you (to remove the TODOs)
Please paste the “copy snippet” YAML block from the **Anchore SBOM Action** Marketplace page (just the step with `uses:` and `with:`). GitHub explicitly supports that workflow authoring pattern ([Using pre-written building blocks in your workflow](https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/find-and-customize-actions)).

With that snippet, I will:
- produce the final workflow template (CycloneDX JSON, output path, scan target if supported)
- draft `use_sbom()` so it writes that exact workflow
- draft `gov-sbom.qmd` text with no placeholders

If you prefer not to paste it, tell me what inputs you see for:
- format (CycloneDX JSON)
- output file/path
- scan directory target
and I’ll incorporate them.

## @mpdougherty

Anchore SBOM Action: uses: `uses: anchore/sbom-action@v0`, with: I'm not finding a clear specification for the `with` parameter at: `https://github.com/marketplace/actions/anchore-sbom-action`. Can you help me determine this?

## Copilot

GitHub’s documentation **does not** document the `with:` inputs for `anchore/sbom-action`—it only tells you the action exists and what it broadly does ([Exporting a software bill of materials for your repository](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/establish-provenance-and-integrity/exporting-a-software-bill-of-materials-for-your-repository)). So the reason you “aren’t finding a clear specification” on the GitHub Docs side is that **it isn’t there**; the authoritative source for `with:` is the action’s own README/Marketplace page.

Given that limitation, here’s the practical, no-guess way to determine the exact `with:` keys.

---

## How to determine `with:` parameters for Anchore SBOM Action (reliable method)

### Step 1: Open the action’s repository README (source of truth)
From the Marketplace page, there’s always a link to the action’s repository. For Anchore SBOM Action, the repo is:

- https://github.com/anchore/sbom-action

In that repo, check either:
- `README.md` (usually documents inputs/outputs), or
- `action.yml` (this is definitive—lists `inputs:` and `outputs:`)

**Why `action.yml` is definitive:** it is what GitHub uses to define the action interface.

### Step 2: Read `action.yml` → look for `inputs:`
You’re looking for input names like:
- output format (CycloneDX JSON vs SPDX)
- output file/path
- scan target (directory, image, etc.)
- whether to upload as artifact / submit dependency snapshot

### Step 3: Pin by SHA
Once you know the correct inputs and have it working with `@v0`, switch to pinning by **commit SHA** (GitHub recommends pinning to a SHA for third‑party actions) ([Using pre-written building blocks in your workflow](https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/find-and-customize-actions)).

---

## Important correction about formats
GitHub’s docs say Anchore SBOM Action creates **SPDX 2.2 compatible SBOMs** ([Exporting a software bill of materials for your repository](https://docs.github.com/en/code-security/how-tos/secure-your-supply-chain/establish-provenance-and-integrity/exporting-a-software-bill-of-materials-for-your-repository)). That doesn’t necessarily mean it *can’t* output CycloneDX—but it does mean we should **not assume** CycloneDX support until we confirm in `action.yml`/README.

If Anchore SBOM Action only outputs SPDX, you have two paths:

1) Generate SPDX with Anchore SBOM Action (still valuable and GitHub-supported), and keep CycloneDX as a later enhancement.
2) Use Syft directly (or another action) for CycloneDX if CycloneDX is non-negotiable now.

We can decide after we inspect the action definition.

---

## Next: I can inspect `anchore/sbom-action` now (read-only)
To remove ambiguity, I can use GitHub REST “get file contents” to read:
- `action.yml` (or `action.yaml`)
- relevant README sections

### Quick clarification before I do:
Do you want the immediate requirement to be:
- **A)** “SBOM in any standard format is acceptable now (SPDX okay), CycloneDX later”, or
- **B)** “CycloneDX is required now, so if Anchore SBOM Action can’t do it, we switch approach”?

Reply with A or B and I’ll proceed to inspect the action’s `action.yml` and come back with:
- the exact `with:` keys you should use, and
- the final workflow template that `reproducibleai::use_sbom()` should write.

## @mpdougherty

Clarification: A. 

## Copilot

The `with:` parameters are defined in the action’s **`action.yml`** (that file is the definitive interface for a GitHub Action). I pulled it read-only, and here are the inputs you can use for `anchore/sbom-action@v0` (or better: a pinned commit SHA).

### Anchore SBOM Action inputs (`with:`)
From `action.yml` in `anchore/sbom-action`:

- `path` (default `"."`): directory to scan
- `file`: file to scan
- `image`: container image to scan
- `registry-username`, `registry-password`: for private registry images
- `format` (default `"spdx-json"`): SBOM output format
- `artifact-name`: name to use for the SBOM file generated by the action
- `output-file`: file path to write the SBOM to
- `syft-version`: pin syft version used by the action
- `dependency-snapshot` (default `"false"`): submit to GitHub dependency snapshot API
- `upload-artifact` (default `"true"`): upload SBOM as workflow artifact
- `upload-artifact-retention` (default `0`, with note about max 90 days unless repo settings change)
- `upload-release-assets` (default `"true"`): attach as release assets (relevant on release workflows)
- `config`: config file

Source (permalink):  
```yaml name=action.yml url=https://github.com/anchore/sbom-action/blob/f0d33c151c04af6fcbf4363834e838fcc7c87783/action.yml#L7-L79
# (See inputs: ...)
```

---

## Important implication for your 120-day retention requirement
Anchore’s input `upload-artifact-retention` is documented in the action as having a **90-day maximum unless changed in repo settings**. That’s consistent with GitHub Actions artifact retention policies.

So for **120 days**, you have two practical choices:

1) **Use `actions/upload-artifact@v4` yourself** (recommended) with `retention-days: 120`, and configure repo/org settings to allow it.
2) Use Anchore’s built-in artifact upload (but you may be capped at 90 depending on settings).

Given your earlier plan to bundle multiple files (`sbom` + `env` + `renv.lock`) under `artifacts/sbom/`, I recommend you set:

- `upload-artifact: false` on the Anchore step (so it only writes the SBOM file),
- then you upload the whole bundle with `actions/upload-artifact@v4`.

---

# Draft workflow template (now fully specified)
This is the version I recommend `reproducibleai::use_sbom()` write for R package repos **right now** (SPDX JSON output is acceptable per your choice A; CycloneDX later if desired).

```yaml name=.github/workflows/sbom.yml
on:
  push:
    branches: [main]
  pull_request:

name: sbom

jobs:
  sbom:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install system dependencies (sf/gdal/proj/geos/etc.)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libcurl4-openssl-dev \
            libssl-dev \
            libxml2-dev \
            libfontconfig1-dev \
            libfreetype6-dev \
            libharfbuzz-dev \
            libfribidi-dev \
            libpng-dev \
            libtiff5-dev \
            libjpeg-dev \
            libgeos-dev \
            libproj-dev \
            proj-data \
            proj-bin \
            libgdal-dev \
            gdal-bin \
            libudunits2-dev \
            libsqlite3-dev \
            libicu-dev \
            make \
            g++ \
            git

      - name: Restore renv
        shell: Rscript {0}
        run: |
          install.packages("renv")
          renv::restore(prompt = FALSE)

      # Generate SPDX JSON SBOM (Syft-based) using Anchore action
      # Replace @v0 with a pinned commit SHA once you finalize.
      - name: Generate SBOM (Anchore/Syft)
        uses: anchore/sbom-action@v0
        with:
          # Scan the restored renv library directory if you decide to set it explicitly later.
          # For now, scan the repo workspace (path: ".")—still useful and multi-language-friendly.
          path: "."
          format: "spdx-json"
          output-file: "artifacts/sbom/sbom.spdx.json"
          upload-artifact: "false"
          dependency-snapshot: "false"

      - name: Write SBOM environment bundle metadata
        shell: Rscript {0}
        run: |
          dir.create("artifacts/sbom", recursive = TRUE, showWarnings = FALSE)
          reproducibleai::write_sbom_env(
            lockfile = "renv.lock",
            out_dir = "artifacts/sbom",
            overwrite = TRUE
          )

      - name: Upload SBOM artifact bundle
        uses: actions/upload-artifact@v4
        with:
          name: sbom-${{ github.repository }}-${{ github.sha }}
          path: artifacts/sbom
          retention-days: 120
```

### Notes on “scan the restored renv library directory”
You chose “target = restored renv library” earlier. Anchore’s action supports `path` as “a path to a directory on the filesystem to scan.” If you want to scan the renv library instead of `"."`, we need a reliable way in workflow YAML to compute the library path after `renv::restore()`.

Two feasible ways (pick one later):
- Write the library path to `$GITHUB_OUTPUT` in an R step and feed it to the action via `${{ steps.<id>.outputs.<name> }}`.
- Or keep `path: "."` for v1, and treat the authoritative “environment” record as `renv.lock` + the env manifest you write (this is acceptable for your current “informational” posture).

Given you’re new to CI, I recommend **start with `path: "."`**, get stable runs, then iterate to scanning the library directory once the baseline works.

---

# Next step: draft `reproducibleai` templates/functions (no repo writes)
Before I draft file contents, one small inspection will help ensure we match repo conventions:

- What’s currently in `inst/` and `tests/` in `MVR-GIS/reproducibleai`?

If you want, I’ll inspect those directories next and then provide ready-to-paste drafts for:
- `R/use_sbom.R` (interactive scaffolding, like `use_instructions()`)
- `R/write_sbom_env.R` (fast env bundle writer)
- roxygen docs and NAMESPACE export entries
- minimal `testthat` tests for “writes expected files / idempotent ignore updates”

Tell me whether you want `use_sbom()` to:
- write workflow file name as `sbom.yml` (generic) or `sbom-r.yml` (explicit), and
- default `overwrite` to `FALSE` (my recommendation, since “run once”).

## @mpdougherty

Please inspect `inst/` and `tests/` in `MVR-GIS/reproducibleai`. Provide drafts for `use_sbom()` and `write_sbom_env()`. No need to provide roxygen man pages or NAMESPACE as I use `devtools::document()` for that purpose. Please draft tests. I'd prefer `sbom-r.yml` to distinguish from other languages in the future. default overwrite to FALSE. 

## Copilot

Below are **ready-to-paste drafts** for `MVR-GIS/reproducibleai` that match the existing style of `use_instructions()` (input validation, explicit file writes, `quiet` flag, invisible return) and the existing testthat approach.

You asked for:
- inspect `inst/` and `tests/` (done)
- drafts for `use_sbom()` and `write_sbom_env()`
- tests (yes)
- workflow file name `sbom-r.yml`
- default `overwrite = FALSE`
- no roxygen/NAMESPACE details (fine)

## What I observed in `reproducibleai`
- `inst/` currently only contains `inst/instructions/*.md` (no templates yet).
- `tests/` uses `testthat` and already has strong tests around file-writing behavior in `use_instructions()`.
- Those tests use `withr::local_tempdir()` but **`withr` is not listed** in DESCRIPTION currently. (It’s probably coming in transitively in your dev env; to make checks reliable, you should add `withr` to `Suggests`.)

---

# 1) New file: `R/use_sbom.R`
This writes:
- `.github/workflows/sbom-r.yml`
- updates `.gitignore` and `.Rbuildignore` idempotently
- interactive-only
- `overwrite = FALSE` default

```r name=R/use_sbom.R
use_sbom <- function(dest_dir = ".",
                     overwrite = FALSE,
                     quiet = FALSE) {
  # Interactive-only (usethis pattern) ----
  if (!interactive()) {
    stop("`use_sbom()` must be run in an interactive R session.", call. = FALSE)
  }

  # Validate inputs ----
  if (!is.character(dest_dir) || length(dest_dir) != 1 || !nzchar(dest_dir)) {
    stop("`dest_dir` must be a non-empty character scalar.", call. = FALSE)
  }
  if (!is.logical(overwrite) || length(overwrite) != 1) {
    stop("`overwrite` must be TRUE/FALSE.", call. = FALSE)
  }
  if (!is.logical(quiet) || length(quiet) != 1) {
    stop("`quiet` must be TRUE/FALSE.", call. = FALSE)
  }

  # Validate R package root ----
  desc_path <- file.path(dest_dir, "DESCRIPTION")
  if (!file.exists(desc_path)) {
    stop(
      "No DESCRIPTION found at: ", desc_path, "\n",
      "`use_sbom()` must be run from an R package project root (or supply `dest_dir`).",
      call. = FALSE
    )
  }

  out_paths <- character(0)

  # Ensure workflow directory exists ----
  workflows_dir <- file.path(dest_dir, ".github", "workflows")
  if (!dir.exists(workflows_dir)) {
    dir.create(workflows_dir, recursive = TRUE, showWarnings = FALSE)
    if (!dir.exists(workflows_dir)) {
      stop("Failed to create directory: ", workflows_dir, call. = FALSE)
    }
    if (!quiet) message("Created directory: ", workflows_dir)
  }

  # Write workflow file ----
  workflow_path <- file.path(workflows_dir, "sbom-r.yml")

  if (file.exists(workflow_path) && !overwrite) {
    stop(
      "Workflow already exists: ", workflow_path, "\n",
      "Set `overwrite = TRUE` to replace it.",
      call. = FALSE
    )
  }

  workflow_yml <- .sbom_workflow_template_r()

  writeLines(workflow_yml, con = workflow_path, useBytes = TRUE)
  out_paths <- c(out_paths, workflow_path)
  if (!quiet) message("Wrote: ", workflow_path)

  # Update ignore files ----
  gitignore_path <- file.path(dest_dir, ".gitignore")
  rbuildignore_path <- file.path(dest_dir, ".Rbuildignore")

  out_paths <- c(out_paths, .ensure_ignore_line(
    path = gitignore_path,
    line = "/artifacts/",
    quiet = quiet
  ))

  out_paths <- c(out_paths, .ensure_ignore_line(
    path = rbuildignore_path,
    line = "^artifacts$",
    quiet = quiet
  ))

  # Guidance ----
  if (!quiet) {
    message("")
    message("Next steps:")
    message("- Ensure `reproducibleai` is included in this project's renv.lock.")
    message("  (If needed: install it, then run `renv::snapshot()`.)")
    message("- Commit the new workflow and ignore-file updates.")
    message("- Open a PR and confirm the 'sbom-r' workflow uploads an SBOM artifact bundle.")
  }

  invisible(unique(out_paths))
}

.sbom_workflow_template_r <- function() {
  # Notes:
  # - Uses SPDX JSON because Anchore action default is spdx-json.
  # - upload-artifact is disabled in anchore step because we upload the whole bundle ourselves
  #   to enforce 120-day retention and include env manifest + renv.lock copy.
  paste(
    "on:",
    "  push:",
    "    branches: [main]",
    "  pull_request:",
    "",
    "name: sbom-r",
    "",
    "jobs:",
    "  sbom-r:",
    "    runs-on: ubuntu-latest",
    "",
    "    steps:",
    "      - name: Checkout",
    "        uses: actions/checkout@v4",
    "",
    "      - name: Setup R",
    "        uses: r-lib/actions/setup-r@v2",
    "        with:",
    "          use-public-rspm: true",
    "",
    "      - name: Install system dependencies (sf/gdal/proj/geos/etc.)",
    "        run: |",
    "          sudo apt-get update",
    "          sudo apt-get install -y --no-install-recommends \\",
    "            libcurl4-openssl-dev \\",
    "            libssl-dev \\",
    "            libxml2-dev \\",
    "            libfontconfig1-dev \\",
    "            libfreetype6-dev \\",
    "            libharfbuzz-dev \\",
    "            libfribidi-dev \\",
    "            libpng-dev \\",
    "            libtiff5-dev \\",
    "            libjpeg-dev \\",
    "            libgeos-dev \\",
    "            libproj-dev \\",
    "            proj-data \\",
    "            proj-bin \\",
    "            libgdal-dev \\",
    "            gdal-bin \\",
    "            libudunits2-dev \\",
    "            libsqlite3-dev \\",
    "            libicu-dev \\",
    "            make \\",
    "            g++ \\",
    "            git",
    "",
    "      - name: Restore renv",
    "        shell: Rscript {0}",
    "        run: |",
    "          install.packages(\"renv\")",
    "          renv::restore(prompt = FALSE)",
    "",
    "      - name: Generate SBOM (Anchore/Syft)",
    "        uses: anchore/sbom-action@v0",
    "        with:",
    "          path: \".\"",
    "          format: \"spdx-json\"",
    "          output-file: \"artifacts/sbom/sbom.spdx.json\"",
    "          upload-artifact: \"false\"",
    "          dependency-snapshot: \"false\"",
    "",
    "      - name: Write SBOM environment bundle metadata",
    "        shell: Rscript {0}",
    "        run: |",
    "          dir.create(\"artifacts/sbom\", recursive = TRUE, showWarnings = FALSE)",
    "          reproducibleai::write_sbom_env(",
    "            lockfile = \"renv.lock\",",
    "            out_dir = \"artifacts/sbom\",",
    "            overwrite = TRUE",
    "          )",
    "",
    "      - name: Upload SBOM artifact bundle",
    "        uses: actions/upload-artifact@v4",
    "        with:",
    "          name: sbom-${{ github.repository }}-${{ github.sha }}",
    "          path: artifacts/sbom",
    "          retention-days: 120",
    "",
    sep = "\n"
  )
}

.ensure_ignore_line <- function(path, line, quiet = FALSE) {
  stopifnot(is.character(path), length(path) == 1, nzchar(path))
  stopifnot(is.character(line), length(line) == 1, nzchar(line))
  stopifnot(is.logical(quiet), length(quiet) == 1)

  dir.create(dirname(path), recursive = TRUE, showWarnings = FALSE)

  if (!file.exists(path)) {
    writeLines(c(line, ""), con = path, useBytes = TRUE)
    if (!quiet) message("Wrote: ", path)
    return(path)
  }

  existing <- readLines(path, warn = FALSE)
  if (any(trimws(existing) == trimws(line))) {
    # already present (idempotent)
    return(path)
  }

  # append with newline separation
  new <- c(existing, line)
  writeLines(new, con = path, useBytes = TRUE)
  if (!quiet) message("Updated: ", path)

  path
}
```

### Notes / rationale
- I embedded the YAML template as a string function (similar to how `use_instructions()` builds its entrypoint text). If you later prefer shipping templates under `inst/templates/`, you can swap `.sbom_workflow_template_r()` to read from `system.file()`.
- `.ensure_ignore_line()` is implemented locally; you could factor it into a shared utility later if you add more `use_*()` scaffolds.

---

# 2) New file: `R/write_sbom_env.R`
This writes the env manifest and copies `renv.lock`.

```r name=R/write_sbom_env.R
write_sbom_env <- function(lockfile = "renv.lock",
                           out_dir = file.path("artifacts", "sbom"),
                           overwrite = TRUE,
                           quiet = FALSE) {
  # Validate inputs ----
  if (!is.character(lockfile) || length(lockfile) != 1 || !nzchar(lockfile)) {
    stop("`lockfile` must be a non-empty character scalar.", call. = FALSE)
  }
  if (!is.character(out_dir) || length(out_dir) != 1 || !nzchar(out_dir)) {
    stop("`out_dir` must be a non-empty character scalar.", call. = FALSE)
  }
  if (!is.logical(overwrite) || length(overwrite) != 1) {
    stop("`overwrite` must be TRUE/FALSE.", call. = FALSE)
  }
  if (!is.logical(quiet) || length(quiet) != 1) {
    stop("`quiet` must be TRUE/FALSE.", call. = FALSE)
  }

  # Validate expected files ----
  if (!file.exists("DESCRIPTION")) {
    stop("No DESCRIPTION found in working directory. Run from an R package root.", call. = FALSE)
  }
  if (!file.exists(lockfile)) {
    stop("Lockfile not found: ", lockfile, call. = FALSE)
  }

  # Ensure output dir ----
  if (!dir.exists(out_dir)) {
    dir.create(out_dir, recursive = TRUE, showWarnings = FALSE)
    if (!dir.exists(out_dir)) {
      stop("Failed to create output directory: ", out_dir, call. = FALSE)
    }
    if (!quiet) message("Created directory: ", out_dir)
  }

  # Read DESCRIPTION metadata (lightweight parsing) ----
  pkg <- .read_dcf_field("DESCRIPTION", "Package")
  ver <- .read_dcf_field("DESCRIPTION", "Version")

  # Determine commit SHA (prefer GitHub Actions env var) ----
  sha <- Sys.getenv("GITHUB_SHA")
  if (!nzchar(sha)) {
    sha <- .try_git_sha()
  }
  sha_short <- if (nzchar(sha)) substr(sha, 1, 7) else "unknown"

  env_path <- file.path(out_dir, paste0(pkg, "_env_sha-", sha_short, ".txt"))
  lock_copy_path <- file.path(out_dir, "renv.lock")

  if (!overwrite && file.exists(env_path)) {
    stop("Env manifest already exists: ", env_path, " (overwrite=FALSE)", call. = FALSE)
  }
  if (!overwrite && file.exists(lock_copy_path)) {
    stop("Lockfile copy already exists: ", lock_copy_path, " (overwrite=FALSE)", call. = FALSE)
  }

  # Copy lockfile into bundle ----
  ok <- file.copy(lockfile, lock_copy_path, overwrite = overwrite)
  if (!ok) stop("Failed to copy lockfile to: ", lock_copy_path, call. = FALSE)
  if (!quiet) message("Wrote: ", lock_copy_path)

  # Write env manifest ----
  ts_utc <- format(as.POSIXct(Sys.time(), tz = "UTC"), "%Y-%m-%dT%H:%M:%SZ")

  lines <- c(
    paste0("package: ", pkg),
    paste0("version: ", ver),
    paste0("git_sha: ", if (nzchar(sha)) sha else "unknown"),
    paste0("timestamp_utc: ", ts_utc),
    paste0("r_version: ", R.version.string),
    paste0("platform: ", R.version$platform),
    paste0("os: ", Sys.info()[["sysname"]]),
    paste0("release: ", Sys.info()[["release"]]),
    paste0("machine: ", Sys.info()[["machine"]])
  )

  # Helpful extra context: renv version if installed
  if (requireNamespace("renv", quietly = TRUE)) {
    lines <- c(lines, paste0("renv_version: ", as.character(utils::packageVersion("renv"))))
  } else {
    lines <- c(lines, "renv_version: (renv not installed in this session)")
  }

  writeLines(lines, con = env_path, useBytes = TRUE)
  if (!quiet) message("Wrote: ", env_path)

  invisible(c(env_path, lock_copy_path))
}

.read_dcf_field <- function(path, field) {
  d <- tryCatch(
    read.dcf(path),
    error = function(e) stop("Failed to read DCF file: ", path, call. = FALSE)
  )
  if (!field %in% colnames(d)) {
    stop("Field '", field, "' not found in ", path, call. = FALSE)
  }
  val <- d[1, field]
  val <- trimws(val)
  if (!nzchar(val)) {
    stop("Field '", field, "' is empty in ", path, call. = FALSE)
  }
  val
}

.try_git_sha <- function() {
  if (Sys.which("git") == "") return("")
  out <- tryCatch(
    system2("git", c("rev-parse", "HEAD"), stdout = TRUE, stderr = TRUE),
    error = function(e) character(0)
  )
  if (length(out) == 0) return("")
  sha <- trimws(out[1])
  if (!grepl("^[0-9a-f]{40}$", sha)) return("")
  sha
}
```

---

# 3) Tests

## A) New test file: `tests/testthat/test-use_sbom.R`
This mirrors your existing `test-use_instructions.R` patterns: tempdir, write files, validate contents, error on overwrite.

```r name=tests/testthat/test-use_sbom.R
test_that("use_sbom() writes workflow + ignore rules", {
  tmp <- withr::local_tempdir()

  # Minimal DESCRIPTION to satisfy validation
  writeLines(
    c(
      "Package: examplepkg",
      "Title: Example",
      "Version: 0.0.1"
    ),
    con = file.path(tmp, "DESCRIPTION")
  )

  # Run
  out <- withr::with_dir(tmp, {
    use_sbom(dest_dir = ".", overwrite = FALSE, quiet = TRUE)
  })

  expect_type(out, "character")

  # Workflow exists
  wf <- file.path(tmp, ".github", "workflows", "sbom-r.yml")
  expect_true(file.exists(wf))

  wf_txt <- paste(readLines(wf, warn = FALSE), collapse = "\n")
  expect_match(wf_txt, "name: sbom-r", fixed = TRUE)
  expect_match(wf_txt, "anchore/sbom-action@v0", fixed = TRUE)
  expect_match(wf_txt, "output-file: \"artifacts/sbom/sbom.spdx.json\"", fixed = TRUE)
  expect_match(wf_txt, "retention-days: 120", fixed = TRUE)

  # Ignore files updated/created
  expect_true(file.exists(file.path(tmp, ".gitignore")))
  expect_true(file.exists(file.path(tmp, ".Rbuildignore")))

  gi <- readLines(file.path(tmp, ".gitignore"), warn = FALSE)
  expect_true(any(trimws(gi) == "/artifacts/"))

  rbi <- readLines(file.path(tmp, ".Rbuildignore"), warn = FALSE)
  expect_true(any(trimws(rbi) == "^artifacts$"))
})

test_that("use_sbom() errors if workflow exists and overwrite=FALSE", {
  tmp <- withr::local_tempdir()
  writeLines(
    c("Package: examplepkg", "Title: Example", "Version: 0.0.1"),
    con = file.path(tmp, "DESCRIPTION")
  )

  # Create existing workflow
  dir.create(file.path(tmp, ".github", "workflows"), recursive = TRUE, showWarnings = FALSE)
  writeLines("existing", con = file.path(tmp, ".github", "workflows", "sbom-r.yml"))

  expect_error(
    withr::with_dir(tmp, use_sbom(overwrite = FALSE, quiet = TRUE)),
    "already exists",
    fixed = FALSE
  )
})

test_that("use_sbom() validates package root", {
  tmp <- withr::local_tempdir()
  expect_error(
    withr::with_dir(tmp, use_sbom(quiet = TRUE)),
    "No DESCRIPTION",
    fixed = FALSE
  )
})
```

## B) New test file: `tests/testthat/test-write_sbom_env.R`
This validates the bundle outputs and SHA behavior.

```r name=tests/testthat/test-write_sbom_env.R
test_that("write_sbom_env() writes env manifest and lockfile copy", {
  tmp <- withr::local_tempdir()

  writeLines(
    c("Package: examplepkg", "Title: Example", "Version: 0.0.1"),
    con = file.path(tmp, "DESCRIPTION")
  )
  writeLines("{\"R\": {\"Version\": \"4.4.0\"}}", con = file.path(tmp, "renv.lock"))

  # Provide a stable SHA
  withr::local_envvar(GITHUB_SHA = "0123456789abcdef0123456789abcdef01234567")

  out <- withr::with_dir(tmp, {
    write_sbom_env(lockfile = "renv.lock", out_dir = "artifacts/sbom", overwrite = TRUE, quiet = TRUE)
  })

  expect_true(file.exists(file.path(tmp, "artifacts", "sbom", "renv.lock")))

  # env file name should use short SHA
  env_path <- file.path(tmp, "artifacts", "sbom", "examplepkg_env_sha-0123456.txt")
  expect_true(file.exists(env_path))

  env_txt <- paste(readLines(env_path, warn = FALSE), collapse = "\n")
  expect_match(env_txt, "package: examplepkg", fixed = TRUE)
  expect_match(env_txt, "version: 0.0.1", fixed = TRUE)
  expect_match(env_txt, "git_sha: 0123456789abcdef", fixed = FALSE)
})

test_that("write_sbom_env() errors if lockfile missing", {
  tmp <- withr::local_tempdir()
  writeLines(
    c("Package: examplepkg", "Title: Example", "Version: 0.0.1"),
    con = file.path(tmp, "DESCRIPTION")
  )

  expect_error(
    withr::with_dir(tmp, write_sbom_env(lockfile = "renv.lock", out_dir = "artifacts/sbom", quiet = TRUE)),
    "Lockfile not found",
    fixed = FALSE
  )
})
```

---

# 4) One necessary housekeeping change (for tests to work)
Your existing tests already use `withr::local_tempdir()`, but `withr` is **not** listed in `Suggests` in `DESCRIPTION`. To keep `R CMD check` reliable, you should add:

- `withr` to `Suggests:` in `DESCRIPTION`

(Otherwise tests may fail in clean environments.)

---

# 5) Verification steps (manual)
After adding these files in `reproducibleai`:

1) Run locally:
   - `devtools::document()`
   - `devtools::test()`
2) In an example target repo:
   - install dev version of reproducibleai
   - run `reproducibleai::use_sbom()`
   - commit changes
   - open PR and confirm the `sbom-r` workflow produces an artifact containing:
     - `sbom.spdx.json`
     - `<pkg>_env_sha-<sha>.txt`
     - `renv.lock`

---

## Two small questions before you implement
1) Should `use_sbom()` also create an empty `artifacts/.gitkeep`? (I’d say **no**, since artifacts are CI outputs and should not be committed.)
2) Do you want `write_sbom_env()` to also include a hash of `renv.lock` (e.g., sha256) in the env manifest? It’s a nice audit enhancement and doesn’t add dependencies.

## @mpdougherty

Clarification: 1. No. 2. Yes. `withr` added to DESCRIPTION Suggests. Apparently I missunderstood your roxygen2 question. Please redraft the use and write functions and always include function documentation metadata for producing man pages. 

## Copilot

Understood. Below are redrafted `use_sbom()` and `write_sbom_env()` **with full roxygen2 documentation**, plus updated tests. I also included the `renv.lock` **SHA-256 hash** in the env manifest (base R only) and noted the `withr` Suggests change.

I’m keeping the style consistent with your existing `use_instructions()` implementation (explicit validation, clear errors, `quiet`, invisible return).

---

## 0) One manual DESCRIPTION edit (required)
Add `withr` to `Suggests:` in `MVR-GIS/reproducibleai/DESCRIPTION` so tests are dependable in clean environments.

---

## 1) New file: `R/use_sbom.R`

```r name=R/use_sbom.R
#' Configure an R package repository to generate SBOM artifacts in CI
#'
#' Writes a GitHub Actions workflow (`.github/workflows/sbom-r.yml`) that generates
#' an SBOM using Anchore's SBOM Action (Syft-based), and writes additional
#' environment metadata (and a copy of `renv.lock`) into `artifacts/sbom/`.
#'
#' This function is intended to be run once during initial repository setup in
#' an interactive session (similar to the `usethis` pattern).
#'
#' What this function does:
#' - Validates that `dest_dir` is an R package root (has `DESCRIPTION`).
#' - Creates `.github/workflows/` if missing.
#' - Writes `.github/workflows/sbom-r.yml` (fails if it exists and `overwrite = FALSE`).
#' - Ensures ignore rules:
#'   - Adds `/artifacts/` to `.gitignore`
#'   - Adds `^artifacts$` to `.Rbuildignore`
#'
#' Next steps after running:
#' - Ensure `reproducibleai` is included in the project's `renv.lock`
#'   (install it and run `renv::snapshot()` if needed).
#' - Commit changes and open a PR to verify the workflow produces an SBOM bundle.
#'
#' @param dest_dir Character. Path to the repository root. Defaults to `"."`.
#' @param overwrite Logical. Overwrite an existing workflow file? Defaults to `FALSE`.
#' @param quiet Logical. Suppress informational messages? Defaults to `FALSE`.
#'
#' @return Character vector of written/updated file paths (invisibly).
#'
#' @export
use_sbom <- function(dest_dir = ".",
                     overwrite = FALSE,
                     quiet = FALSE) {
  # Interactive-only (usethis pattern) ----
  if (!interactive()) {
    stop("`use_sbom()` must be run in an interactive R session.", call. = FALSE)
  }

  # Validate inputs ----
  if (!is.character(dest_dir) || length(dest_dir) != 1 || !nzchar(dest_dir)) {
    stop("`dest_dir` must be a non-empty character scalar.", call. = FALSE)
  }
  if (!is.logical(overwrite) || length(overwrite) != 1) {
    stop("`overwrite` must be TRUE/FALSE.", call. = FALSE)
  }
  if (!is.logical(quiet) || length(quiet) != 1) {
    stop("`quiet` must be TRUE/FALSE.", call. = FALSE)
  }

  # Validate R package root ----
  desc_path <- file.path(dest_dir, "DESCRIPTION")
  if (!file.exists(desc_path)) {
    stop(
      "No DESCRIPTION found at: ", desc_path, "\n",
      "`use_sbom()` must be run from an R package project root (or supply `dest_dir`).",
      call. = FALSE
    )
  }

  out_paths <- character(0)

  # Ensure workflow directory exists ----
  workflows_dir <- file.path(dest_dir, ".github", "workflows")
  if (!dir.exists(workflows_dir)) {
    dir.create(workflows_dir, recursive = TRUE, showWarnings = FALSE)
    if (!dir.exists(workflows_dir)) {
      stop("Failed to create directory: ", workflows_dir, call. = FALSE)
    }
    if (!quiet) message("Created directory: ", workflows_dir)
  }

  # Write workflow file ----
  workflow_path <- file.path(workflows_dir, "sbom-r.yml")

  if (file.exists(workflow_path) && !overwrite) {
    stop(
      "Workflow already exists: ", workflow_path, "\n",
      "Set `overwrite = TRUE` to replace it.",
      call. = FALSE
    )
  }

  workflow_yml <- sbom_workflow_template_r()

  writeLines(workflow_yml, con = workflow_path, useBytes = TRUE)
  out_paths <- c(out_paths, workflow_path)
  if (!quiet) message("Wrote: ", workflow_path)

  # Update ignore files ----
  gitignore_path <- file.path(dest_dir, ".gitignore")
  rbuildignore_path <- file.path(dest_dir, ".Rbuildignore")

  out_paths <- c(out_paths, ensure_ignore_line(
    path = gitignore_path,
    line = "/artifacts/",
    quiet = quiet
  ))

  out_paths <- c(out_paths, ensure_ignore_line(
    path = rbuildignore_path,
    line = "^artifacts$",
    quiet = quiet
  ))

  # Guidance ----
  if (!quiet) {
    message("")
    message("Next steps:")
    message("- Ensure `reproducibleai` is included in this project's renv.lock.")
    message("  (If needed: install it, then run `renv::snapshot()`.)")
    message("- Commit the new workflow and ignore-file updates.")
    message("- Open a PR and confirm the 'sbom-r' workflow uploads an SBOM artifact bundle.")
  }

  invisible(unique(out_paths))
}

#' Workflow template for SBOM generation (R packages)
#'
#' Returns the GitHub Actions workflow YAML text used by `use_sbom()`.
#'
#' - Uses Anchore's SBOM Action (Syft-based) to generate an SBOM (default: SPDX JSON).
#' - Writes SBOM-adjacent environment metadata via `reproducibleai::write_sbom_env()`.
#' - Uploads the `artifacts/sbom/` directory as a single artifact bundle with
#'   120-day retention (subject to org/repo policy).
#'
#' @return A single character string containing the YAML workflow.
#' @keywords internal
sbom_workflow_template_r <- function() {
  paste(
    "on:",
    "  push:",
    "    branches: [main]",
    "  pull_request:",
    "",
    "name: sbom-r",
    "",
    "jobs:",
    "  sbom-r:",
    "    runs-on: ubuntu-latest",
    "",
    "    steps:",
    "      - name: Checkout",
    "        uses: actions/checkout@v4",
    "",
    "      - name: Setup R",
    "        uses: r-lib/actions/setup-r@v2",
    "        with:",
    "          use-public-rspm: true",
    "",
    "      - name: Install system dependencies (sf/gdal/proj/geos/etc.)",
    "        run: |",
    "          sudo apt-get update",
    "          sudo apt-get install -y --no-install-recommends \\",
    "            libcurl4-openssl-dev \\",
    "            libssl-dev \\",
    "            libxml2-dev \\",
    "            libfontconfig1-dev \\",
    "            libfreetype6-dev \\",
    "            libharfbuzz-dev \\",
    "            libfribidi-dev \\",
    "            libpng-dev \\",
    "            libtiff5-dev \\",
    "            libjpeg-dev \\",
    "            libgeos-dev \\",
    "            libproj-dev \\",
    "            proj-data \\",
    "            proj-bin \\",
    "            libgdal-dev \\",
    "            gdal-bin \\",
    "            libudunits2-dev \\",
    "            libsqlite3-dev \\",
    "            libicu-dev \\",
    "            make \\",
    "            g++ \\",
    "            git",
    "",
    "      - name: Restore renv",
    "        shell: Rscript {0}",
    "        run: |",
    "          install.packages(\"renv\")",
    "          renv::restore(prompt = FALSE)",
    "",
    "      - name: Generate SBOM (Anchore/Syft)",
    "        uses: anchore/sbom-action@v0",
    "        with:",
    "          path: \".\"",
    "          format: \"spdx-json\"",
    "          output-file: \"artifacts/sbom/sbom.spdx.json\"",
    "          upload-artifact: \"false\"",
    "          dependency-snapshot: \"false\"",
    "",
    "      - name: Write SBOM environment bundle metadata",
    "        shell: Rscript {0}",
    "        run: |",
    "          dir.create(\"artifacts/sbom\", recursive = TRUE, showWarnings = FALSE)",
    "          reproducibleai::write_sbom_env(",
    "            lockfile = \"renv.lock\",",
    "            out_dir = \"artifacts/sbom\",",
    "            overwrite = TRUE",
    "          )",
    "",
    "      - name: Upload SBOM artifact bundle",
    "        uses: actions/upload-artifact@v4",
    "        with:",
    "          name: sbom-${{ github.repository }}-${{ github.sha }}",
    "          path: artifacts/sbom",
    "          retention-days: 120",
    "",
    sep = "\n"
  )
}

#' Ensure a line exists in an ignore file (idempotent append)
#'
#' Creates the file if missing and appends the specified line if not already present.
#'
#' @param path File path to update/create.
#' @param line A single line to ensure is present.
#' @param quiet Logical. Suppress messages.
#'
#' @return The updated file path.
#' @keywords internal
ensure_ignore_line <- function(path, line, quiet = FALSE) {
  stopifnot(is.character(path), length(path) == 1, nzchar(path))
  stopifnot(is.character(line), length(line) == 1, nzchar(line))
  stopifnot(is.logical(quiet), length(quiet) == 1)

  dir.create(dirname(path), recursive = TRUE, showWarnings = FALSE)

  if (!file.exists(path)) {
    writeLines(c(line, ""), con = path, useBytes = TRUE)
    if (!quiet) message("Wrote: ", path)
    return(path)
  }

  existing <- readLines(path, warn = FALSE)
  if (any(trimws(existing) == trimws(line))) {
    return(path)
  }

  writeLines(c(existing, line), con = path, useBytes = TRUE)
  if (!quiet) message("Updated: ", path)

  path
}
```

---

## 2) New file: `R/write_sbom_env.R`
Includes SHA-256 of `renv.lock` (`tools::md5sum` is MD5; for SHA-256 we can use `openssl` package, but that adds a dependency; instead, use base R + `system2("sha256sum")` would be OS-specific. Since you asked for SHA-256 and want low cost OSS, the most portable-in-R solution is to **Suggest `openssl`**. If you want to avoid new deps entirely, the most portable base option is **MD5**.)

To avoid guessing, I’m giving you **two variants**. Pick one:

### Variant 1 (recommended): use `openssl::sha256()` (add `openssl` to Suggests)
- Most portable and deterministic across OS.
- One small dependency.

### Variant 2 (no new deps): use MD5 via `tools::md5sum()` (base R)

You asked “Yes, sha256” so below is **Variant 1**. If you truly do not want to add `openssl`, tell me and I’ll rewrite to MD5.

```r name=R/write_sbom_env.R
#' Write SBOM environment bundle metadata for an R package repo
#'
#' Writes a lightweight environment manifest and copies the specified `renv.lock`
#' into an SBOM artifact bundle directory (default: `artifacts/sbom`).
#'
#' This function is intended to be called from CI after `renv::restore()` and SBOM
#' generation steps (e.g., Anchore SBOM Action). It does not generate the SBOM
#' itself; it creates additional evidence that documents the environment associated
#' with the SBOM artifact.
#'
#' Files written:
#' - `<Package>_env_sha-<shortsha>.txt`: environment manifest (Package/Version, git SHA,
#'   R version/platform, timestamp, lockfile hash)
#' - `renv.lock`: copy of the lockfile used
#'
#' @param lockfile Character. Path to `renv.lock` (default `"renv.lock"`).
#' @param out_dir Character. Output directory for the SBOM bundle.
#'   Default `file.path("artifacts", "sbom")`.
#' @param overwrite Logical. Overwrite existing bundle files? Defaults to `TRUE`.
#' @param quiet Logical. Suppress informational messages? Defaults to `FALSE`.
#'
#' @return Character vector of written file paths (invisibly).
#'
#' @export
write_sbom_env <- function(lockfile = "renv.lock",
                           out_dir = file.path("artifacts", "sbom"),
                           overwrite = TRUE,
                           quiet = FALSE) {
  # Validate inputs ----
  if (!is.character(lockfile) || length(lockfile) != 1 || !nzchar(lockfile)) {
    stop("`lockfile` must be a non-empty character scalar.", call. = FALSE)
  }
  if (!is.character(out_dir) || length(out_dir) != 1 || !nzchar(out_dir)) {
    stop("`out_dir` must be a non-empty character scalar.", call. = FALSE)
  }
  if (!is.logical(overwrite) || length(overwrite) != 1) {
    stop("`overwrite` must be TRUE/FALSE.", call. = FALSE)
  }
  if (!is.logical(quiet) || length(quiet) != 1) {
    stop("`quiet` must be TRUE/FALSE.", call. = FALSE)
  }

  # Validate expected files ----
  if (!file.exists("DESCRIPTION")) {
    stop("No DESCRIPTION found in working directory. Run from an R package root.", call. = FALSE)
  }
  if (!file.exists(lockfile)) {
    stop("Lockfile not found: ", lockfile, call. = FALSE)
  }

  # Ensure output dir ----
  if (!dir.exists(out_dir)) {
    dir.create(out_dir, recursive = TRUE, showWarnings = FALSE)
    if (!dir.exists(out_dir)) {
      stop("Failed to create output directory: ", out_dir, call. = FALSE)
    }
    if (!quiet) message("Created directory: ", out_dir)
  }

  # Read DESCRIPTION metadata ----
  pkg <- read_dcf_field("DESCRIPTION", "Package")
  ver <- read_dcf_field("DESCRIPTION", "Version")

  # Determine commit SHA ----
  sha <- Sys.getenv("GITHUB_SHA")
  if (!nzchar(sha)) {
    sha <- try_git_sha()
  }
  sha_short <- if (nzchar(sha)) substr(sha, 1, 7) else "unknown"

  # Hash lockfile (sha256) ----
  lock_hash <- lockfile_sha256(lockfile)

  env_path <- file.path(out_dir, paste0(pkg, "_env_sha-", sha_short, ".txt"))
  lock_copy_path <- file.path(out_dir, "renv.lock")

  if (!overwrite && file.exists(env_path)) {
    stop("Env manifest already exists: ", env_path, " (overwrite=FALSE)", call. = FALSE)
  }
  if (!overwrite && file.exists(lock_copy_path)) {
    stop("Lockfile copy already exists: ", lock_copy_path, " (overwrite=FALSE)", call. = FALSE)
  }

  # Copy lockfile into bundle ----
  ok <- file.copy(lockfile, lock_copy_path, overwrite = overwrite)
  if (!ok) stop("Failed to copy lockfile to: ", lock_copy_path, call. = FALSE)
  if (!quiet) message("Wrote: ", lock_copy_path)

  # Write env manifest ----
  ts_utc <- format(as.POSIXct(Sys.time(), tz = "UTC"), "%Y-%m-%dT%H:%M:%SZ")

  lines <- c(
    paste0("package: ", pkg),
    paste0("version: ", ver),
    paste0("git_sha: ", if (nzchar(sha)) sha else "unknown"),
    paste0("timestamp_utc: ", ts_utc),
    paste0("r_version: ", R.version.string),
    paste0("platform: ", R.version$platform),
    paste0("os: ", Sys.info()[["sysname"]]),
    paste0("release: ", Sys.info()[["release"]]),
    paste0("machine: ", Sys.info()[["machine"]]),
    paste0("renv_lock_sha256: ", lock_hash)
  )

  if (requireNamespace("renv", quietly = TRUE)) {
    lines <- c(lines, paste0("renv_version: ", as.character(utils::packageVersion("renv"))))
  } else {
    lines <- c(lines, "renv_version: (renv not installed in this session)")
  }

  writeLines(lines, con = env_path, useBytes = TRUE)
  if (!quiet) message("Wrote: ", env_path)

  invisible(c(env_path, lock_copy_path))
}

#' Read a field from a DCF file (DESCRIPTION)
#' @keywords internal
read_dcf_field <- function(path, field) {
  d <- tryCatch(
    read.dcf(path),
    error = function(e) stop("Failed to read DCF file: ", path, call. = FALSE)
  )
  if (!field %in% colnames(d)) {
    stop("Field '", field, "' not found in ", path, call. = FALSE)
  }
  val <- trimws(d[1, field])
  if (!nzchar(val)) {
    stop("Field '", field, "' is empty in ", path, call. = FALSE)
  }
  val
}

#' Try to read git SHA from local repository
#' @keywords internal
try_git_sha <- function() {
  if (Sys.which("git") == "") return("")
  out <- tryCatch(
    system2("git", c("rev-parse", "HEAD"), stdout = TRUE, stderr = TRUE),
    error = function(e) character(0)
  )
  if (length(out) == 0) return("")
  sha <- trimws(out[1])
  if (!grepl("^[0-9a-f]{40}$", sha)) return("")
  sha
}

#' Compute sha256 hash of a file
#'
#' Uses `openssl::sha256()` (recommended for portability). This implies `openssl`
#' should be available (Suggests is acceptable if only used in CI / tooling).
#'
#' @keywords internal
lockfile_sha256 <- function(path) {
  if (!requireNamespace("openssl", quietly = TRUE)) {
    stop(
      "Package 'openssl' is required to compute sha256. ",
      "Add it to Suggests or switch to an MD5-based hash.",
      call. = FALSE
    )
  }
  raw <- readBin(path, what = "raw", n = file.info(path)$size)
  unname(openssl::sha256(raw))
}
```

### Dependency note for SHA-256
To keep this working in CI and locally, add `openssl` to `Suggests` too (unless you want me to switch to MD5). If you accept `openssl` as a dependency, it’s a reasonable one for security-adjacent tooling.

---

## 3) Tests

### A) `tests/testthat/test-use_sbom.R`

```r name=tests/testthat/test-use_sbom.R
test_that("use_sbom() writes workflow + ignore rules", {
  tmp <- withr::local_tempdir()

  # Minimal DESCRIPTION to satisfy validation
  writeLines(
    c(
      "Package: examplepkg",
      "Title: Example",
      "Version: 0.0.1"
    ),
    con = file.path(tmp, "DESCRIPTION")
  )

  out <- withr::with_dir(tmp, {
    use_sbom(dest_dir = ".", overwrite = FALSE, quiet = TRUE)
  })

  expect_type(out, "character")

  wf <- file.path(tmp, ".github", "workflows", "sbom-r.yml")
  expect_true(file.exists(wf))

  wf_txt <- paste(readLines(wf, warn = FALSE), collapse = "\n")
  expect_match(wf_txt, "name: sbom-r", fixed = TRUE)
  expect_match(wf_txt, "anchore/sbom-action@v0", fixed = TRUE)
  expect_match(wf_txt, "output-file: \"artifacts/sbom/sbom.spdx.json\"", fixed = TRUE)
  expect_match(wf_txt, "retention-days: 120", fixed = TRUE)

  expect_true(file.exists(file.path(tmp, ".gitignore")))
  expect_true(file.exists(file.path(tmp, ".Rbuildignore")))

  gi <- readLines(file.path(tmp, ".gitignore"), warn = FALSE)
  expect_true(any(trimws(gi) == "/artifacts/"))

  rbi <- readLines(file.path(tmp, ".Rbuildignore"), warn = FALSE)
  expect_true(any(trimws(rbi) == "^artifacts$"))
})

test_that("use_sbom() errors if workflow exists and overwrite=FALSE", {
  tmp <- withr::local_tempdir()
  writeLines(
    c("Package: examplepkg", "Title: Example", "Version: 0.0.1"),
    con = file.path(tmp, "DESCRIPTION")
  )

  dir.create(file.path(tmp, ".github", "workflows"), recursive = TRUE, showWarnings = FALSE)
  writeLines("existing", con = file.path(tmp, ".github", "workflows", "sbom-r.yml"))

  expect_error(
    withr::with_dir(tmp, use_sbom(overwrite = FALSE, quiet = TRUE)),
    "already exists",
    fixed = FALSE
  )
})

test_that("use_sbom() validates package root", {
  tmp <- withr::local_tempdir()
  expect_error(
    withr::with_dir(tmp, use_sbom(quiet = TRUE)),
    "No DESCRIPTION",
    fixed = FALSE
  )
})
```

### B) `tests/testthat/test-write_sbom_env.R`

```r name=tests/testthat/test-write_sbom_env.R
test_that("write_sbom_env() writes env manifest and lockfile copy (sha256)", {
  skip_if_not_installed("openssl")

  tmp <- withr::local_tempdir()

  writeLines(
    c("Package: examplepkg", "Title: Example", "Version: 0.0.1"),
    con = file.path(tmp, "DESCRIPTION")
  )
  writeLines("{\"R\": {\"Version\": \"4.4.0\"}}", con = file.path(tmp, "renv.lock"))

  withr::local_envvar(GITHUB_SHA = "0123456789abcdef0123456789abcdef01234567")

  out <- withr::with_dir(tmp, {
    write_sbom_env(lockfile = "renv.lock", out_dir = "artifacts/sbom", overwrite = TRUE, quiet = TRUE)
  })

  expect_true(file.exists(file.path(tmp, "artifacts", "sbom", "renv.lock")))

  env_path <- file.path(tmp, "artifacts", "sbom", "examplepkg_env_sha-0123456.txt")
  expect_true(file.exists(env_path))

  env_txt <- paste(readLines(env_path, warn = FALSE), collapse = "\n")
  expect_match(env_txt, "package: examplepkg", fixed = TRUE)
  expect_match(env_txt, "version: 0.0.1", fixed = TRUE)
  expect_match(env_txt, "git_sha: 0123456789abcdef", fixed = FALSE)
  expect_match(env_txt, "renv_lock_sha256: ", fixed = TRUE)
})

test_that("write_sbom_env() errors if lockfile missing", {
  tmp <- withr::local_tempdir()
  writeLines(
    c("Package: examplepkg", "Title: Example", "Version: 0.0.1"),
    con = file.path(tmp, "DESCRIPTION")
  )

  expect_error(
    withr::with_dir(tmp, write_sbom_env(lockfile = "renv.lock", out_dir = "artifacts/sbom", quiet = TRUE)),
    "Lockfile not found",
    fixed = FALSE
  )
})
```

---

## 4) Small implementation notes (so you aren’t surprised)
1) **`openssl`**: For sha256 in pure R, `openssl` is the most portable approach. If you add it to `Suggests`, your package remains light, and the test is already written to skip when not installed.
2) **Action pinning**: the workflow uses `anchore/sbom-action@v0` now; once stable, you should pin it to a commit SHA per GitHub guidance.
3) **Artifact retention**: `actions/upload-artifact@v4` requests 120 days, but GitHub org/repo policies can cap it.

---

## Next steps checklist (manual)
In `MVR-GIS/reproducibleai`:
- [ ] Add new files `R/use_sbom.R` and `R/write_sbom_env.R`
- [ ] Add `withr` and `openssl` to `Suggests` in DESCRIPTION
- [ ] Run `devtools::document()`
- [ ] Run `devtools::test()`

Then in a target R package repo:
- [ ] Add reproducibleai to `renv.lock`
- [ ] Run `reproducibleai::use_sbom()`
- [ ] Commit and PR, confirm the artifact bundle contains:
  - `sbom.spdx.json`
  - `<pkg>_env_sha-<sha>.txt`
  - `renv.lock`

If you want, next we can draft the **R packages** subsection for `MVR-GIS/MVR-User-Guide/gov-sbom.qmd` that documents these exact steps and explains what to expect from the workflow output.

## @mpdougherty

I added the functions and test to `MVR-GIS/reproducibleai` in commit 58e3cd8838f1dc0bc07dd1970a4cba6725c05baa. However, when I hit step "Add ... `renv.lock` I just realized that I ambitiously stated an untrue fact that our group has uniformly adopted `renv` across all of our R repos. I had not initialized `renv` for this R package. In the instructions that we develop for this functionality, we'll need to explain how to configure an R project to use `renv`. Can you provide me with the initial `renv` commands that we will adopt in our instructions?

## Copilot

Adopt a simple, uniform “initialize once, snapshot often” `renv` workflow for R package repos, and document it as a short sequence of commands that (a) creates `renv.lock`, (b) activates renv for the project, and (c) makes restores reproducible on other machines/CI.

Below is a recommended baseline you can put directly into your SBOM instructions.

---

## Minimal `renv` initialization for an R package repo (new or existing)

### 0) From the package root
In R (RStudio/Positron), set your working directory to the repo root (the folder containing `DESCRIPTION`).

### 1) Install `renv` (once per machine)
```r
install.packages("renv")
```

### 2) Initialize `renv` for the project
This creates (at minimum) a `renv.lock` and the activation infrastructure.
```r
renv::init()
```

**Common option for existing projects:** if you already have packages installed and want `renv` to capture what the project uses, `renv::init()` is still the right starting point. It will attempt to discover dependencies and create the lockfile.

### 3) Verify the lockfile exists
```r
file.exists("renv.lock")
```

### 4) Snapshot (update lockfile after changes)
Any time you add/remove/update dependencies (including adding `reproducibleai`), run:
```r
renv::snapshot()
```

### 5) Restore (what CI or a new developer machine does)
On a fresh checkout (or CI):
```r
renv::restore()
```

That’s the core loop your SBOM workflow depends on: **restore → generate SBOM**.

---

## Recommended “team standard” tweaks (still simple)

### A) Make sure `renv.lock` is committed
In Git:
- Commit `renv.lock`
- Commit the `renv/activate.R` file that `renv::init()` creates

Do **not** commit the entire package library; renv handles that locally/CI.

### B) If you use GitHub Actions, add this one-time CI note to docs
CI should always start with:
```r
install.packages("renv")
renv::restore(prompt = FALSE)
```
(Your SBOM workflow already follows this pattern.)

### C) When you add `reproducibleai` as a tool dependency
After installing it (from wherever you source it), run:
```r
renv::snapshot()
```
to ensure it’s recorded in `renv.lock`.

---

## Two small clarifying choices for your written instructions
To keep your docs unambiguous, decide and state:

1) Should new repos run `renv::init()` with defaults, or do you want to standardize options (e.g., `bare = TRUE`)?
2) Do you want renv to be mandatory for *all* R repos, or only those that must meet the SBOM requirement?

If you answer those, I can draft the exact “Getting started with renv” subsection to insert into `gov-sbom.qmd` (or wherever you want it), written in the same imperative style as your user-manual module expects.

## @mpdougherty

Clarifications: 1. I think recommending renv for all R projects is modern best practice.  Defaults are fine because the next steps will require the snapshot. 2. Recommended for all, but required for SBOM. 

## Copilot

Adopt **`renv::init()` with defaults** as the standard starting point for all R repos, and make it explicit that **SBOM is only supported/required when `renv.lock` exists** (i.e., `renv` is enabled).

Here is a concise, copy/paste-ready set of commands and the accompanying “policy language” you can use in your instructions.

---

## Standard `renv` setup commands (team baseline)

### One-time per machine
```r
install.packages("renv")
```

### One-time per repository (run from repo root; `DESCRIPTION` present for packages)
```r
renv::init()
```

### After adding/updating dependencies (including adding `reproducibleai`)
```r
renv::snapshot()
```

### On a fresh clone / CI / new machine
```r
renv::restore(prompt = FALSE)
```

That’s the complete “happy path” sequence.

---

## What to commit to git (make this explicit in the docs)
After `renv::init()`:

- **Commit**: `renv.lock`  
- **Commit**: `renv/activate.R` (created by renv; ensures the project activates renv when opened)
- **Do not commit**: the `renv/library/` directory (it is machine-specific and can be huge)

---

## Required vs recommended language (matches your clarification)
Use wording like:

- **Recommended:** Enable `renv` for all R projects to make dependencies reproducible and reviewable.
- **Required for SBOM:** SBOM generation requires a `renv.lock` file. If the repo does not use `renv`, the SBOM workflow is not supported.

---

## Verification checks (quick sanity steps)
After `renv::init()`:
```r
file.exists("renv.lock")
dir.exists("renv")
file.exists(file.path("renv", "activate.R"))
```

After `renv::snapshot()`:
```r
renv::status()
```

---

## Where this fits into your SBOM workflow instructions
In your `gov-sbom.qmd` “R packages” SBOM section, the sequence should read:

1) Enable `renv` (`renv::init()`) if not already enabled  
2) Install `reproducibleai` and `renv::snapshot()`  
3) Run `reproducibleai::use_sbom()`  
4) Commit changes, open PR, verify SBOM artifact bundle

If you want, I can draft the exact prose for these steps in `gov-sbom.qmd` (including a short troubleshooting note for common `renv::restore()` failures with `sf`-class system dependencies) in the user-manual style you’re using in this repo.

## @mpdougherty

Before we develop the "getting started" guide in `MVR-GIS/MVR-User-Guide/gov-sbom.qmd`, let's develop the primary documentation in a new vignette article in the `MVR-GIS/reproducibleai`. 

## Copilot

Do this in `MVR-GIS/reproducibleai` by adding a new vignette (R Markdown) that documents:

- prerequisites (`renv`, GitHub Actions)
- one-time setup: `renv::init()` (if needed), add `reproducibleai`, `renv::snapshot()`, then `reproducibleai::use_sbom()`
- what files get created/modified
- what CI produces (`artifacts/sbom/` bundle)
- how to verify and troubleshoot (especially system deps for `sf` stacks)
- how to pin the Anchore action to a SHA (later hardening step)

Because we’re in read-only/no-write mode here, below is a ready-to-paste vignette draft and a checklist for integrating it into your package.

---

## Option set (pick 1)
1) **Rmd vignette (`vignettes/sbom.Rmd`)** (recommended): simplest, matches existing `VignetteBuilder: knitr` and `Suggests: rmarkdown, knitr`.
2) **Quarto vignette**: possible, but you’re already using Rmd and this adds moving parts.
3) **README-only**: quicker, but vignettes are the right long-form home.

I recommend **Option 1**.

---

## Integration checklist (manual)
1) Create file: `vignettes/sbom.Rmd` (content below)
2) Ensure DESCRIPTION has (you likely already do):
   - `Suggests: knitr, rmarkdown` (present)
   - `VignetteBuilder: knitr` (present)
3) Run:
   - `devtools::build_vignettes()` (or `pkgbuild::build()`)
   - `devtools::check()` to confirm vignette builds
4) If you don’t want vignettes built on every CI run, you can configure CI later; for now, building locally is enough.

---

## Draft vignette: `vignettes/sbom.Rmd`

```rmarkdown name=vignettes/sbom.Rmd
---
title: "SBOM for R Package Repositories (renv + GitHub Actions)"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{SBOM for R Package Repositories (renv + GitHub Actions)}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

## Purpose

This vignette documents how to configure an **R package repository** to generate a
**Software Bill of Materials (SBOM)** as part of continuous integration (CI)
using:

- `renv` for reproducible dependency locking (`renv.lock`)
- GitHub Actions for CI
- Anchore SBOM Action (Syft-based) to generate an SBOM file (SPDX JSON by default)
- `reproducibleai` to standardize configuration (`use_sbom()`) and to write
  additional environment metadata (`write_sbom_env()`)

This workflow is designed to be:

- **repeatable** (same steps across repos)
- **auditable** (SBOM bundle includes `renv.lock` + environment manifest)
- **scalable** (multi-language projects can share the SBOM generation approach later)

## What gets produced

On every pull request and on merges to `main`, the workflow will generate an
artifact bundle under:

- `artifacts/sbom/`

Typically including:

- `sbom.spdx.json` (SBOM generated by the Anchore action)
- `<Package>_env_sha-<shortsha>.txt` (environment manifest generated by `reproducibleai`)
- `renv.lock` (copied into the bundle to document the dependency lock)

Artifacts are uploaded with a target retention of **120 days**, subject to
GitHub org/repo policy.

## Prerequisites

### GitHub Actions

Your repository must have GitHub Actions enabled.

### renv (required for SBOM)

SBOM generation in this workflow requires `renv.lock`.

If your repo does not yet use `renv`, initialize it from the package root:

```r
install.packages("renv")
renv::init()
```

Commit the resulting files:

- `renv.lock`
- `renv/activate.R`

Do **not** commit `renv/library/`.

## Step-by-step: enable SBOM in an R package repo

From the **package root** (folder containing `DESCRIPTION`):

### 1) Install `reproducibleai` and snapshot

Install `reproducibleai` using your standard method (e.g., from GitHub), then:

```r
renv::snapshot()
```

This ensures `reproducibleai` is recorded in `renv.lock` so CI can restore it.

### 2) Scaffold SBOM workflow

Run once:

```r
reproducibleai::use_sbom()
```

This will:

- write `.github/workflows/sbom-r.yml`
- add `/artifacts/` to `.gitignore`
- add `^artifacts$` to `.Rbuildignore`

Commit these changes.

### 3) Verify in CI

Open a pull request and confirm:

- the `sbom-r` workflow runs successfully
- the workflow run has an uploaded artifact containing `artifacts/sbom/`
- the bundle includes `sbom.spdx.json`, `renv.lock`, and an env manifest file

## Troubleshooting

### `renv::restore()` fails compiling packages (common with sf stacks)

If your package depends on `sf`, `terra`, `units`, `xml2`, etc., CI may fail due
to missing system libraries/headers on Ubuntu runners.

The default workflow written by `use_sbom()` installs a baseline set of Ubuntu
packages (GDAL/PROJ/GEOS/udunits, etc.), but you may need to extend it depending
on your dependency graph.

Look for errors like:

- `fatal error: ...: No such file or directory`
- `configure: error: ... not found`

Then add the corresponding Ubuntu `apt-get` package(s) to the workflow.

### SBOM format (SPDX vs CycloneDX)

The Anchore SBOM Action defaults to `format: spdx-json`. CycloneDX can be adopted
later if/when required; keep the workflow standardized across repos unless a
specific consumer mandates CycloneDX.

### Pinning the Anchore action (recommended hardening)

For stronger supply-chain controls, pin third-party actions to a full commit SHA
rather than a moving tag like `@v0`. After validating the workflow, replace:

- `uses: anchore/sbom-action@v0`

with a specific commit SHA.

## Appendix: what `write_sbom_env()` records

The environment manifest includes:

- Package name + version (from `DESCRIPTION`)
- git SHA (from `GITHUB_SHA` in CI when available)
- timestamp (UTC)
- R version + platform details
- sha256 hash of `renv.lock`

This provides a lightweight audit trail linking the SBOM file to the exact locked
dependency specification used by the repository.
```

---

## Two small clarifying questions before you paste this in
1) Do you want the vignette to assume installation of `reproducibleai` via **`renv::install("MVR-GIS/reproducibleai")`**, or keep it generic (“install using your standard method”) as I wrote?
2) Should the vignette explicitly mention that the Anchore action can scan a `path` and that you may later switch it from `"."` to a restored renv library directory, or keep that out for v1 simplicity?

## @mpdougherty

Clarification: No to both. Render Issue: Please update the vignette output without using delimited code fences that cannot be rendered in the browser "Workspace" panel. Please 4 space indented code blocks for browser render compatibility. 

## Copilot

````rmarkdown name=vignettes/sbom.Rmd
---
title: "SBOM for R Package Repositories (renv + GitHub Actions)"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{SBOM for R Package Repositories (renv + GitHub Actions)}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

## Purpose

This vignette documents how to configure an **R package repository** to generate a
**Software Bill of Materials (SBOM)** as part of continuous integration (CI)
using:

- `renv` for reproducible dependency locking (`renv.lock`)
- GitHub Actions for CI
- Anchore SBOM Action (Syft-based) to generate an SBOM file (SPDX JSON by default)
- `reproducibleai` to standardize configuration (`use_sbom()`) and to write
  additional environment metadata (`write_sbom_env()`)

This workflow is designed to be:

- **repeatable** (same steps across repos)
- **auditable** (SBOM bundle includes `renv.lock` + environment manifest)
- **scalable** (multi-language projects can share the SBOM generation approach later)

## What gets produced

On every pull request and on merges to `main`, the workflow will generate an
artifact bundle under:

- `artifacts/sbom/`

Typically including:

- `sbom.spdx.json` (SBOM generated by the Anchore action)
- `<Package>_env_sha-<shortsha>.txt` (environment manifest generated by `reproducibleai`)
- `renv.lock` (copied into the bundle to document the dependency lock)

Artifacts are uploaded with a target retention of **120 days**, subject to
GitHub org/repo policy.

## Prerequisites

### GitHub Actions

Your repository must have GitHub Actions enabled.

### renv (required for SBOM)

SBOM generation in this workflow requires `renv.lock`.

If your repo does not yet use `renv`, initialize it from the package root:

    install.packages("renv")
    renv::init()

Commit the resulting files:

- `renv.lock`
- `renv/activate.R`

Do **not** commit `renv/library/`.

## Step-by-step: enable SBOM in an R package repo

From the **package root** (folder containing `DESCRIPTION`):

### 1) Install `reproducibleai` and snapshot

Install `reproducibleai` using your standard method (for your environment), then:

    renv::snapshot()

This ensures `reproducibleai` is recorded in `renv.lock` so CI can restore it.

### 2) Scaffold SBOM workflow

Run once:

    reproducibleai::use_sbom()

This will:

- write `.github/workflows/sbom-r.yml`
- add `/artifacts/` to `.gitignore`
- add `^artifacts$` to `.Rbuildignore`

Commit these changes.

### 3) Verify in CI

Open a pull request and confirm:

- the `sbom-r` workflow runs successfully
- the workflow run has an uploaded artifact containing `artifacts/sbom/`
- the bundle includes `sbom.spdx.json`, `renv.lock`, and an env manifest file

## Troubleshooting

### `renv::restore()` fails compiling packages (common with sf stacks)

If your package depends on `sf`, `terra`, `units`, `xml2`, etc., CI may fail due
to missing system libraries/headers on Ubuntu runners.

The default workflow written by `use_sbom()` installs a baseline set of Ubuntu
packages (GDAL/PROJ/GEOS/udunits, etc.), but you may need to extend it depending
on your dependency graph.

Look for errors like:

- `fatal error: ...: No such file or directory`
- `configure: error: ... not found`

Then add the corresponding Ubuntu `apt-get` package(s) to the workflow.

### SBOM format (SPDX vs CycloneDX)

The Anchore SBOM Action defaults to SPDX JSON output (`format: spdx-json`).
CycloneDX can be adopted later if/when required.

### Pinning the Anchore action (recommended hardening)

For stronger supply-chain controls, pin third-party actions to a full commit SHA
rather than a moving tag like `@v0`. After validating the workflow, replace:

- `uses: anchore/sbom-action@v0`

with a specific commit SHA.

## Appendix: what `write_sbom_env()` records

The environment manifest includes:

- Package name + version (from `DESCRIPTION`)
- git SHA (from `GITHUB_SHA` in CI when available)
- timestamp (UTC)
- R version + platform details
- sha256 hash of `renv.lock`

This provides a lightweight audit trail linking the SBOM file to the exact locked
dependency specification used by the repository.
````

## @mpdougherty

Added the vignette in commit 85725a83d9db5674ec2887d53193a86500efeeaa. For the last several commits since deploying the new action, it has yet to complete. Are you able to see the logs of those actions?

## Copilot

Yes. The job is failing because the workflow expects the Anchore action to write the SBOM to `artifacts/sbom/sbom.spdx.json`, but that file is never created, so the post-step hits:

> `##[error] ENOENT: no such file or directory, open 'artifacts/sbom/sbom.spdx.json'`

Log excerpt (from the failing run on commit `85725a83d9db5674ec2887d53193a86500efeeaa`):

- `SBOM scan completed in: 2.829s`
- `##[error] ENOENT: no such file or directory, open 'artifacts/sbom/sbom.spdx.json'`

### Root cause
`anchore/sbom-action@v0` is not writing to your requested `output-file` path, likely because the destination directory does not exist at the time the action tries to write the file (your workflow only creates `artifacts/sbom` **after** the Anchore step).

Many tools/actions do **not** create parent directories for `output-file`; they expect them to exist.

### Fix (manual): create `artifacts/sbom/` before the Anchore step
In `.github/workflows/sbom-r.yml`, add a step before “Generate SBOM (Anchore/Syft)”:

```yaml
- name: Create SBOM output directory
  run: mkdir -p artifacts/sbom
```

So the relevant section becomes:

```yaml
      - name: Restore renv
        shell: Rscript {0}
        run: |
          install.packages("renv")
          renv::restore(prompt = FALSE)

      - name: Create SBOM output directory
        run: mkdir -p artifacts/sbom

      - name: Generate SBOM (Anchore/Syft)
        uses: anchore/sbom-action@v0
        with:
          path: "."
          format: "spdx-json"
          output-file: "artifacts/sbom/sbom.spdx.json"
          upload-artifact: "false"
          dependency-snapshot: "false"

      - name: Write SBOM environment bundle metadata
        shell: Rscript {0}
        run: |
          dir.create("artifacts/sbom", recursive = TRUE, showWarnings = FALSE)
          reproducibleai::write_sbom_env(
            lockfile = "renv.lock",
            out_dir = "artifacts/sbom",
            overwrite = TRUE
          )
```

### Secondary note from the logs (not failing yet, but will matter)
Earlier in the log, there’s a warning about missing system packages:

- `pandoc [required by knitr, pkgdown, rmarkdown]`
- plus others (cmake, libgit2-dev, libx11-dev)

This is likely coming from an R dependency installation diagnostic step elsewhere in your workflow(s). It’s not what caused the current failure, but if you later add steps that install/build vignettes or run checks, you may need to install `pandoc` (and possibly others) on the runner.

### After applying the fix
Re-run the workflow; you should see `artifacts/sbom/sbom.spdx.json` present and your subsequent `write_sbom_env()` + upload-artifact steps should succeed.

If you paste your current `sbom-r.yml` (or tell me the commit SHA that added it), I can point to the exact place to insert the `mkdir -p` step and confirm there aren’t any other ordering issues.

## @mpdougherty

See the changes in commit 4e02166feade08526464e925964bd21944670e5e. Still failing. Please advise.

## Copilot

The failure has moved earlier: the job now dies when trying to call your R function because **`reproducibleai` is not installed in the CI library after `renv::restore()`**.

Key log line (run for commit `4e02166feade08526464e925964bd21944670e5e`):

- `##[error] Error in loadNamespace(x) : there is no package called ‘reproducibleai’`

### Root cause
In the `reproducibleai` repo itself, `renv::restore()` does **not** install the package-under-test by default. In CI, you currently have:

1) restore renv
2) run `reproducibleai::write_sbom_env(...)`

But unless you explicitly **install the local package** (the repo contents) into the library, `library(reproducibleai)` / `reproducibleai::` calls will fail.

This is a common CI gotcha for packages: restoring dependencies is not the same as installing “this package”.

---

## Fix (recommended): install the local package before calling it
Add an R step after `renv::restore()`:

```yaml
- name: Install this package
  shell: Rscript {0}
  run: |
    install.packages("remotes")
    remotes::install_local(upgrade = "never", dependencies = FALSE)
```

Then your `write_sbom_env()` step will work.

### Why `remotes::install_local()`?
- It installs the package from the current repo directory.
- `dependencies = FALSE` avoids re-resolving deps (renv already handled deps).
- `upgrade = "never"` avoids accidental updates.

You could also use base R: `R CMD INSTALL .`, but `remotes::install_local()` is straightforward.

**Note:** this adds a dependency on `remotes` in CI only (installed ad hoc). You don’t need to add it to DESCRIPTION unless you want to.

---

## Alternative fix (more “renv native”, but slightly more complex)
Instead of installing via `remotes`, you can have `renv` install the project itself:

```r
renv::install(".")
```

…but in practice in CI, I still recommend the explicit “install this package” step because it’s clear and standard.

---

## Where to insert it in `sbom-r.yml`
Order should be:

1. checkout
2. system deps
3. `renv::restore()`
4. **install local package**
5. create `artifacts/sbom/`
6. Anchore SBOM action
7. `reproducibleai::write_sbom_env()`
8. upload artifact

---

## Secondary issue you may hit next: workflow scanning vs package install timing
Even if you install the package, the Anchore action step does not depend on R. It can run before or after installing `reproducibleai`. But `write_sbom_env()` must run after install.

---

## One more note from your logs (not the current failure)
The log also says:

- `The project is out-of-sync -- use renv::status() for details.`

That’s informational, but it’s worth checking locally:
- run `renv::status()`
- then `renv::snapshot()` if needed
so CI restores exactly what you expect.

---

## Quick confirmation question (so I can give you the exact YAML snippet cleanly)
In `sbom-r.yml`, is your “Write SBOM environment bundle metadata” step currently using `Rscript {0}` and directly calling `reproducibleai::write_sbom_env()` (as drafted)? If you paste that section, I’ll show the minimal diff with the new “Install this package” step inserted in the right place.

## @mpdougherty

```
on:
  push:
    branches: [main]
  pull_request:

name: sbom-r

jobs:
  sbom-r:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install system dependencies (sf/gdal/proj/geos/etc.)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libcurl4-openssl-dev \
            libssl-dev \
            libxml2-dev \
            libfontconfig1-dev \
            libfreetype6-dev \
            libharfbuzz-dev \
            libfribidi-dev \
            libpng-dev \
            libtiff5-dev \
            libjpeg-dev \
            libgeos-dev \
            libproj-dev \
            proj-data \
            proj-bin \
            libgdal-dev \
            gdal-bin \
            libudunits2-dev \
            libsqlite3-dev \
            libicu-dev \
            make \
            g++ \
            git

      - name: Restore renv
        shell: Rscript {0}
        run: |
          install.packages("renv")
          renv::restore(prompt = FALSE)

      - name: Install this package
        shell: Rscript {0}
        run: |
          install.packages("remotes")
          remotes::install_local(upgrade = "never", dependencies = FALSE)

      - name: Create SBOM output directory
        run: mkdir -p artifacts/sbom

      - name: Generate SBOM (Anchore/Syft)
        uses: anchore/sbom-action@v0
        with:
          path: "."
          format: "spdx-json"
          output-file: "artifacts/sbom/sbom.spdx.json"
          upload-artifact: "false"
          dependency-snapshot: "false"

      - name: Write SBOM environment bundle metadata
        shell: Rscript {0}
        run: |
          dir.create("artifacts/sbom", recursive = TRUE, showWarnings = FALSE)
          reproducibleai::write_sbom_env(
            lockfile = "renv.lock",
            out_dir = "artifacts/sbom",
            overwrite = TRUE
          )

      - name: Upload SBOM artifact bundle
        uses: actions/upload-artifact@v4
        with:
          name: sbom-${{ github.repository }}-${{ github.sha }}
          path: artifacts/sbom
          retention-days: 120


```
Here is the .yml with the changes you recommended: 

## Copilot

Your YAML now has the two critical ordering fixes (create `artifacts/sbom` before the Anchore step, and install the local package before calling `reproducibleai::write_sbom_env()`), so if it’s *still* failing, the next likely causes are:

1) `remotes::install_local(..., dependencies = FALSE)` is failing because required dependencies (e.g., `openssl`, `withr`) are not installed in the renv library in CI, or  
2) the Anchore step is still not writing `sbom.spdx.json` (less likely now that the directory exists), or  
3) `write_sbom_env()` is failing because `openssl` isn’t installed (your tests skip, but the function currently **errors** if `openssl` is missing).

### Most probable root cause: `openssl` not installed → `write_sbom_env()` errors
In the function draft you implemented, `lockfile_sha256()` does:

- `if (!requireNamespace("openssl", quietly = TRUE)) stop(...)`

That means CI must have `openssl` installed as an R package. Having it in `Suggests` is not enough unless it’s actually installed into the renv library used in CI.

#### Fix option A (recommended for SBOM workflow robustness): make sha256 optional with fallback
Change `write_sbom_env()` so that if `openssl` is missing, it still writes the env file but records something like:

- `renv_lock_sha256: (unavailable; openssl not installed)`

This avoids breaking SBOM generation for repos that don’t have `openssl` installed.

**Why recommended:** SBOM is supposed to be “informational” right now; failing the whole workflow because a hash helper dependency isn’t present will be frustrating across many repos.

#### Fix option B: ensure `openssl` is installed in CI
If you want sha256 to be mandatory, install `openssl` in CI before calling `write_sbom_env()`:

```yaml
      - name: Install R packages needed for SBOM metadata
        shell: Rscript {0}
        run: |
          install.packages("openssl")
```

But this bypasses renv’s lock (you’d be installing from CRAN outside the lockfile). The better renv-aligned way is: add `openssl` to your project dependencies and snapshot it into `renv.lock` so restore installs it.

For the `reproducibleai` repo itself: ensure `openssl` is in `renv.lock` and snapshot.

---

## Second likely issue: `remotes` install is outside renv
Right now you’re installing `remotes` from CRAN during the run:

```r
install.packages("remotes")
remotes::install_local(...)
```

That’s usually okay, but it *can* create “out-of-sync” warnings and makes your environment less purely lockfile-driven.

### Cleaner alternative: install local package with base R (no remotes)
Replace the “Install this package” step with:

```yaml
      - name: Install this package
        run: R CMD INSTALL .
```

That avoids needing `remotes` at all.

This is the most minimal, standard approach for an R package CI job.

---

## What I need to give a precise fix: the current failing log
Yes, I can see logs, but the tool response you pasted earlier only showed one job. To avoid guessing, please provide either:
- the latest failing run’s log excerpt around the error (10–30 lines), **or**
- the commit SHA associated with the failing run after these YAML changes

Then I’ll tell you exactly which of the above is happening.

---

## If you want to try the highest-probability fix immediately
Do these two changes:

### 1) Make `write_sbom_env()` tolerant if `openssl` is missing
Modify `lockfile_sha256()` to return a string instead of erroring:

- if openssl missing → return `"(unavailable; openssl not installed)"`

### 2) Replace remotes-based local install with `R CMD INSTALL .`
Update step:

```yaml
      - name: Install this package
        run: R CMD INSTALL .
```

Those two changes remove the most common remaining failure modes.

If you paste your current `write_sbom_env()` implementation (or just `lockfile_sha256()`), I can provide the exact patch for the “tolerant sha256” behavior in the same style as the rest of the package.

## @mpdougherty

```
#' Compute sha256 hash of a file
#'
#' Uses `openssl::sha256()` (recommended for portability). This implies `openssl`
#' should be available (Suggests is acceptable if only used in CI / tooling).
#'
#' @keywords internal
lockfile_sha256 <- function(path) {
  if (!requireNamespace("openssl", quietly = TRUE)) {
    stop(
      "Package 'openssl' is required to compute sha256. ",
      "Add it to Suggests or switch to an MD5-based hash.",
      call. = FALSE
    )
  }
  raw <- readBin(path, what = "raw", n = file.info(path)$size)
  unname(openssl::sha256(raw))
}
```

## Copilot

Update `lockfile_sha256()` to **never error** when `openssl` is missing. Instead, return a clear sentinel string. That will stop SBOM runs from failing just because a “nice-to-have” hash dependency isn’t installed.

Here’s a drop-in replacement in the same style:

```r name=R/write_sbom_env.R
#' Compute sha256 hash of a file
#'
#' Uses `openssl::sha256()` when available (recommended for portability). If
#' `openssl` is not installed, returns a sentinel string rather than erroring.
#'
#' @keywords internal
lockfile_sha256 <- function(path) {
  if (!file.exists(path)) {
    stop("File not found: ", path, call. = FALSE)
  }

  if (!requireNamespace("openssl", quietly = TRUE)) {
    return("(unavailable; package 'openssl' not installed)")
  }

  size <- file.info(path)$size
  if (is.na(size) || size < 0) {
    stop("Unable to read file size for: ", path, call. = FALSE)
  }

  raw <- readBin(path, what = "raw", n = size)
  unname(openssl::sha256(raw))
}
```

### Why this fixes your CI pain
- `write_sbom_env()` will keep working across repos that haven’t standardized on `openssl` yet.
- Your env manifest will still record *something* deterministic about the lockfile: the fact that sha256 is unavailable is itself an auditable signal.
- You can later make sha256 “required” by policy (and then flip it back to an error) once you’ve rolled `openssl` into all repos’ `renv.lock`.

---

## Optional (but recommended) workflow simplification
Replace the remotes-based local install step with:

```yaml
- name: Install this package
  run: R CMD INSTALL .
```

This removes the “install remotes” step and reduces non-lockfile drift.

---

## Verification steps (fast)
1) Locally (without installing `openssl`), run from package root:

    writeLines("{\"test\": true}", "renv.lock")
    dir.create("artifacts/sbom", recursive = TRUE, showWarnings = FALSE)
    reproducibleai::write_sbom_env(lockfile = "renv.lock", out_dir = "artifacts/sbom", overwrite = TRUE)

Confirm the env file includes:

- `renv_lock_sha256: (unavailable; package 'openssl' not installed)`

2) Re-run the GitHub Action; it should now proceed past `write_sbom_env()`.

If it still fails after this change, the next thing to check is whether the Anchore step is writing `sbom.spdx.json` (but with `mkdir -p artifacts/sbom` in place, that’s much less likely).

## @mpdougherty

I made the requested changes in commit: 1f54525e0945a2c1bebe58ef972949aae4c41149. Progress. But, here is the error message I'm now getting: Run actions/upload-artifact@v4
With the provided path, there will be 3 files uploaded
Error: The artifact name is not valid: sbom-MVR-GIS/reproducibleai-1f54525e0945a2c1bebe58ef972949aae4c41149. Contains the following character:  Forward slash /
          
Invalid characters include:  Double quote ", Colon :, Less than <, Greater than >, Vertical bar |, Asterisk *, Question mark ?, Carriage return \r, Line feed \n, Backslash \, Forward slash /
          
These characters are not allowed in the artifact name due to limitations with certain file systems such as NTFS. To maintain file system agnostic behavior, these characters are intentionally not allowed to prevent potential problems with downloads on different file systems.

## Copilot

Change the artifact `name:` so it doesn’t include `${{ github.repository }}` (it contains a `/`). Use `${{ github.repository_id }}` or `${{ github.event.repository.name }}` instead.

### Minimal fix in `.github/workflows/sbom-r.yml`
Replace:

    name: sbom-${{ github.repository }}-${{ github.sha }}

with one of these safe options:

**Option A (recommended; unique and slash-free):**
    name: sbom-${{ github.repository_id }}-${{ github.sha }}

**Option B (readable; repo name only):**
    name: sbom-${{ github.event.repository.name }}-${{ github.sha }}

So the final step becomes:

```yaml
      - name: Upload SBOM artifact bundle
        uses: actions/upload-artifact@v4
        with:
          name: sbom-${{ github.repository_id }}-${{ github.sha }}
          path: artifacts/sbom
          retention-days: 120
```

### Why this works
- `github.repository` is `owner/repo` → contains `/` → invalid for artifact names.
- `github.repository_id` is numeric → always safe.
- `github.event.repository.name` is just `reproducibleai` → safe and human-readable.

After that change, the upload-artifact step should succeed.
