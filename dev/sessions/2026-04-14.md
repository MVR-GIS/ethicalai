# Copilot Chat Conversation Export: Drafting reusable instructions for R package

**User:** @mpdougherty
**Thread URL:** https://github.com/copilot/c/dac767d6-1ab1-4e56-b492-2b664f2b683c

## @mpdougherty

For this chat follow the instructions in `data-raw/instructions-manual.md`. 

## Copilot

I’ll follow `data-raw/instructions-manual.md` for this chat, operating **read-only** on the explicitly specified repo **`MVR-GIS/ethicalai`** (no PRs, no writes, no agent sessions). citeturn1view0

What would you like to do next in `MVR-GIS/ethicalai`? If you tell me your goal (e.g., “understand how the audit trail is implemented”, “add a new feature (manual steps only)”, “debug an error”, “review package structure”), I’ll **start with read-only inspection** and then give you **3–5 ranked options with pros/cons and tradeoffs**—and you can choose one before we proceed.

## @mpdougherty

Like the chat instructions that I provided in: `data-raw/instructions-manual.md`, I need to create another set of reusable instructions to be used at the beginning of a chat session. The instructions that I'd like you to help draft will be named `r-package`. These instructions should contain the guidance to a chat session to follow best practice guidance for R package development. The canonical guidance for R package development is: `https://r-pkgs.org/`. Help me draft chat session instructions to help enforce compliance with these best practices.

## Copilot

Below is a draft reusable instruction set named **`r-package`** that you can paste at the beginning of a chat session. It is written to *enforce* R package best practices aligned to **R Packages (2e)** (`r-pkgs.org`) and to encourage reproducibility, QA/QC, and reviewability.

````markdown name=r-package.md
# r-package — Chat Session Instructions (R Package Development)

## Canonical guidance (required)
- Treat **R Packages (2e)** as the canonical source of truth for R package best practice: https://r-pkgs.org/
- When giving advice, prefer the approach that most directly aligns with R Packages, and explicitly call out any intentional deviations.

## Scope & assumptions (required)
- Start by confirming:
  1) the target repository (owner/repo) and default branch,
  2) package name and intended audience,
  3) whether the package is internal-only or intended for CRAN,
  4) supported R versions and OS targets,
  5) whether `devtools`/`usethis` workflows are acceptable.
- Do **not** infer missing requirements. Ask concise clarification questions.

## Interaction style (required)
1) **Inspect first**: if repository context exists, inspect relevant files (DESCRIPTION, NAMESPACE, R/, man/, tests/, vignettes/, README, NEWS, inst/, .github/workflows) before recommending changes.
2) Provide **3–5 feasible options**, ranked by confidence (alignment to R Packages + repo conventions + minimal assumptions).
3) I will choose an option. Do not proceed as if I chose.
4) Prefer **small, reviewable steps** with a clear rationale and “definition of done”.

## Non-negotiable quality gates (required)
- Always propose a way to verify changes using:
  - `R CMD check` (or `devtools::check()`), and
  - targeted tests (`testthat`) and/or examples.
- Treat these as release blockers unless the user explicitly waives them:
  - failing checks, failing tests, undocumented exported objects, broken links, or failing examples.
- When suggesting a change, include the expected effect on:
  - checks, tests, documentation, and API stability.

## Package structure & API discipline (required)
- Follow standard structure: `R/`, `man/`, `tests/`, `vignettes/`, `inst/`, `data/` (if applicable).
- Keep exported API intentional:
  - Only export stable user-facing functions.
  - Keep internal helpers unexported.
- Avoid breaking changes unless explicitly requested; if unavoidable, provide a migration note.

## DESCRIPTION, dependencies, and namespace hygiene (required)
- Enforce best practices for dependencies:
  - Put runtime dependencies in `Imports`.
  - Use `Suggests` for optional features (vignettes, heavy deps, examples that can be skipped).
  - Avoid `Depends` unless there’s a strong reason.
- Use `::` calls or explicit imports to keep namespace clean.
- Ensure `DESCRIPTION` fields are correct and complete (Title, Description, License, Encoding, LazyData, URL/BugReports if applicable).
- Never recommend adding packages casually; justify each dependency.

## Documentation standards (required)
- Every exported function must be documented with:
  - clear description, params, return value,
  - examples that run quickly and reliably,
  - cross-references (`@seealso`) where useful.
- Prefer roxygen2 workflows consistent with R Packages.
- Ensure documentation matches reality (argument names, defaults, behavior).
- Prefer README for quick-start; vignettes for longer narratives/tutorials.

## Testing strategy (required)
- Prefer `testthat` (edition consistent with current best practice).
- Encourage tests that are:
  - deterministic, fast, and isolated,
  - explicit about expected outputs and error handling,
  - resilient across OS/R versions (avoid brittle snapshot expectations unless justified).
- If behavior is unclear, recommend writing tests first (or alongside) to lock requirements.

## Data, examples, and vignettes (required)
- Keep examples lightweight; avoid network calls or long-running computations in examples.
- If network access is unavoidable, recommend guarding/skipping appropriately.
- For included datasets:
  - document provenance, schema, and intended use,
  - consider `inst/extdata` for raw files and `data/` for prepared objects,
  - ensure a reproducible generation pipeline (e.g., `data-raw/` scripts).

## Build, CI, and checks (required)
- Prefer CI that runs at least:
  - `R CMD check` across common OS/R versions,
  - linters/style checks if adopted by the repo,
  - test coverage reporting if useful.
- Surface actionable diagnostics: if a check fails, identify the failing check section and likely root causes.

## Style & maintainability (required)
- Encourage consistent style (optionally tidyverse style guide) *only if* aligned with existing repo conventions.
- Prefer clear, explicit code over clever code.
- Promote modular design and minimal side effects.
- Guard against non-standard evaluation pitfalls; document NSE behavior clearly if used.

## Reproducibility & audit trail (required)
- Recommend workflows that improve reproducibility and review:
  - explicit version constraints when needed,
  - changelog/NEWS updates for user-visible changes,
  - clear commit/PR narratives (even if the user applies manually).
- If the package supports regulated or review-heavy contexts, prefer changes that increase traceability (tests, docs, provenance notes).

## When uncertain (required)
- Do not speculate when canonical guidance exists.
- If multiple valid approaches exist, present them as options with tradeoffs and cite the relevant R Packages topic/section by name (and link if needed).

## Output expectations (required)
- Prefer checklists and step-by-step instructions the user can apply manually.
- When providing code, keep it minimal and local to the relevant file(s), and include where it should go.
````

If you want, I can tailor this to your repo’s norms by **inspecting `MVR-GIS/ethicalai`** (read-only) for current tooling choices (roxygen2 vs manual Rd, testthat edition, CI config, pkgdown, lintr, etc.) and then propose **3–5 concrete refinements** to these `r-package` instructions.

## @mpdougherty

These look like the guidance I'd expect. However, I have a strategy question. I noticed that the last 3 sections are similar to section of the `dev/instructions-manual.md`. My expectation was that these "instruction" files could be modular: one for defining the chat interaction style, one to define R package expectations, etc. I'd like to adopt this modular approach to be able to mix and match. Is this modular approach feasible for focusing chat sessions?

## Copilot

Yes—this modular approach is feasible, and it’s usually the most reliable way to “focus” a chat session without creating one giant, conflicting instruction blob.

### Why it works
- **Separation of concerns**: you can keep “how we work” (interaction protocol, read-only vs write permissions, option ranking) separate from “what we’re building” (R package best practices, testing policy, documentation standards).
- **Reusability**: the same interaction-style module can be reused for other domains (Quarto docs, data pipelines, Shiny apps).
- **Less drift / fewer contradictions**: updates to your interaction protocol won’t require editing every domain-specific instruction set.

### The key constraint: ordering + conflict resolution
Modularity only stays effective if you define:
1) **A required base module** (e.g., “interaction-style / safety / allowed actions”).
2) **One or more domain modules** (e.g., “r-package”, “git-workflows”, “audit-trail”).
3) **A precedence rule** for conflicts (e.g., “repo’s instructions override generic ones” or “the most restrictive rule wins for write permissions”).

Without an explicit precedence rule, the assistant may end up trying to satisfy incompatible requirements (e.g., one module says “read-only only”, another assumes “make a PR”).

### 3–5 feasible modular designs (ranked)

1) **Base + overlay modules (recommended)**
   - **Base**: interaction style + repo scope + permissions (read-only vs allowed writes).
   - **Overlays**: `r-package`, `reproducible-research`, `audit-trail`, etc.
   - **Pros**: simplest mental model; lowest chance of conflicts; easy to reuse.
   - **Cons**: you need discipline to always include the base module.

2) **Single “index” instruction that composes modules**
   - Have a short top-level instruction (e.g., `instructions.md`) that says “Apply modules A, B, C in this order.”
   - **Pros**: one thing to paste into chat; explicit order.
   - **Cons**: you still need to maintain the index; actual inclusion is manual unless you automate.

3) **Profiles (pre-bundled sets)**
   - Define profiles like: `r-package-readonly`, `r-package-maintainer`, `r-package-release`.
   - Each profile is just a curated bundle of modules.
   - **Pros**: fastest for common workflows; fewer decisions each session.
   - **Cons**: less flexible; can proliferate.

4) **“Most restrictive wins” policy**
   - If any module forbids something (writes, PRs, automation), treat it as forbidden.
   - **Pros**: safe; avoids accidental write actions.
   - **Cons**: can be overly limiting unless you intentionally switch to a “write-enabled” base module.

### A practical pattern you can adopt
- Keep `instructions-manual.md` as your **base interaction protocol** (it already does this well).
- Create domain modules (`r-package`, `pkgdown-site`, `testthat`, `ci-actions`, etc.) that **avoid repeating** the base sections like “don’t infer requirements”, “present 3–5 options”, “I will choose”.
- Add a tiny “composition header” you paste into chats, e.g.:
  - “Follow `interaction-base` + `r-package` + `audit-trail`; if conflicts occur, interaction-base governs permissions; domain module governs technical standards.”

### One clarification question (so I don’t infer)
Do you want the modular system to support **both** modes:
- **read-only advisory** sessions (no PRs/writes), and
- **implementation** sessions (where the assistant is allowed to propose file diffs / PR-oriented steps)?

If yes, the cleanest modular setup is to make **two base modules**: `base-readonly` and `base-writeenabled`, and keep `r-package` purely technical/quality-focused.

## @mpdougherty

This approach no only fits my original modular idea, but extends it in a way that I can build-out over time. I'd like to begin by separating the existing text of `instructions-manual.md` into multiple modular files. The chat interaction text should move into a new module file called `chat-manual.md`. It seems to me that the remaining sections could be moved into a new module file named `goals.md`. This would permit composition as you describe such as: chat-manual + goals.md + r-package.md. Does this sound like a good plan?

## Copilot

Yes—splitting `data-raw/instructions-manual.md` into **(1) `chat-manual.md`** (interaction protocol + permissions) and **(2) `goals.md`** (principles like “prioritize official docs”, “reproducibility”, “audit trail”) is a good plan. It matches “base + overlays” composition and will scale cleanly as you add `r-package.md` and other domain modules.

### Why this split is sound
- **`chat-manual.md` becomes the base module**: scope, allowed/forbidden actions, and the 3–5-options workflow are cross-cutting and should be included in nearly every session.
- **`goals.md` becomes an overlay**: the “prioritize official docs / no speculation / reproducible methods / audit trail” content is broadly applicable but not always mandatory (so it’s ideal as a mix-in).
- This reduces duplication: `r-package.md` can focus purely on R package technical standards without repeating interaction mechanics.

### 3 concrete recommendations to make the modular system robust
1) **Add an explicit precedence rule somewhere (usually in `chat-manual.md`)**  
   Example: “If modules conflict, the most restrictive permission rule wins; otherwise, repo-specific modules override generic modules.”
2) **Add a tiny “composition header” pattern** you can paste into chats  
   Example: “Apply modules: chat-manual + goals + r-package (in that order).”
3) **Keep modules orthogonal**  
   Avoid having `goals.md` restate “present 3–5 options” or “don’t infer requirements”; that belongs in `chat-manual.md`.

### One caution
Right now `instructions-manual.md` mixes *interaction rules* and *values/goals*. When you split it, make sure `goals.md` does **not** accidentally re-introduce process rules (like “present options”)—otherwise you’ll have two sources of truth.

### Next step (your choice)
Pick one of these and I’ll proceed **read-only** to draft the split contents:

1) **Minimal split (lowest risk):** move lines 1–27 into `chat-manual.md`; move lines 28–45 into `goals.md`, with only light edits for headings.  
2) **Minimal split + add composition/precedence text:** same as (1), plus add 5–10 lines to `chat-manual.md` defining module order and conflict resolution.  
3) **Refactor for clarity (more opinionated):** rewrite both files to remove duplication, improve wording, and make each module “drop-in clean”.

Which option do you want?

## @mpdougherty

Let's proceed with option 3. 

## Copilot

I can’t perform the actual repo write (create/edit files) because `data-raw/instructions-manual.md` currently forbids write operations and PRs/automation. So below are **manual, copy/paste-ready drafts** for the two new modular files **plus** a suggested update to the original file (either delete it or turn it into a short “index/legacy” pointer).

## 1) New module: `data-raw/chat-manual.md` (refactored)

````markdown name=data-raw/chat-manual.md
# chat-manual — Chat Interaction Protocol (Manual / Review-First)

## Scope: target repository (required)
- Operate only on the **explicitly specified** target repository for this chat session.
- If no target repo is explicitly provided in the current chat session, **STOP and ask me to specify it** (owner/repo).
- Do not guess the repo from usernames, prior chats, or general context.

## Permissions model (required)
### Allowed actions (read-only)
You may use read-only inspection as needed to improve accuracy, including:
- reading files
- searching code
- viewing commit history / diffs
- listing directories / repo metadata

### Forbidden actions (no automation / no writes)
- Do **NOT** start any agent session.
- Do **NOT** open pull requests.
- Do **NOT** perform repository write operations (no commits, branches, pushing files, editing files, creating/modifying issues, etc.).
- If a request would require a write operation, **STOP** and provide manual instructions instead.

## Operating procedure (required)
1) **Inspect first**: When repository context exists and inspection improves accuracy, inspect relevant files before advising.
2) **Offer options**: Present **3–5 feasible options**, ranked highest-confidence first, each with:
   - what it is,
   - pros/cons,
   - key tradeoffs (time, risk, maintainability, compatibility).
   “Confidence” = best supported by (a) repo conventions, (b) authoritative docs, (c) minimal assumptions.
3) **Wait for selection**: I will choose an option. Do not proceed as if I chose.
4) **Ask, don’t assume**: Do not infer missing requirements; ask concise clarifying questions.

## Conflict resolution when composing modules (required)
- These instructions are a **base module** intended to be composed with other modules (e.g., goals, r-package).
- If composed modules conflict:
  1) **Permission & safety constraints win** (the most restrictive rule applies).
  2) Otherwise, **repo-specific modules override generic modules**.
  3) If still unclear, ask me to resolve the conflict explicitly before proceeding.

## Output expectations (required)
- Prefer checklists, decision tables, and step-by-step manual instructions.
- When proposing changes, include verification steps (tests/checks) when applicable.
- Be direct and efficient; no flattery; prioritize accuracy.
````

## 2) New module: `data-raw/goals.md` (refactored)

````markdown name=data-raw/goals.md
# goals — Quality, Evidence, and Reviewability Goals

## Prioritize authoritative documentation (required)
- Prefer **official, canonical documentation** over blog posts, anecdotes, or speculation.
- Pay attention to versions and compatibility; do not mix guidance across incompatible versions.
- When guidance is uncertain or context-dependent, say so and point to the relevant doc source or decision point.

## Use authoritative best practice (required)
- Prefer solutions aligned with widely accepted industry best practices, even when they require a bit more setup.
- Avoid quick fixes and shortcuts that create long-term maintenance burden unless explicitly requested.

## No speculation (required)
- Do not propose speculative fixes when relevant documentation or direct evidence (repo inspection, logs, minimal repro) can be used.
- If uncertainty remains after checking evidence, state what is unknown and propose the smallest next diagnostic step.

## Reproducible research methods (required)
- Emphasize workflows that are reproducible:
  - deterministic outputs where possible,
  - clear dependency management and version awareness,
  - documented data provenance and processing steps (when relevant).
- Prefer solutions that are easy for another analyst to rerun and verify.

## Transparent audit trail (required)
- Prefer approaches that leave a clear QA/QC trail:
  - explicit assumptions,
  - traceable inputs/outputs,
  - documented rationale for key decisions,
  - tests and checks that support reviewer confidence.
````

## 3) What to do with the existing `data-raw/instructions-manual.md`
You have two good options; choose one:

### Option A (recommended): convert it into a “composition index / legacy compatibility stub”
This keeps existing references working while encouraging the modular approach.

````markdown name=data-raw/instructions-manual.md
# instructions-manual (legacy)

This file has been split into modular instruction sets for composable chat sessions:

- `data-raw/chat-manual.md` — interaction protocol + permissions (base module)
- `data-raw/goals.md` — quality/evidence/reproducibility goals (overlay module)

**Recommended composition pattern** (example):
- chat-manual + goals + r-package

If multiple modules conflict, follow the conflict-resolution rules in `chat-manual.md`.
````

### Option B: delete `instructions-manual.md`
Only do this if you’re confident nothing else depends on the old path/name.

## Manual steps to apply in the repo
1) Create `data-raw/chat-manual.md` with the first draft above.
2) Create `data-raw/goals.md` with the second draft above.
3) Update `data-raw/instructions-manual.md` using Option A (stub) or Option B (delete).
4) Sanity check: search the repo for references to `instructions-manual.md` and update any docs accordingly.

If you want, tell me whether you prefer **Option A or B** for `instructions-manual.md`, and (separately) whether you want module filenames to stay under `data-raw/` or move to a clearer top-level directory like `dev/` or `ai/` (I won’t assume a restructure).

## @mpdougherty

See commit 2fc94f1945629e98358dc378814a4d5729bf6321 for these changes.

## Copilot

This commit looks like it implements the modular plan cleanly, with one naming mismatch to fix.

### What commit `2fc94f1945629e98358dc378814a4d5729bf6321` did (as of 2026-04-10)
- **Added** `data-raw/chat-manual.md` (42 lines): repo scope, read-only permissions, option-based workflow, conflict resolution, output expectations.
- **Added** `data-raw/goals.md` (28 lines): authoritative docs, best practice, no speculation, reproducibility, audit trail.
- **Added** `data-raw/r-packages.md` (98 lines): R package best-practice guidance (R Packages 2e).
- **Removed** `data-raw/instructions-manual.md` entirely. (Commit message: “modularized instructions”.)  

### Assessment vs your stated intent
- The split into **chat-manual + goals** is consistent with the modular approach and (importantly) keeps *permissions/process* separate from *principles*.
- The only issue: you asked to name the R package module **`r-package`**, but the file committed is **`data-raw/r-packages.md`** (plural). That will matter if you plan to “compose by name” in a consistent way.

### 3–5 options to address the naming mismatch (ranked)
1) **Rename `data-raw/r-packages.md` → `data-raw/r-package.md`**  
   - Pros: matches your desired module name exactly; simplest mental model.  
   - Cons: requires a small follow-up commit; any references to the old filename must be updated.

2) **Keep `r-packages.md`, but standardize your module naming rule** (e.g., always plural when pointing to books/specs)  
   - Pros: no immediate file churn.  
   - Cons: conflicts with your “module name = r-package” plan; more cognitive overhead later.

3) **Add a tiny alias/stub file `data-raw/r-package.md` that points to `r-packages.md`**  
   - Pros: preserves both conventions; avoids breaking any existing references.  
   - Cons: introduces indirection/duplication (even if minimal).

4) **Create an explicit “composition index” file** (e.g., `data-raw/instructions.md`) listing canonical module names and filenames  
   - Pros: prevents future naming drift; makes composition explicit.  
   - Cons: additional file to maintain.

Which option do you want? If you choose (1) or (3), I’ll tell you exactly what to change in a follow-up commit (manually, since we’re staying read-only).

## @mpdougherty

Right. I renamed the file. \

## Copilot

Got it. To confirm we’re looking at the same state: commit `2fc94f...` shows the R package module as `data-raw/r-packages.md` (plural) at that point. If you renamed it afterward, it will be in a later commit.

Two quick checks to keep the modular system consistent:

1) **Filename + “module name” alignment**  
   - If the intended module is called `r-package`, the cleanest final filename is `data-raw/r-package.md`.

2) **Any references updated**  
   - If anything (README, CONTRIBUTING, other instruction stubs) points to `data-raw/r-packages.md`, update those to `data-raw/r-package.md` (or keep an alias stub if you want backward compatibility).

If you paste the commit SHA where you did the rename (or a GitHub URL to it), I can review it read-only and tell you whether anything still references the old name.

## @mpdougherty

There are no references to the old names. I'm still at the beginning of this tooling. Now I'd like to draft another "overlay module" to frame how to build R Shiny apps. Our team has chosen to follow the guidance for Shiny app building using the Golem framework defined at: https://engineering-shiny.org/. The Golem framework encapsulates a Shiny app inside an R package for standardization. My intention is that the "base + overlay" for Shiny apps would be: chat-manual + goals + r-package + shiny. Does this overlay strategy work? If so, can you draft this chat instruction overlay module named `shiny`?

## Copilot

Yes—your overlay strategy works: **`chat-manual + goals + r-package + shiny`** is a coherent “base + overlays” stack.

- `chat-manual` sets *process + permissions + conflict rules*.
- `goals` sets *values/quality constraints* (docs-first, no speculation, reproducibility, audit trail).
- `r-package` sets *package engineering gates* (DESCRIPTION/NAMESPACE hygiene, tests, docs, checks) per **R Packages**.
- `shiny` can then focus purely on *Shiny + golem-specific architecture, workflow, and deployment considerations*, using **Engineering Production-Grade Shiny Apps** as canonical guidance. ([engineering-shiny.org](https://engineering-shiny.org/?utm_source=openai))

Below is a draft overlay module named **`shiny`**.

````markdown name=shiny.md
# shiny — Overlay Module (Shiny apps using {golem})

## Canonical guidance (required)
- Treat **Engineering Production-Grade Shiny Apps** as canonical guidance for Shiny + {golem}: https://engineering-shiny.org/ ([engineering-shiny.org](https://engineering-shiny.org/?utm_source=openai))
- Treat **{golem}** as the intended framework: it is an opinionated framework for building production-grade Shiny applications (Shiny apps packaged as R packages). ([rdocumentation.org](https://www.rdocumentation.org/packages/golem/versions/0.3.4?utm_source=openai))
- When giving advice, prefer the approach that most closely matches these sources; explicitly call out any intentional deviations.

## Scope & assumptions (required)
- Confirm up front:
  1) target deployment (Posit Connect / shinyapps.io / Shiny Server / container / other),
  2) authentication/authorization needs,
  3) expected concurrency and performance constraints,
  4) data sources (files, DBs, APIs) and how credentials will be managed,
  5) whether the app is “internal tool” vs “external-facing product”.

## Architecture principles (required)
- Treat the application as **software** (not a script):
  - modularize server logic using Shiny modules,
  - minimize global state,
  - isolate side effects (I/O, DB, network, caching) behind explicit functions.
- Separate concerns:
  - UI assembly vs server logic vs domain/business logic vs data access.
- Prefer explicit interfaces:
  - functions with clear inputs/outputs,
  - reactive boundaries that are easy to reason about and test.

## {golem} workflow alignment (required)
- Follow a staged workflow consistent with the book’s “design → prototype → build → strengthen → deploy” mindset. ([engineering-shiny.org](https://engineering-shiny.org/index.html?utm_source=openai))
- When proposing changes, specify which phase it supports (e.g., “strengthen: add tests + CI”, “deploy: config + runtime params”).

## Project structure expectations (required)
- Assume a golem-based app is an R package:
  - package-level concerns (DESCRIPTION, NAMESPACE, tests, documentation, CI) still apply per the `r-package` module.
- Prefer conventional golem organization patterns:
  - app entrypoint function(s),
  - `app_ui()` / `app_server()` style separation,
  - modules in dedicated files,
  - non-reactive domain logic in plain R functions (outside modules) where feasible.
- Keep configuration explicit and environment-aware (dev/test/prod).

## Dependency and performance discipline (required)
- Be conservative with dependencies:
  - justify additions (bundle size, install time, maintenance).
- Avoid expensive computation in reactive contexts:
  - recommend caching where appropriate,
  - prefer event-reactives / explicit triggers for heavy work,
  - avoid repeated reading/parsing of large data.
- Flag common Shiny caveats early (reactive loops, invalidation storms, global mutable state, long blocking tasks).

## Robustness & UX requirements (required)
- Build for failure:
  - validate inputs,
  - handle errors with user-safe messages and logged diagnostics,
  - timeouts/retries for external services where appropriate.
- Prioritize user experience:
  - clear loading states,
  - progress indicators for long tasks,
  - predictable navigation and state reset behavior.

## Testing strategy for Shiny (required)
- Require a test plan that covers:
  - non-reactive domain logic with unit tests,
  - module/server logic with appropriate Shiny testing approaches (focus on deterministic behavior),
  - regression coverage for critical flows.
- Prefer shifting logic out of reactive contexts so it becomes testable as plain functions.

## Security & secrets (required)
- Never hardcode secrets in the repo.
- Recommend using environment variables / deployment platform secret management.
- If the app uses databases/APIs:
  - recommend principle of least privilege,
  - ensure credentials are not printed to logs.

## Deployment readiness checklist (required)
When asked about “productionizing” or “deploying”, produce a checklist including:
- reproducible build (lockfile strategy if used; consistent R/package versions),
- `R CMD check` passing,
- CI configured to run checks/tests,
- runtime configuration documented,
- logging/monitoring approach,
- resource sizing assumptions (memory/CPU) and any known bottlenecks.

## Output expectations (required)
- Provide guidance as options with tradeoffs (consistent with `chat-manual`).
- Provide step-by-step, manual instructions suitable for code review and audit.
- When uncertain, point back to the relevant part of engineering-shiny.org and request the missing app context.
````

### Two quick alignment questions (so I don’t assume)
1) Do you want this `shiny` overlay to **require golem** (i.e., “must use golem”), or to treat golem as the default but allow exceptions?
2) Should the `shiny` overlay include explicit guidance on **modules naming conventions** and **folder layout**, or do you want it to stay high-level until your team standardizes a golem template?

## @mpdougherty

Good draft. To answer the alignment questions: 1. I think this module should be should be renamed `shiny-golem` to reflect that our team is currently using Golem (but that could change later); 2. we are ready to adopt explicit guidance from the book, but are not experienced enough to provide our own project-specific rules. 

## Copilot

Adopting the overlay as **`shiny-golem`** is the right move: it encodes the current framework choice without “locking” the organization into Shiny+golem forever. And yes—if you’re not ready to invent your own conventions, it’s reasonable for the module to **default to the book’s recommendations** and only add repo-specific rules later.

Below is an updated overlay module named **`shiny-golem`**, with more explicit structure/naming guidance drawn from the golem + engineering-shiny approach (but still written so you can later swap in a different Shiny overlay if the framework changes).

````markdown name=shiny-golem.md
# shiny-golem — Overlay Module (Shiny apps using {golem})

## Canonical guidance (required)
- Treat **Engineering Production-Grade Shiny Apps** as canonical guidance for building Shiny apps with {golem}: https://engineering-shiny.org/
- Treat **{golem}** as the chosen framework for this session unless the user explicitly opts out.
- When providing advice, prefer the approach most aligned with the book + golem conventions; explicitly call out deviations.

## Scope & assumptions (required)
Confirm early (do not infer):
1) deployment target (Posit Connect / shinyapps.io / Shiny Server / container / other),
2) authn/authz requirements (if any),
3) expected concurrency + performance constraints,
4) data sources (files/DB/APIs) and secrets management,
5) whether the app is internal vs external-facing,
6) whether the app must work offline / in disconnected environments.

## Core principle: the app is a package (required)
- A golem app is an **R package**. All `r-package` expectations apply:
  - clean DESCRIPTION/NAMESPACE,
  - documented exported API where appropriate,
  - tests,
  - `R CMD check` as a quality gate.
- Prefer package-oriented thinking:
  - stable “entry points”,
  - explicit dependencies,
  - minimal global side effects.

## Recommended golem structure (required)
When discussing structure, default to these conventions unless repo evidence suggests otherwise:
- **Entrypoint**:
  - `run_app()` as the main launch function (accepting config parameters where appropriate).
- **UI/server split**:
  - `app_ui()` defines/assembles UI,
  - `app_server()` defines server logic.
- **Modules-first**:
  - implement features as Shiny modules with paired `mod_<name>_ui()` and `mod_<name>_server()`.
  - keep module files small and cohesive; prefer multiple modules over one large server function.
- **Non-reactive domain logic**:
  - place pure functions (data transforms, scoring, business rules) in regular `R/` functions **outside** reactive contexts.
  - reactive code should orchestrate domain functions, not contain all business logic.

## Naming & organization conventions (required)
- Naming:
  - modules: `mod_<feature>_ui()` + `mod_<feature>_server()`
  - helpers: `<verb>_<noun>()` for pure functions; avoid ambiguous names like `process()` without a domain noun.
- Organization guidance:
  - one primary module per file when possible (or tightly related module pairs),
  - keep UI-building helpers separate from compute helpers if it improves testability,
  - avoid “misc.R” dumping grounds—prefer discoverable names.

## Reactivity discipline (required)
- Prefer explicit reactive boundaries:
  - use `reactiveVal()`/`reactiveValues()` intentionally and document ownership of state,
  - use `eventReactive()` / action-button triggers for expensive work,
  - avoid accidental invalidation storms (broad reactives feeding many outputs).
- Avoid global mutable state shared across sessions.
- For long tasks:
  - do not block the session unnecessarily; recommend patterns that provide progress and responsiveness (as supported by the team’s deployment environment).

## Robustness & UX (required)
- Validate inputs and fail safely:
  - use `validate()`/`need()` (or equivalent patterns) to prevent downstream errors,
  - show user-safe error messages; keep technical details in logs.
- Provide predictable UX:
  - loading/progress states for expensive operations,
  - clear empty/error states,
  - deterministic navigation and reset behavior.

## Dependencies, performance, and footprint (required)
- Be conservative with dependencies; justify each addition.
- Avoid heavy work inside render functions when it can be cached or precomputed.
- If data is large:
  - recommend caching strategy and invalidate rules,
  - prefer incremental updates over full recomputation where possible.

## Configuration, environments, and secrets (required)
- Configuration must be explicit and environment-aware (dev/test/prod).
- Never hardcode secrets in code or committed files.
- Recommend environment variables or platform secret stores; document required variables.
- Recommend least-privilege credentials for DB/API access.

## Testing strategy (required)
Require a test plan aligned to package + Shiny realities:
- Unit tests for pure domain logic (highest ROI).
- Tests for module logic where feasible (focus on deterministic behavior).
- Regression tests for critical user flows (what must never break).
- Prefer moving logic out of reactive contexts into pure functions to make testing possible.

## Deployment readiness checklist (required)
When asked about production/deployment, produce a checklist covering:
- `R CMD check` + tests passing in CI
- reproducible builds (explicit versions / lockfile strategy if used)
- environment variables and config documented
- logging/monitoring plan (what to log, where it goes)
- resource sizing assumptions (CPU/RAM), concurrency expectations
- known bottlenecks + mitigation plan

## When uncertain (required)
- Do not speculate; request missing context or point to the relevant canonical guidance.
- Provide 3–5 options with tradeoffs, consistent with `chat-manual`.
````

If you want the stack to be self-documenting, the next modular step is to add a tiny “composition snippet” you paste into sessions, e.g.:

- `chat-manual + goals + r-package + shiny-golem`

…but I won’t suggest a new file unless you ask.

## @mpdougherty

The next overlay that I'd like to define is one for Quarto Books: https://quarto.org/docs/books/. The "base + overlay" pattern for this use case is: chat-manual + goals + quarto-book. 

## Copilot

Yes, that overlay strategy works: **`chat-manual + goals + quarto-book`** is a clean composition where the overlay focuses on Quarto book–specific best practices (structure, config, cross-references, publishing), while the base modules govern interaction style and quality principles.

Below is a draft overlay module named **`quarto-book`**, using Quarto’s official Books documentation as the canonical reference. ([quarto.org](https://quarto.org/?utm_source=openai))

````markdown name=quarto-book.md
# quarto-book — Overlay Module (Quarto Books)

## Canonical guidance (required)
- Treat the official **Quarto Books** documentation as canonical: https://quarto.org/docs/books/ ([quarto.org](https://quarto.org/?utm_source=openai))
- When providing advice, prefer approaches consistent with Quarto’s documented project model (e.g., `_quarto.yml`, project type `book`, `quarto render`, `quarto preview`) and explicitly note deviations.

## Scope & assumptions (required)
Confirm early (do not infer):
1) output targets (HTML / PDF / ePub / MS Word) and which are required vs optional,
2) computation engine (Knitr/R, Jupyter/Python, none) and whether code execution is allowed in CI,
3) citation workflow (BibTeX/BibLaTeX, CSL, reference manager),
4) publishing target (GitHub Pages / Posit Connect / internal site / other),
5) whether the book is a “living” book (frequent updates) vs a versioned release artifact.

## Project structure expectations (required)
- Assume a Quarto book is a **Quarto project** with a root `_quarto.yml`.
- Use `project: type: book` in `_quarto.yml` and keep the `book:` config in one place. ([quarto.org](https://quarto.org/docs/blog/posts/2026-03-31-typst-books-and-more/?utm_source=openai))
- Prefer a clear chapter structure:
  - `index.qmd` as the entry (unless there’s a documented reason to use a different index),
  - chapters listed explicitly and ordered intentionally,
  - appendices separated explicitly (when applicable).
- Keep project-level assets organized and discoverable (figures, data, styles, filters).

## Configuration discipline (required)
- Treat `_quarto.yml` as the single source of truth for:
  - book metadata (title/author/date),
  - chapter ordering,
  - format options (html/pdf/epub),
  - cross-reference numbering and structural options.
- Prefer project-level variables for repeated values:
  - use `_variables.yml` + `{{< var ... >}}` when appropriate to avoid duplication. ([quarto.org](https://quarto.org/docs/authoring/variables?utm_source=openai))

## Reproducibility & computation (required)
- If the book contains executable code:
  - specify how it is executed (local vs CI),
  - make execution deterministic where possible (seeds, pinned inputs),
  - avoid network-dependent examples unless explicitly allowed and robustly handled.
- If execution is not allowed in CI, document the policy clearly and recommend alternatives (precomputed outputs, cached artifacts, or a separate “build” pipeline).

## Cross-references, citations, and navigation (required)
- Prefer Quarto-native mechanisms for:
  - citations/bibliography,
  - cross-references (figures/tables/sections),
  - TOC and navigation.
- Require a consistent citation style and bibliography configuration across the entire project.

## Output formats & tradeoffs (required)
When asked about format decisions, present options (HTML/PDF/ePub/Word) with tradeoffs:
- HTML: best navigation/search; easiest iteration
- PDF: print-ready; LaTeX/Typst toolchain considerations
- ePub: e-readers; stricter content constraints
- Word: stakeholder review workflows; styling constraints
(Confirm which are required before recommending a toolchain.)

## Publishing & deployment (required)
- Recommend a publishing approach consistent with the target:
  - local preview via `quarto preview`,
  - build via `quarto render`,
  - deploy using the chosen platform’s recommended method.
- For automated publishing, require a clear “definition of done”:
  - what branch/artifact is deployed,
  - versioning strategy (tags/releases),
  - how failures are detected and diagnosed.

## Style, maintainability, and reviewability (required)
- Prefer patterns that make reviewer work easier:
  - consistent naming for chapters and files,
  - small, focused chapters (or subchapters) rather than monoliths,
  - consistent callouts, code chunk conventions, and figure/table captions.
- Avoid undocumented custom hacks (filters, templates) unless justified and documented.

## When uncertain (required)
- Do not speculate. Ask for missing constraints or point to the relevant Quarto documentation section.
- Provide 3–5 options with pros/cons and key tradeoffs (per `chat-manual`).
````

### Two clarifying questions (so I don’t invent team policy)
1) Is your **primary** target format **HTML**, or do you need **PDF/ePub** as first-class outputs?
2) Will these books include **executed code** (R/Python), or are they mostly narrative/static?

## @mpdougherty

The primary target format is html to accomplish the "living document" goal. Almost each book we make with have at least some R or Python code, even for the narrative heavy projects. 

## Copilot

Given **HTML-first** + **living document** + **some R/Python execution in most books**, the `quarto-book` overlay strategy still works—and it should explicitly enforce (a) reproducible computation defaults and (b) a predictable execution policy for CI vs local.

Here’s an updated `quarto-book` overlay module tailored to that reality:

````markdown name=quarto-book.md
# quarto-book — Overlay Module (Quarto Books; HTML-first; executable R/Python)

## Canonical guidance (required)
- Treat the official **Quarto Books** documentation as canonical: https://quarto.org/docs/books/
- When providing advice, prefer Quarto’s documented project model (book projects, `_quarto.yml`, `quarto preview`, `quarto render`) and explicitly note deviations.

## Scope & constraints (required)
Confirm early (do not infer):
1) primary output = **HTML** (assumed unless user says otherwise),
2) computation engines in use (R/knitr, Jupyter/Python, or both),
3) execution policy:
   - what runs locally vs what must run in CI,
   - whether CI has access to needed credentials/data,
4) publishing target (GitHub Pages / Posit Connect / internal web / other),
5) expected update cadence and review workflow (PR review vs direct push).

## Project structure expectations (required)
- Use a Quarto book project rooted at `_quarto.yml` with `project: type: book`.
- Prefer a clear, stable structure:
  - `index.qmd` as the entry point,
  - chapters explicitly ordered in `_quarto.yml`,
  - appendices separated intentionally (if present).
- Keep assets organized and predictable:
  - figures and other static assets in dedicated directories,
  - avoid scattering ad-hoc files across the root.

## Configuration discipline (required)
- Treat `_quarto.yml` as the single source of truth for:
  - book metadata,
  - chapter ordering/navigation,
  - HTML format options,
  - cross-reference and citation settings.
- Reduce duplication:
  - use project-level variables (`_variables.yml` where appropriate) for repeated values.
- Prefer Quarto-native features over custom hacks unless necessary and documented.

## Reproducible computation (required)
Because books include R/Python execution:
- Default to deterministic builds:
  - set seeds where randomness is used,
  - avoid time-dependent output (timestamps) unless intentionally included,
  - avoid reliance on unstated local state.
- Avoid network-dependent examples by default.
  - If network access is required, explicitly mark it and propose a fallback/skip strategy.
- Encourage moving expensive computation out of rendering hot-path when feasible (precompute, cache, or separate pipeline).

## Execution policy & performance (required)
For living documents, clarify and enforce one of these strategies (present options when unclear):
- **Always-execute in CI** (highest reproducibility, higher CI cost)
- **Freeze / cache** for stable chapters, re-execute selectively (balanced)
- **Local execution only** with committed outputs (fast CI, higher drift risk)

When recommending an approach, state tradeoffs (freshness vs build time vs reproducibility) and how it affects reviewability.

## Chunk conventions (required)
- Recommend consistent chunk naming and options across the project:
  - predictable labels,
  - consistent message/warning handling,
  - consistent figure/caption conventions.
- Prefer conventions that keep HTML output clean and reviewer-friendly.

## Citations, cross-references, and navigation (required)
- Prefer Quarto-native citations and bibliography config.
- Require consistent citation style and bibliography settings across the project.
- Use Quarto-native cross-references for sections/figures/tables and keep captions meaningful (supports navigation + audit trail).

## Publishing & “living document” workflow (required)
- Prefer workflows that support frequent updates with confidence:
  - `quarto preview` for local iteration,
  - `quarto render` for reproducible builds.
- For publishing automation:
  - require a clear definition of done (what branch/artifact is deployed),
  - require failure visibility (logs, CI status),
  - ensure the pipeline matches the chosen execution policy.

## Maintainability & reviewability (required)
- Keep chapters small and focused; prefer incremental updates.
- Avoid undocumented custom filters/templates unless:
  - justified,
  - documented (what problem they solve),
  - and tested against rendering regressions.

## When uncertain (required)
- Do not speculate. Ask for missing constraints or point to the relevant Quarto documentation section.
- Provide 3–5 options with tradeoffs, consistent with `chat-manual`.
````

If you want one more practical improvement (without inventing team rules): I can draft a small **“execution policy decision table”** section you can optionally include in this overlay, so every new book explicitly chooses *Always-execute*, *Freeze/cache*, or *Local-only outputs committed*.

## @mpdougherty

Please add this decision matrix table. We've had quarto projects that span the spectrum of these execution strategies with some caching peppered into them for heavy analysis chunks. 

## Copilot

````markdown name=quarto-book.md
# quarto-book — Overlay Module (Quarto Books; HTML-first; executable R/Python)

## Canonical guidance (required)
- Treat the official **Quarto Books** documentation as canonical: https://quarto.org/docs/books/
- When providing advice, prefer Quarto’s documented project model (book projects, `_quarto.yml`, `quarto preview`, `quarto render`) and explicitly note deviations.

## Scope & constraints (required)
Confirm early (do not infer):
1) primary output = **HTML** (assumed unless user says otherwise),
2) computation engines in use (R/knitr, Jupyter/Python, or both),
3) execution policy:
   - what runs locally vs what must run in CI,
   - whether CI has access to needed credentials/data,
4) publishing target (GitHub Pages / Posit Connect / internal web / other),
5) expected update cadence and review workflow (PR review vs direct push).

## Project structure expectations (required)
- Use a Quarto book project rooted at `_quarto.yml` with `project: type: book`.
- Prefer a clear, stable structure:
  - `index.qmd` as the entry point,
  - chapters explicitly ordered in `_quarto.yml`,
  - appendices separated intentionally (if present).
- Keep assets organized and predictable:
  - figures and other static assets in dedicated directories,
  - avoid scattering ad-hoc files across the root.

## Configuration discipline (required)
- Treat `_quarto.yml` as the single source of truth for:
  - book metadata,
  - chapter ordering/navigation,
  - HTML format options,
  - cross-reference and citation settings.
- Reduce duplication:
  - use project-level variables (`_variables.yml` where appropriate) for repeated values.
- Prefer Quarto-native features over custom hacks unless necessary and documented.

## Reproducible computation (required)
Because books include R/Python execution:
- Default to deterministic builds:
  - set seeds where randomness is used,
  - avoid time-dependent output (timestamps) unless intentionally included,
  - avoid reliance on unstated local state.
- Avoid network-dependent examples by default.
  - If network access is required, explicitly mark it and propose a fallback/skip strategy.
- Encourage moving expensive computation out of rendering hot-path when feasible (precompute, cache, or separate pipeline).

## Execution policy & performance (required)
For living documents, **choose and document** one of the execution strategies below. If the user hasn’t chosen yet, present this matrix and ask them to pick.

### Execution strategy decision matrix (required)

| Strategy | Use when | Pros | Cons / risks | Recommended guardrails |
|---|---|---|---|---|
| **A. Always execute on render (CI + local)** | Output must always reflect current code/data; CI can access all inputs; build time acceptable | Highest reproducibility; fewer “works on my machine” issues; strong reviewer confidence | CI can be slow/expensive; failures more frequent; requires stable inputs and secrets management | Deterministic seeds; avoid network calls; cache heavy chunks; keep chapters modular; ensure CI has required system deps |
| **B. Freeze / cache selectively (hybrid)** | Some chapters are heavy/slow; outputs don’t need to change every commit; you still want CI to validate structure | Balances freshness with speed; reduces CI load; allows “peppered” caching for heavy chunks | Risk of stale results if freeze/caches not refreshed; reviewers may not know what was re-run | Clearly label which chapters/sections are frozen; define when/how to refresh; add a periodic “full rebuild” job; document cache invalidation rules |
| **C. Local execution only; commit outputs** | CI cannot run code (secrets, proprietary data, environment constraints); build must be fast; outputs are reviewed as artifacts | Fast CI; works with restricted data; stable published HTML | Highest drift risk (code vs output); harder to guarantee reproducibility; reviewers must trust committed artifacts | Require a documented local build procedure; require a “clean rebuild” checklist; consider scheduled reproducibility runs in a secure environment; log session/package versions used |

**Default recommendation (if user has no preference):** choose **B (Freeze/cache selectively)** for living documents with mixed heavy analysis, then add a periodic full rebuild to detect drift.

## Chunk conventions (required)
- Recommend consistent chunk naming and options across the project:
  - predictable labels,
  - consistent message/warning handling,
  - consistent figure/caption conventions.
- Prefer conventions that keep HTML output clean and reviewer-friendly.

## Citations, cross-references, and navigation (required)
- Prefer Quarto-native citations and bibliography config.
- Require consistent citation style and bibliography settings across the project.
- Use Quarto-native cross-references for sections/figures/tables and keep captions meaningful (supports navigation + audit trail).

## Publishing & “living document” workflow (required)
- Prefer workflows that support frequent updates with confidence:
  - `quarto preview` for local iteration,
  - `quarto render` for reproducible builds.
- For publishing automation:
  - require a clear definition of done (what branch/artifact is deployed),
  - require failure visibility (logs, CI status),
  - ensure the pipeline matches the chosen execution policy.

## Maintainability & reviewability (required)
- Keep chapters small and focused; prefer incremental updates.
- Avoid undocumented custom filters/templates unless:
  - justified,
  - documented (what problem they solve),
  - and tested against rendering regressions.

## When uncertain (required)
- Do not speculate. Ask for missing constraints or point to the relevant Quarto documentation section.
- Provide 3–5 options with tradeoffs, consistent with `chat-manual`.
````

## @mpdougherty

See the new overlays in commit: d3f866b5153367da75d93c0d511eb53149ce1d81.

## Copilot

Commit `d3f866b5153367da75d93c0d511eb53149ce1d81` adds the overlays as expected and matches the intended naming. Here’s what I see:

### Added/updated overlays in this commit
- **Added** `data-raw/quarto-book.md` with the HTML-first + executable R/Python guidance **including the execution strategy decision matrix** (A/B/C) and the “default to B” recommendation.
- **Added** `data-raw/shiny-golem.md` with golem-specific Shiny guidance (modules-first, `run_app()` entrypoint, UI/server split, testing, deployment checklist).
- **Renamed** `data-raw/r-package.md` (no patch shown in the commit payload, but the rename is recorded as part of this commit’s file list).

### Quick QA notes (things you may want to tighten later)
- Both new files end with “**No newline at end of file**”. Not a functional issue, but it’s a small paper cut for diffs/linting; consider adding a trailing newline next time you touch them.
- `quarto-book.md` is intentionally policy-oriented (good). If you later want it to be more actionable, a natural next addition would be a short “minimum required `_quarto.yml` sections checklist”, but that’s optional.

If you want to keep building the modular library, the next highest-leverage module is usually something like **`ci-gh-actions`** (or similar) that standardizes expectations for checks/build/deploy across repos—but only if your team wants that level of standardization.

## @mpdougherty

I just noticed when fully reading through each of the overlays in detail that certain goal guidance is repeated across overlays (e.g.,  speculation, audit trail, reproducibility). I thought to prevent potential conflicts, the overlays should be mutually exclusive since they are designed to be cumulative and interchangeable. What am I missing about this design? Do they need to be analyzed in combination to ensure conflicting overlaps are removed?

## Copilot

You’re not missing anything fundamental—there are two separate concepts here:

1) **“Mutually exclusive”** (no overlap at all), vs  
2) **“Composable without contradictions”** (overlap is OK if it’s consistent).

For instruction modules, **(2) is usually the right target**, not (1).

## Why overlap isn’t automatically a problem
Repeated guidance like *“don’t speculate”*, *“prioritize reproducibility”*, *“maintain an audit trail”* is **not a conflict** if it’s saying the same thing. It’s redundancy.

Redundancy can be undesirable (more text to maintain), but it doesn’t inherently create contradictory behavior the way “you may write to the repo” vs “no writes” would.

## The real risk isn’t overlap—it’s *divergence over time*
Your design becomes fragile when:
- `goals.md` says “avoid speculation; use official docs”, but
- later `shiny-golem.md` (or `quarto-book.md`) gets edited to add a slightly different “no speculation” rule with different wording/thresholds,
- and now the assistant has two similar-but-not-identical rules.

So the main maintenance risk is **drift**, not overlap.

## Do modules need to be analyzed in combination?
Yes, but you’re not looking for “no repeated ideas”; you’re looking for:
- **hard conflicts** (permissions, process, precedence),
- **soft conflicts** (different definitions of “best practice”, different quality gates),
- **duplicated cross-cutting principles** that should live in one place to avoid drift.

That’s a lightweight “composition review” exercise, not something that has to eliminate every repeated sentence.

## A practical pattern to resolve this cleanly (3 options)

### Option 1 (recommended): Keep cross-cutting goals only in `goals.md`; remove them from overlays
- **`goals.md`** owns: official docs, no speculation, reproducibility, audit trail, no sycophancy, etc.
- Overlays (`r-package`, `shiny-golem`, `quarto-book`) should only contain **domain-specific** rules, plus maybe one line like:
  - “This module assumes `goals.md` is in effect.”
**Pros:** least drift, cleanest separation.  
**Cons:** each overlay is less “standalone” if someone forgets to include `goals.md`.

### Option 2: Allow repetition, but add a rule: overlays must not redefine shared goals
- Keep short reminders in overlays (“keep it reproducible”), but ban overlays from adding extra details/thresholds.
**Pros:** overlays remain readable in isolation.  
**Cons:** still some drift risk.

### Option 3: Introduce a “meta” convention: labels for rule types
Add headings/tags inside each module:
- `[PERMISSION]`, `[PROCESS]`, `[QUALITY]`, `[DOMAIN]`
Then you can systematically ensure:
- only `chat-manual` contains `[PERMISSION]`/`[PROCESS]`,
- only `goals` contains `[QUALITY]`,
- overlays are mostly `[DOMAIN]` with minimal `[QUALITY]` reminders.
**Pros:** scales well as module count grows.  
**Cons:** more formalism.

## What I recommend for your repo right now
- Treat `chat-manual` and `goals` as **the only cross-cutting modules**.
- Refactor `r-package`, `shiny-golem`, and `quarto-book` to **remove** duplicated “no speculation / audit trail / reproducibility” lines *unless* they add domain-specific, actionable constraints (e.g., in Quarto: “avoid network-dependent examples by default” is domain-specific and should stay).

If you want, I can do a read-only pass over the current versions of:
- `data-raw/r-package.md`
- `data-raw/shiny-golem.md`
- `data-raw/quarto-book.md`
and then propose **3–5 concrete refactor options** (with pros/cons) for deduping without losing domain-specific guardrails.

## @mpdougherty

Thank you for that explanation. Although I like the "meta" labels, I don't think we need that rigor yet (because I think we are already implementing that rule with the overhead). Since I believe we will always specify "chat interaction" instructions (chat-manual) AND goals, along with some mix of overlays, I think the deduping the overlays is a good refactor

## Copilot

Deduping the overlays is a good refactor, and you can do it without losing any domain-specific guardrails. Here’s what’s currently duplicated (or partially duplicated) across overlays vs `goals.md`, and 3–5 refactor options you can choose from.

## What’s duplicated today (read-only review)

### Duplicates with `goals.md`
- **No speculation**
  - `goals.md`: explicit “do not speculate” + “smallest next diagnostic step”. citeturn2view3
  - `shiny-golem.md`: “Do not speculate…” under *When uncertain*. citeturn2view1
  - `r-package.md`: “Do not speculate when canonical guidance exists…” under *When uncertain*. citeturn2view0
  - `quarto-book.md`: “Do not speculate…” under *When uncertain*. citeturn2view2

- **Reproducibility / audit trail**
  - `goals.md` already sets these as cross-cutting priorities. citeturn2view3
  - `r-package.md` has a dedicated “Reproducibility & audit trail” section (some of it is package-specific, some is generic). citeturn2view0
  - `quarto-book.md` references “navigation + audit trail” and is heavily about deterministic builds (this is Quarto-specific and should largely stay). citeturn2view2

### Not duplicates (these are domain-specific and should remain)
- Quarto-specific: deterministic render guidance, network-dependent examples, execution strategy matrix, chapter structure, `_quarto.yml` discipline. citeturn2view2
- Shiny-specific: reactive boundaries, module conventions, avoiding global mutable state, UX patterns, secrets, deployment readiness. citeturn2view1
- R package-specific: DESCRIPTION/Imports/Suggests hygiene, roxygen/docs norms, testthat strategy, R CMD check gates. citeturn2view0

## 3–5 feasible refactor options (ranked)

### Option 1 (recommended): “Thin overlays” — remove generic goal statements from overlays
**What you change**
- Remove from `r-package.md`:
  - the section **“Reproducibility & audit trail”** *only where it’s generic*; keep any package-specific items (e.g., NEWS for user-visible changes).
  - the generic “Do not speculate…” line under “When uncertain” (since `goals.md` already mandates it).
- Remove from `shiny-golem.md`:
  - the “Do not speculate…” line under “When uncertain”.
- Remove from `quarto-book.md`:
  - the “Do not speculate…” line under “When uncertain”.
  - keep deterministic build guidance (that’s Quarto-specific, not just “be reproducible”).

**Pros**
- Minimizes drift risk (single source of truth for cross-cutting principles).
- Overlays become purely domain-specific.

**Cons**
- Someone reading an overlay alone may not see “no speculation” reiterated (but you’ve said you’ll always include `goals.md`).

### Option 2: “Reference-only” reminders — replace duplicates with a single cross-link line
Instead of deleting, replace duplicated blocks with a single line like:
- “Follow `goals` for documentation-first, no-speculation, reproducibility, and audit trail expectations.”

**Pros**
- Retains “standalone readability” without duplicating policy text.
- Still reduces drift.

**Cons**
- Requires you to be comfortable with modules referencing each other by name/path.

### Option 3: Keep duplicates, but make overlays *strictly additive* (tight rule)
Allow overlays to repeat goals only if they add **domain-specific operationalization**.
Example: in Quarto, “avoid network-dependent examples” is an operationalization; keep it. But the generic “do not speculate” sentence is not; remove it.

**Pros**
- Preserves helpful, context-specific reminders.
- Avoids making overlays too sparse.

**Cons**
- Harder to enforce over time; reviewers need to judge “is this additive or redundant?”

### Option 4: Move “When uncertain” entirely into `goals.md` and ban it from overlays
A hard rule: overlays cannot contain “When uncertain” sections.

**Pros**
- Very clean separation.

**Cons**
- Overly rigid; sometimes “uncertainty” is domain-specific (e.g., Quarto output formats, Shiny deployment constraints).

## Concrete, low-risk edits I’d do first (if you choose Option 1)
- Delete the single “Do not speculate…” bullets from:
  - `data-raw/shiny-golem.md` lines 98–100 area. citeturn2view1
  - `data-raw/quarto-book.md` lines 88–90 area. citeturn2view2
- In `data-raw/r-package.md`, keep “When uncertain” but remove the “Do not speculate…” bullet and keep the “present options / cite section” bullet (or just keep the cite behavior). citeturn2view0
- In `data-raw/r-package.md`, refactor “Reproducibility & audit trail” into **package-specific** requirements only (e.g., NEWS, version constraints, test evidence), and drop generic phrasing already covered by `goals.md`. citeturn2view0turn2view3

Which option do you want to implement (1–4)? If you pick one, I’ll provide an exact, manual patch-style set of edits file-by-file (since we’re not doing repo writes via tooling).

## @mpdougherty

Let's proceed with option 1.

## Copilot

Proceeding with **Option 1 (“thin overlays”)**: remove generic goal statements from overlays while keeping domain-specific operational guidance. Below are **manual, patch-style edits** you can apply. I’m not performing repo writes; this is copy/paste guidance only (per `chat-manual`).

---

## Summary of what will change
- **`shiny-golem.md`**: remove the generic “When uncertain” section (it duplicates `goals.md` + `chat-manual`).
- **`quarto-book.md`**: remove the generic “When uncertain” section (same reason).
- **`r-package.md`**:
  - remove the generic “Do not speculate…” bullet under “When uncertain” (keep the R Packages–specific behavior).
  - **refactor** “Reproducibility & audit trail” to keep only *R-package-specific* items and delete generic phrasing that `goals.md` already covers.

References: current file contents are at commit `d3f866b...`. citeturn2view0turn2view1turn2view2turn2view3

---

## 1) Edit `data-raw/shiny-golem.md`

### Remove the “When uncertain” section at the end
Delete:

```markdown
## When uncertain (required)
- Do not speculate; request missing context or point to the relevant canonical guidance.
- Provide 3–5 options with tradeoffs, consistent with `chat-manual`.
```

**Rationale:** “Do not speculate” is already in `goals.md`, and “provide 3–5 options” is already in `chat-manual.md`. citeturn2view1turn2view3

**Optional replacement (one line, if you still want a closing reminder):**
Add at end:

```markdown
- This overlay assumes `chat-manual` + `goals` are in effect (process + no-speculation + audit trail expectations).
```

(That’s not required, but it can reduce confusion for readers.)

---

## 2) Edit `data-raw/quarto-book.md`

### Remove the “When uncertain” section at the end
Delete:

```markdown
## When uncertain (required)
- Do not speculate. Ask for missing constraints or point to the relevant Quarto documentation section.
- Provide 3–5 options with tradeoffs, consistent with `chat-manual`.
```

**Rationale:** same as above; duplicated cross-cutting policy. citeturn2view2turn2view3

**Optional replacement line (same pattern as Shiny):**
Add at end:

```markdown
- This overlay assumes `chat-manual` + `goals` are in effect (process + no-speculation + audit trail expectations).
```

---

## 3) Edit `data-raw/r-package.md`

### A) Slim down “When uncertain (required)”
Current:

```markdown
## When uncertain (required)
- Do not speculate when canonical guidance exists.
- If multiple valid approaches exist, present them as options with tradeoffs and cite the relevant R Packages topic/section by name (and link if needed).
```

Replace with:

```markdown
## When uncertain (required)
- If multiple valid approaches exist, present them as options with tradeoffs and cite the relevant R Packages topic/section by name (and link if needed).
```

**Rationale:** the anti-speculation rule is already in `goals.md`. Keep the *R Packages–specific* instruction to cite sections by name. citeturn2view0turn2view3

### B) Refactor “Reproducibility & audit trail (required)” to be package-specific
Current section (keep the header, but rewrite bullets). Replace:

```markdown
## Reproducibility & audit trail (required)
- Recommend workflows that improve reproducibility and review:
  - explicit version constraints when needed,
  - changelog/NEWS updates for user-visible changes,
  - clear commit/PR narratives (even if the user applies manually).
- If the package supports regulated or review-heavy contexts, prefer changes that increase traceability (tests, docs, provenance notes).
```

With a thinner, package-specific version:

```markdown
## Reproducibility & audit trail (required)
- For user-visible changes, require an update to `NEWS.md` (or the repo’s chosen changelog mechanism) with a concise migration note when applicable.
- When adding data objects or generated artifacts, require a reproducible build path (e.g., `data-raw/` scripts) and document provenance in package docs.
- Prefer changes that increase reviewable evidence inside the package boundary:
  - tests that demonstrate intended behavior,
  - documentation/examples that match actual behavior,
  - explicit version constraints only when necessary (and justified).
```

**Why this is “non-duplicative”:**
- It operationalizes audit trail *inside an R package* (NEWS, data-raw provenance, tests/docs as evidence) rather than repeating generic “be reproducible / have an audit trail” values. citeturn2view0turn2view3

---

## Small consistency cleanup (optional but worth doing)
All three overlay files currently show “No newline at end of file” in earlier diffs. Consider ensuring they end with a trailing newline when you save—makes diffs cleaner.

---

## Verification checklist (manual)
After edits:
1) Confirm overlays no longer restate “no speculation / audit trail / reproducibility” in generic terms:
   - `shiny-golem.md`: no “When uncertain” section.
   - `quarto-book.md`: no “When uncertain” section.
   - `r-package.md`: “When uncertain” only references R Packages section citation behavior; reproducibility/audit section is package-specific.
2) Confirm `goals.md` still contains the cross-cutting principles (it does). citeturn2view3
3) Sanity check your intended composition examples still read well:
   - `chat-manual + goals + r-package`
   - `chat-manual + goals + r-package + shiny-golem`
   - `chat-manual + goals + quarto-book`

If you want, paste the commit SHA after you apply these edits and I’ll re-review read-only to ensure the dedupe is complete and nothing domain-specific was accidentally removed.

## @mpdougherty

See the changes in commit: d474513beb3ff31b5809263f3e2b89e14f12310f.

## Copilot

Commit `d474513beb3ff31b5809263f3e2b89e14f12310f` implements **Option 1** as intended: it removes the cross-cutting “When uncertain / no speculation / provide options” content from overlays and keeps that responsibility in `goals.md` + `chat-manual.md`.

### What changed (and whether it matches the goal)

- **`data-raw/quarto-book.md`**
  - Removed the entire **“When uncertain (required)”** section (4 lines). This is correct for deduping.  
  - Note: the diff still shows “No newline at end of file” (meaning the file likely still lacks a trailing newline).  

- **`data-raw/shiny-golem.md`**
  - Removed the entire **“When uncertain (required)”** section (4 lines). Also correct for deduping.
  - Also still shows “No newline at end of file”.

- **`data-raw/r-package.md`**
  - Updated **“Reproducibility & audit trail”** to package-specific operational requirements (NEWS.md, data-raw provenance, tests/docs evidence, justified version constraints). This is exactly the “thin overlay” approach you wanted.
  - Removed the generic “Do not speculate…” bullet under “When uncertain” and retained the R Packages–specific instruction to present options + cite the book section. Correct.

- **`data-raw/chat-manual.md`**
  - Removed the “No newline at end of file” marker by adding a trailing newline (good housekeeping).

- **`data-raw/goals.md`**
  - It looks like you attempted to address the newline issue, but the patch shows an added line containing two spaces and still reports “No newline at end of file”. That suggests the file may still be missing a final newline (and now also contains trailing whitespace on a blank line).

### Only remaining issue I’d fix
To fully clean this up, I’d do one more tiny formatting pass:
- Ensure **all** instruction modules end with exactly **one** trailing newline (and no trailing whitespace-only lines), especially:
  - `data-raw/goals.md`
  - `data-raw/quarto-book.md`
  - `data-raw/shiny-golem.md`

This doesn’t affect meaning, but it prevents persistent diff noise.

If you want, paste the next commit SHA after that newline cleanup and I’ll verify the “no overlap” refactor is still intact.

## @mpdougherty

Now I need to create a new instruction overlay to address best practice for reproducibly writing technical user manuals. I'd like to name this overlay `user-manual`. My intention is to use it like: chat-manual + goals + quarto-book + user-manual. It should follow best industry practice as demonstrated in the following examples: https://r4ds.hadley.nz/, https://r-pkgs.org/, https://wesmckinney.com/book/, https://jjallaire.github.io/visualization-curriculum

## Copilot

Your overlay strategy works: **`chat-manual + goals + quarto-book + user-manual`** is a clean composition where:

- `quarto-book` governs *Quarto book mechanics* (structure, execution policy decisions, publishing),
- `user-manual` governs *manual-writing practice* (information architecture, examples, reproducibility, editorial standards, maintenance).

Below is a draft overlay module named **`user-manual`**. It’s written to be Quarto-friendly, HTML-first, and aligned with industry patterns seen in the example books you listed (task-oriented flow, runnable examples, strong navigation, versioned maintenance).

````markdown name=user-manual.md
# user-manual — Overlay Module (Reproducible Technical User Manuals)

## Canonical inspiration (required)
Use these as exemplars for style, structure, and maintenance patterns:
- https://r4ds.hadley.nz/
- https://r-pkgs.org/
- https://wesmckinney.com/book/
- https://jjallaire.github.io/visualization-curriculum

## Scope & audience (required)
Confirm early (do not infer):
1) primary audience (beginner / intermediate / expert; role-based personas),
2) expected prior knowledge and prerequisites,
3) primary user goals (what tasks they must accomplish),
4) supported platforms/versions (software versions, OS, dependencies),
5) whether the manual is a living document with ongoing releases.

## Information architecture (required)
- Prefer a task- and concept-layered structure:
  - **Getting started**: fastest path to first success
  - **Core workflows**: the “80% use cases”
  - **Reference**: complete but concise, searchable sections
  - **How-to guides**: problem/goal oriented (“How do I…?”)
  - **Deep dives**: design rationale, internals (optional, clearly separated)
- Keep chapters short and scannable:
  - use descriptive headings,
  - one main concept per section,
  - summary/checklist at the end of major chapters when helpful.
- Maintain strong navigation:
  - consistent terminology,
  - cross-links between concept ↔ how-to ↔ reference.

## Reproducible examples and outputs (required)
- Every code example must be:
  - minimal (smallest working example),
  - reproducible (deterministic seeds where applicable),
  - runnable in the documented environment.
- Prefer examples that demonstrate complete workflows (not fragments) for key tasks.
- Avoid network calls, unstable APIs, and time-dependent outputs in examples unless explicitly necessary.
  - If unavoidable, clearly label and provide a fallback/expected output.
- Use a consistent pattern for outputs:
  - show expected output when it meaningfully aids learning,
  - avoid huge outputs; summarize and link to full artifacts if needed.

## Testing and verification for manuals (required)
Require a verification approach appropriate to the manual’s tooling:
- If the manual executes code:
  - require a documented execution policy (align with `quarto-book` strategy matrix),
  - require a CI-friendly verification plan (at minimum: render + link check; ideally: execute key chapters).
- Treat as “release blockers” unless explicitly waived:
  - broken links, broken cross-references,
  - code examples that fail to run (for executed manuals),
  - stale instructions that contradict the current software behavior.

## Editorial standards (required)
- Use consistent terminology and naming:
  - align UI labels, function names, and CLI commands with the actual product.
- Prefer direct, imperative instructions for tasks (“Run…”, “Click…”).
- Explain “why” only where it prevents common errors; avoid long tangents in task sections.
- Use callouts intentionally (e.g., Note/Warning/Tip) and keep them short and actionable.

## Versioning, change management, and longevity (required)
- For living manuals, require an explicit versioning posture:
  - versioned docs per release vs “always latest” with a changelog.
- Require a visible change signal:
  - a NEWS/Changelog section for docs changes, or a “What’s new” page.
- When behavior changes, require updates in:
  - tutorials/getting started,
  - reference sections,
  - and any impacted screenshots/output snippets (avoid drift).

## Visuals and figures (required)
- Use figures to clarify mental models and workflows:
  - diagrams for architecture / data flow,
  - screenshots only when necessary (and keep them current).
- Prefer reproducible figure generation (code-driven) over manual editing where possible.

## Accessibility and inclusivity (required)
- Prefer accessible visuals (contrast, captions/alt text where supported).
- Avoid unexplained jargon; define terms on first use.
- Provide copy/paste-friendly code and command examples.

## “Definition of done” for manual work (required)
When planning or reviewing a documentation change, require:
- the user goal it supports,
- where it sits in the information architecture,
- how it is verified (render/tests/link checks),
- what could drift in the future and how to detect it.

## When uncertain (required)
- Ask what the user is trying to accomplish and what environment they are in (versions, platform).
- Present 3–5 options with tradeoffs only if the best approach depends on missing constraints.
````

### Important note about deduping
You recently deduped overlays to avoid repeating `goals.md` and `chat-manual.md`. If you want to keep that rule, the only section above that arguably duplicates base modules is **“When uncertain”** (and parts of “reproducible” language). If you want strict “thin overlays,” I can revise this draft to:
- remove the “When uncertain” section entirely, and
- keep only domain-specific documentation practices (architecture, examples, editorial, verification).

Do you want `user-manual` to follow the same “thin overlay” rule (no `When uncertain`, no generic “no speculation” restatements), or do you want it to be slightly more standalone?

## @mpdougherty

Let's continue to follow the “thin overlay” rule. 

## Copilot

````markdown name=user-manual.md
# user-manual — Overlay Module (Reproducible Technical User Manuals)

## Canonical inspiration (required)
Use these as exemplars for style, structure, and maintenance patterns:
- https://r4ds.hadley.nz/
- https://r-pkgs.org/
- https://wesmckinney.com/book/
- https://jjallaire.github.io/visualization-curriculum

## Scope & audience (required)
Confirm early (do not infer):
1) primary audience (beginner / intermediate / expert; role-based personas),
2) expected prior knowledge and prerequisites,
3) primary user goals (what tasks they must accomplish),
4) supported platforms/versions (software versions, OS, dependencies),
5) whether the manual is a living document with ongoing releases.

## Information architecture (required)
- Prefer a task- and concept-layered structure:
  - **Getting started**: fastest path to first success
  - **Core workflows**: the “80% use cases”
  - **How-to guides**: problem/goal oriented (“How do I…?”)
  - **Reference**: complete but concise, searchable sections
  - **Deep dives**: design rationale, internals (optional, clearly separated)
- Keep chapters short and scannable:
  - descriptive headings,
  - one main concept per section,
  - summaries/checklists at the end of major chapters when helpful.
- Maintain strong navigation:
  - consistent terminology,
  - cross-links between concept ↔ how-to ↔ reference.

## Reproducible examples & outputs (required)
- Every code example must be:
  - minimal (smallest working example),
  - runnable in the documented environment,
  - deterministic where randomness is involved (explicit seeding and stable inputs).
- Prefer examples that demonstrate complete workflows for key tasks (not isolated fragments).
- Avoid network calls, unstable APIs, and time-dependent outputs in examples unless explicitly necessary.
  - If unavoidable, label clearly and provide expected output and/or a fallback path.
- Output display rules:
  - show expected output where it aids learning or validation,
  - avoid huge outputs; summarize and link to fuller artifacts when needed.

## Verification expectations for manuals (required)
- Require a verification approach that matches the book’s execution policy (as defined in `quarto-book`):
  - at minimum: render succeeds + cross-references resolve + links are checked,
  - ideally: execute at least the “Getting started” + critical workflow chapters in CI.
- Treat as “release blockers” unless explicitly waived:
  - broken links,
  - broken cross-references,
  - failing code examples in chapters that are supposed to execute,
  - instructions that contradict current software behavior for core workflows.

## Editorial standards (required)
- Use consistent terminology and naming that matches the actual product:
  - function names, argument names, UI labels, commands, file paths.
- Prefer direct, imperative task writing:
  - “Run …”, “Click …”, “Open …”
- Explain “why” only where it prevents common errors or clarifies tradeoffs; keep task sections focused.
- Use callouts intentionally (Note/Warning/Tip):
  - keep them short,
  - make them actionable,
  - avoid duplicating mainline instructions.

## Versioning, change management, and drift control (required)
- Require an explicit docs versioning posture:
  - “always latest” vs versioned per release.
- Require a visible change signal:
  - a “What’s new” page and/or a docs changelog (especially for living manuals).
- When behavior changes, require updating:
  - Getting started/tutorial paths,
  - affected how-to guides,
  - reference sections,
  - and any screenshots/snippets that could drift.
- Prefer “single-source” patterns that reduce drift:
  - reusable snippets/partials for repeated instructions,
  - centralized variables for versions/paths where the tooling supports it.

## Visuals (required)
- Use visuals to clarify workflows and mental models:
  - diagrams for architecture/data flow,
  - screenshots only when necessary.
- Prefer reproducible, code-generated figures when possible.
- If screenshots are required:
  - keep them minimal,
  - ensure they match the current UI/version,
  - avoid embedding sensitive data.

## Accessibility and inclusivity (required)
- Prefer accessible visuals (contrast) and meaningful captions.
- Define jargon on first use; avoid unexplained acronyms.
- Keep code and commands copy/paste-friendly (avoid line wraps that break execution).

## Definition of done for manual work (required)
When planning or reviewing a documentation change, require:
- the user goal it supports,
- where it belongs in the information architecture,
- how it is verified (render/tests/link checks/execution policy),
- what could drift and how drift will be detected.
````

## @mpdougherty

See the latest changes in commit: 546cf5ec4eb77ee9fe5f92efbda0a88258054471. Now I'd like to draft a new function named `R/use_instructions.R`. The purpose of this function is to allow the user to compose chat session instructions using the syntax described above and write the selected instruction files into their working directory for reference in future reproducible chat sessions. Based on the requested composition, this function would write the requested instruction files into the folder `dev/instructions`.  Please provide a test to validate the function interface and resulting outputs. 

## Copilot

Commit `546cf5ec4eb77ee9fe5f92efbda0a88258054471` moves the instruction modules into a dedicated folder and leaves you in a good place to implement `use_instructions()`.

### What changed in that commit
All instruction modules were **renamed into** `data-raw/instructions/` with no content changes:
- `data-raw/instructions/chat-manual.md` citeturn3view3
- `data-raw/instructions/goals.md` citeturn3view4
- `data-raw/instructions/r-package.md` citeturn3view8
- `data-raw/instructions/shiny-golem.md` citeturn3view7
- `data-raw/instructions/quarto-book.md` citeturn3view6
- `data-raw/instructions/user-manual.md` citeturn3view5

### Current state relevant to your request
- There is **no existing `use_instructions` function** in the repo yet (no `R/use_instructions.R` found). citeturn3search0
- The package already uses **testthat edition 3** (so you can add tests in `tests/testthat/`). citeturn3view2

---

## Proposed function interface (R/use_instructions.R)

A clean, testable interface for your “compose and write to `dev/instructions`” requirement:

- `use_instructions(spec, dest_dir = "dev/instructions", source_dir = system.file("instructions", package = "ethicalai"))`
  - `spec` is a single string like:  
    `"chat-manual + goals + quarto-book + user-manual"`
  - The function parses the spec, maps tokens to `*.md` files, and copies those files to `dest_dir`.

Because your instruction files currently live in **`data-raw/instructions/`**, you’ll need to decide how they are made available at runtime. In an R package, the standard approach is to ship them under `inst/` so they are discoverable via `system.file()`. (Right now they’re in `data-raw/`, which is typically not installed with the package.)

### Suggested behavior details (important for tests)
- Accept `spec` as either:
  - a single string with `+` separators, or
  - a character vector of module names.
- Normalize module tokens: trim whitespace; preserve hyphens (e.g., `shiny-golem`).
- For each module token `<name>`, expect a source file `<name>.md`.
- Create `dest_dir` if it doesn’t exist.
- Copy files into `dest_dir` with the same filename.
- Return (invisibly or explicitly) a character vector of written file paths.

---

## Test to validate interface and outputs (tests/testthat/test-use_instructions.R)

This test validates:
- parsing the `spec` syntax,
- creation of `dev/instructions`,
- correct set of files written,
- written file contents match sources.

```r name=tests/testthat/test-use_instructions.R
test_that("use_instructions() composes modules and writes files to dev/instructions", {
  skip_if_not_installed("ethicalai")

  # Arrange: use a temp destination so tests don't touch the user's working tree
  tmp <- withr::local_tempdir()
  dest_dir <- file.path(tmp, "dev", "instructions")

  # NOTE: This assumes instructions are available via system.file().
  # If they are not shipped in inst/, this will return "" and the test will fail,
  # which is useful because it forces the packaging decision to be made.
  source_dir <- system.file("instructions", package = "ethicalai")
  expect_true(nzchar(source_dir))
  expect_true(dir.exists(source_dir))

  spec <- "chat-manual + goals + quarto-book + user-manual"

  # Act
  out <- use_instructions(spec = spec, dest_dir = dest_dir, source_dir = source_dir)

  # Assert: output paths
  expect_type(out, "character")
  expect_true(all(file.exists(out)))

  # Assert: exact filenames written
  expected_files <- c(
    "chat-manual.md",
    "goals.md",
    "quarto-book.md",
    "user-manual.md"
  )
  expect_equal(sort(basename(out)), sort(expected_files))

  # Assert: contents match sources exactly
  for (f in expected_files) {
    expect_identical(
      readLines(file.path(dest_dir, f), warn = FALSE),
      readLines(file.path(source_dir, f), warn = FALSE)
    )
  }
})
```

### Notes (so the test is meaningful)
- This test assumes the instruction markdown files are shipped with the installed package under `inst/instructions/` (so `system.file("instructions", package="ethicalai")` works). With the files currently in `data-raw/instructions/`, they won’t be available after installation, and the test will correctly fail until that’s addressed.

If you paste your preferred exact function signature (whether you want `source_dir` configurable, whether to return paths, etc.), I can tighten the test to match it precisely.

## @mpdougherty

Good catch. Moved instructions to `inst` for access via `system.file` in commit: 6a35aa19e05f7b350637e5557c0f1b018f863eb8. For the function interface, I think the character vector of module tokens is the R-ish way to do this (e.g., c("chat-manual", "goals", "r-package"). A Domain Specific Language like `chat-manual + goals + r-package` is really clean, but would require hefty function documentation that users would have to read. I choose the R character vector syntax for the `spec` parameter. Since the available modules in this package are intended to grow over time, how do propose a clean way for users to determine which modules are available and receive guidance on composition strategy?

## Copilot

A clean way to make this scalable is to provide **(1) a programmatic module index** and **(2) opinionated “recipes” (recommended compositions)**, both discoverable from R without reading long docs.

You’ve already set yourself up for this by moving the instruction files into `inst/instructions/` (so they can be enumerated at runtime via `system.file()`), per commit `6a35aa19e05f7b350637e5557c0f1b018f863eb8`. citeturn4view0

## Recommended design (3–5 options, ranked)

### Option 1 (recommended): `instructions_available()` + `instructions_recipes()`
Add two small exported helpers:

1) `instructions_available()`  
   - returns a character vector of module names discovered from `inst/instructions/*.md` (strip `.md`).
   - optionally include a `data.frame` variant with title/one-line summary extracted from the first heading line.

2) `instructions_recipes()`  
   - returns a named list of recommended compositions (“recipes”), e.g.:
     - `r-package` = `c("chat-manual", "goals", "r-package")`
     - `shiny-golem` = `c("chat-manual", "goals", "r-package", "shiny-golem")`
     - `quarto-book` = `c("chat-manual", "goals", "quarto-book")`
     - `quarto-manual` = `c("chat-manual", "goals", "quarto-book", "user-manual")`

**Pros:** discoverable, scalable, no heavy DSL docs, supports “best practice defaults”.  
**Cons:** requires maintaining recipes (but that’s a feature: it’s where you encode composition strategy).

### Option 2: Make `use_instructions()` self-documenting with `list = TRUE`
Example:
- `use_instructions(list = TRUE)` prints available modules and recipes, and returns them invisibly.
- `use_instructions(spec = c(...))` does the copying.

**Pros:** single entry point for users.  
**Cons:** mixes concerns slightly (but still reasonable).

### Option 3: Store module metadata in a lightweight manifest file
Ship `inst/instructions/_manifest.yml` with fields like:
- `name`, `type` (base/goal/overlay), `depends`, `conflicts`, `description`, `recommended_with`.

Then functions read the manifest, not filenames.

**Pros:** enables validation (“you forgot goals”), dependency resolution, future automation.  
**Cons:** more overhead now; you said you don’t want that rigor yet.

### Option 4: Vignette-only documentation
Write a vignette “Composing chat instructions”.

**Pros:** standard R packaging practice.  
**Cons:** users still have to read it; not as discoverable as an index function.

## What I would implement now (concrete recommendation)
- Keep `spec` as **character vector of tokens** in `use_instructions(spec = ...)`.
- Add:
  - `instructions_available()` for discovery
  - `instructions_recipes()` for composition guidance

### Suggested user workflow
```r
ethicalai::instructions_available()
ethicalai::instructions_recipes()

ethicalai::use_instructions(ethicalai::instructions_recipes()[["quarto-manual"]])
# or:
ethicalai::use_instructions(c("chat-manual", "goals", "quarto-book", "user-manual"))
```

### Composition guidance without a DSL
Put the guidance in:
- the **names and descriptions of recipes**, and
- a short docstring for `instructions_recipes()` (“Always include chat-manual + goals; add overlays as needed; include r-package for anything that is an R package (including golem apps).”).

If you want to stay extremely “thin,” you can even make recipes the *only* place you teach strategy.

---

If you want, I can propose exact function signatures for:
- `instructions_available()` (vector vs tibble; whether to include first-heading titles),
- `instructions_recipes()` (named list vs environment),
so they integrate cleanly with your upcoming `use_instructions()` tests.

## @mpdougherty

I would like to go with option 1. Please provide drafts of these proposed functions for my review.

## Copilot

Below are **draft implementations** for the two helper functions you picked in Option 1:

- `instructions_available()` — discovers available instruction modules shipped in `inst/instructions/`
- `instructions_recipes()` — returns opinionated recommended compositions (as a named list of character vectors)

They are written to be:
- dependency-light (base R only),
- stable as modules grow (discovery is filesystem-based),
- easy to test.

You can put these into separate files or the same file; I’m showing as two R files for clarity.

```r name=R/instructions_available.R
#' List available instruction modules shipped with the package
#'
#' Discovers instruction modules from `inst/instructions/*.md` at runtime.
#'
#' @param include_path Logical; if `TRUE`, return a data.frame with module names
#'   and file paths. If `FALSE` (default), return a character vector of module
#'   names.
#'
#' @return If `include_path = FALSE`, a character vector of module names.
#'   If `include_path = TRUE`, a data.frame with columns `module` and `path`.
#'
#' @export
instructions_available <- function(include_path = FALSE) {
  stopifnot(is.logical(include_path), length(include_path) == 1)

  dir <- system.file("instructions", package = "ethicalai")
  if (!nzchar(dir) || !dir.exists(dir)) {
    # This should not happen if inst/instructions is shipped correctly,
    # but the error message should be explicit.
    stop(
      "No instructions directory found in installed package. ",
      "Expected 'inst/instructions' to be installed and discoverable via system.file().",
      call. = FALSE
    )
  }

  paths <- list.files(dir, pattern = "\\.md$", full.names = TRUE)
  paths <- paths[!grepl("/_", paths)]  # ignore any optional underscore files (_manifest.md, etc.)

  modules <- sub("\\.md$", "", basename(paths))

  # stable ordering
  ord <- order(modules)
  modules <- modules[ord]
  paths <- paths[ord]

  if (!include_path) {
    return(modules)
  }

  data.frame(
    module = modules,
    path = paths,
    stringsAsFactors = FALSE
  )
}
```

```r name=R/instructions_recipes.R
#' Recommended instruction compositions ("recipes")
#'
#' Returns a set of recommended instruction module compositions for common
#' workflows. Recipes are expressed as character vectors suitable for passing to
#' `use_instructions(spec = ...)`.
#'
#' Design intent:
#' - Always include base modules: `chat-manual` + `goals`.
#' - Add overlays as needed (e.g., `r-package`, `shiny-golem`, `quarto-book`,
#'   `user-manual`).
#'
#' @param validate Logical; if `TRUE`, validate that referenced modules exist in
#'   `instructions_available()`. Defaults to `TRUE`.
#'
#' @return A named list. Each element is a character vector of module tokens.
#'
#' @export
instructions_recipes <- function(validate = TRUE) {
  stopifnot(is.logical(validate), length(validate) == 1)

  recipes <- list(
    # Base-only
    base = c("chat-manual", "goals"),

    # R packages
    r_package = c("chat-manual", "goals", "r-package"),

    # Shiny apps using golem (golem == package)
    shiny_golem = c("chat-manual", "goals", "r-package", "shiny-golem"),

    # Quarto books
    quarto_book = c("chat-manual", "goals", "quarto-book"),

    # Quarto books that are also technical manuals
    quarto_user_manual = c("chat-manual", "goals", "quarto-book", "user-manual")
  )

  if (validate) {
    avail <- instructions_available()
    missing <- setdiff(unique(unlist(recipes, use.names = FALSE)), avail)

    if (length(missing) > 0) {
      stop(
        "Some recipes reference missing instruction modules: ",
        paste(missing, collapse = ", "),
        call. = FALSE
      )
    }
  }

  recipes
}
```

### Notes / small choices for your review
1) **Recipe names**: I used `snake_case` list names (`r_package`, `shiny_golem`, …) because list indexing in R is nicer (`recipes[["quarto_user_manual"]]`). If you’d rather keep the *module tokens* as names (with hyphens), we can, but it’s slightly clunkier to type and print.
2) **Validation**: `instructions_recipes(validate=TRUE)` fails fast if you remove/rename a module but forget to update recipes. This is helpful as modules grow.
3) **Underscore filtering**: `instructions_available()` ignores files that start with `_` (optional convention if you later add `_README.md` or `_manifest.yml`).

If you want, I can also draft the (very small) `use_instructions()` function skeleton that consumes these—so the interface and tests all line up.

## @mpdougherty

While I create these functions and review the code, please supply test files for each of these draft functions.

## Copilot

Below are **testthat (edition 3)** test files for the two draft functions. They’re written to be robust both **inside the package repo** and **when installed**, assuming `inst/instructions/*.md` is present (you moved it in commit `6a35aa19…`). citeturn4view0

These tests do **not** touch the user’s working directory.

---

```r name=tests/testthat/test-instructions-available.R
test_that("instructions_available() returns character vector of module tokens", {
  mods <- instructions_available()

  expect_type(mods, "character")
  expect_true(length(mods) > 0)

  # should be unique + sorted
  expect_true(!anyDuplicated(mods))
  expect_identical(mods, sort(mods))

  # smoke check that known modules exist (update if you rename modules)
  expect_true(all(c("chat-manual", "goals") %in% mods))
})

test_that("instructions_available(include_path=TRUE) returns module/path data.frame", {
  df <- instructions_available(include_path = TRUE)

  expect_s3_class(df, "data.frame")
  expect_true(all(c("module", "path") %in% names(df)))

  expect_type(df$module, "character")
  expect_type(df$path, "character")

  # Paths should exist and end in .md
  expect_true(all(file.exists(df$path)))
  expect_true(all(grepl("\\.md$", df$path)))

  # module tokens should correspond to filenames
  expect_identical(df$module, sub("\\.md$", "", basename(df$path)))

  # stable ordering
  expect_identical(df$module, sort(df$module))
})

test_that("instructions_available() ignores underscore-prefixed files", {
  # This test is conditional: it only asserts behavior if such a file exists.
  df <- instructions_available(include_path = TRUE)
  has_underscore_file <- any(grepl("/_[^/]*\\.md$", df$path))

  if (has_underscore_file) {
    fail("instructions_available() should ignore underscore-prefixed .md files.")
  } else {
    succeed()
  }
})
```

---

```r name=tests/testthat/test-instructions-recipes.R
test_that("instructions_recipes() returns named list of character vectors", {
  rec <- instructions_recipes(validate = FALSE)

  expect_type(rec, "list")
  expect_true(length(rec) > 0)
  expect_true(!is.null(names(rec)))
  expect_true(all(nzchar(names(rec))))

  for (nm in names(rec)) {
    expect_type(rec[[nm]], "character", info = paste("recipe:", nm))
    expect_true(length(rec[[nm]]) > 0, info = paste("recipe:", nm))
    # discourage duplicates inside a recipe (not strictly required, but good hygiene)
    expect_false(anyDuplicated(rec[[nm]]) > 0, info = paste("recipe:", nm))
  }
})

test_that("instructions_recipes(validate=TRUE) references only available modules", {
  rec <- instructions_recipes(validate = TRUE)
  avail <- instructions_available()

  used <- unique(unlist(rec, use.names = FALSE))
  expect_true(all(used %in% avail))
})

test_that("instructions_recipes(validate=TRUE) errors if a recipe references a missing module", {
  # We can't directly inject a bad recipe into instructions_recipes(), so instead
  # we temporarily mask instructions_available() to simulate a missing module.
  local_mocked_bindings(
    instructions_available = function(include_path = FALSE) {
      # Return something that is missing at least one known module token used by recipes
      c("chat-manual", "goals")
    }
  )

  expect_error(
    instructions_recipes(validate = TRUE),
    "missing instruction modules",
    fixed = FALSE
  )
})
```

### Notes / dependencies
- The `local_mocked_bindings()` helper comes from **testthat**.
- These tests assume at least `chat-manual` and `goals` exist under `inst/instructions/` (they do in your repo state post-move). citeturn3view3turn3view4turn4view0

If you want the tests to be even more future-proof as modules evolve, I can modify them to *not* hardcode `"chat-manual"`/`"goals"` and instead only assert structural invariants—though I generally like keeping those two as “must exist” because they’re your base.

## @mpdougherty

The above changes were made in commit: feeb21d60b8ef40939f6de277fd6312fb3683557. All test are passing. Now draft the `use_instructions` function and its tests. 

## Copilot

Below are **drafts** for `use_instructions()` and its **testthat** tests, designed to fit the patterns already in your repo (e.g., explicit input validation, creating `dev/*` directories as needed, returning paths invisibly). I’m assuming your instruction modules are now shipped in `inst/instructions/` and discoverable via `system.file("instructions", package="ethicalai")` (as of your recent commits).

---

## `R/use_instructions.R`

Key behavior:
- `spec` is a **character vector of module tokens** (e.g., `c("chat-manual","goals","r-package")`)
- validates that requested modules exist (using `instructions_available(include_path=TRUE)`)
- copies module `.md` files into `dev/instructions/` by default
- returns the written file paths (invisibly), plus optionally prints messages

```r name=R/use_instructions.R
#' Write selected instruction modules into a working directory
#'
#' Copies instruction modules shipped with the package (from `inst/instructions/`)
#' into a local folder (default: `dev/instructions`) for reference in
#' reproducible chat sessions.
#'
#' @param spec Character vector of module tokens, e.g.
#'   `c("chat-manual", "goals", "r-package")`.
#' @param dest_dir Character. Destination directory (default: "dev/instructions").
#' @param overwrite Logical. Overwrite existing files? (default: TRUE).
#' @param quiet Logical. Suppress informational messages? (default: FALSE).
#'
#' @return Character vector of written file paths (invisibly).
#'
#' @export
use_instructions <- function(spec,
#'                             dest_dir = "dev/instructions",
#'                             overwrite = TRUE,
#'                             quiet = FALSE) {
  # Validate inputs ----
  if (missing(spec)) {
    stop("`spec` is required (character vector of module tokens).", call. = FALSE)
  }
  if (!is.character(spec) || length(spec) == 0) {
    stop("`spec` must be a non-empty character vector.", call. = FALSE)
  }
  if (anyNA(spec) || any(!nzchar(trimws(spec)))) {
    stop("`spec` contains missing/empty module tokens.", call. = FALSE)
  }
  if (!is.character(dest_dir) || length(dest_dir) != 1 || !nzchar(dest_dir)) {
    stop("`dest_dir` must be a non-empty character scalar.", call. = FALSE)
  }
  if (!is.logical(overwrite) || length(overwrite) != 1) {
    stop("`overwrite` must be TRUE/FALSE.", call. = FALSE)
  }
  if (!is.logical(quiet) || length(quiet) != 1) {
    stop("`quiet` must be TRUE/FALSE.", call. = FALSE)
  }

  # Normalize and de-duplicate while preserving order ----
  spec <- trimws(spec)
  spec <- spec[!duplicated(spec)]

  # Discover available modules + paths ----
  available_df <- instructions_available(include_path = TRUE)

  requested <- spec
  missing_mods <- setdiff(requested, available_df$module)
  if (length(missing_mods) > 0) {
    stop(
      "Unknown instruction module(s): ", paste(missing_mods, collapse = ", "),
      "\nAvailable modules: ", paste(available_df$module, collapse = ", "),
      call. = FALSE
    )
  }

  # Ensure destination directory exists ----
  if (!dir.exists(dest_dir)) {
    dir.create(dest_dir, recursive = TRUE, showWarnings = FALSE)
    if (!dir.exists(dest_dir)) {
      stop("Failed to create destination directory: ", dest_dir, call. = FALSE)
    }
    if (!quiet) message("Created directory: ", dest_dir)
  }

  # Copy files ----
  out_paths <- character(length(requested))

  for (i in seq_along(requested)) {
    mod <- requested[i]

    src <- available_df$path[match(mod, available_df$module)]
    if (!file.exists(src)) {
      # Should not happen, but keep error explicit.
      stop("Source file missing for module '", mod, "': ", src, call. = FALSE)
    }

    dest <- file.path(dest_dir, paste0(mod, ".md"))
    ok <- file.copy(src, dest, overwrite = overwrite)

    if (!ok) {
      stop(
        "Failed to write instruction file: ", dest,
        if (!overwrite && file.exists(dest)) " (already exists; overwrite=FALSE)" else "",
        call. = FALSE
      )
    }

    out_paths[i] <- dest
    if (!quiet) message("Wrote: ", dest)
  }

  invisible(out_paths)
}
```

Notes for your review:
- I intentionally **deduplicate** `spec` to avoid writing the same file twice. If you’d rather treat duplicates as an error, that’s easy to change.
- Errors include “Available modules…” to help the user self-correct without reading docs.

---

## Tests: `tests/testthat/test-use_instructions.R`

These tests validate:
- interface validation (must supply spec; must be non-empty character)
- correct outputs written in a temp directory (doesn’t touch real `dev/`)
- respects `overwrite = FALSE`
- unknown module error message includes available modules

```r name=tests/testthat/test-use_instructions.R
test_that("use_instructions() writes selected modules to a destination directory", {
  tmp <- withr::local_tempdir()
  dest_dir <- file.path(tmp, "dev", "instructions")

  out <- use_instructions(
    spec = c("chat-manual", "goals"),
    dest_dir = dest_dir,
    quiet = TRUE
  )

  expect_type(out, "character")
  expect_true(all(file.exists(out)))

  expect_equal(
    sort(basename(out)),
    sort(c("chat-manual.md", "goals.md"))
  )

  # Content should match installed sources
  src_dir <- system.file("instructions", package = "ethicalai")
  expect_true(nzchar(src_dir))
  expect_identical(
    readLines(file.path(dest_dir, "chat-manual.md"), warn = FALSE),
    readLines(file.path(src_dir, "chat-manual.md"), warn = FALSE)
  )
})

test_that("use_instructions() validates spec", {
  expect_error(use_instructions(), "`spec` is required", fixed = FALSE)
  expect_error(use_instructions(spec = character()), "non-empty character", fixed = FALSE)
  expect_error(use_instructions(spec = NA_character_), "missing/empty", fixed = FALSE)
  expect_error(use_instructions(spec = "  "), "missing/empty", fixed = FALSE)
})

test_that("use_instructions() errors on unknown modules and prints available modules", {
  expect_error(
    use_instructions(spec = c("does-not-exist"), dest_dir = withr::local_tempdir(), quiet = TRUE),
    "Available modules",
    fixed = FALSE
  )
})

test_that("use_instructions() honors overwrite=FALSE", {
  tmp <- withr::local_tempdir()
  dest_dir <- file.path(tmp, "dev", "instructions")

  # First write
  out1 <- use_instructions(
    spec = c("chat-manual"),
    dest_dir = dest_dir,
    overwrite = TRUE,
    quiet = TRUE
  )
  expect_true(file.exists(out1))

  # Second write should fail if overwrite=FALSE
  expect_error(
    use_instructions(
      spec = c("chat-manual"),
      dest_dir = dest_dir,
      overwrite = FALSE,
      quiet = TRUE
    ),
    "overwrite=FALSE",
    fixed = FALSE
  )
})

test_that("use_instructions() de-duplicates spec while preserving order", {
  tmp <- withr::local_tempdir()
  dest_dir <- file.path(tmp, "dev", "instructions")

  out <- use_instructions(
    spec = c("goals", "goals", "chat-manual"),
    dest_dir = dest_dir,
    quiet = TRUE
  )

  expect_equal(basename(out), c("goals.md", "chat-manual.md"))
})
```

---

### One small naming consistency suggestion (optional)
Right now you have `R/instruction_recipes.R` (singular “instruction”) in commit `feeb21…`. If you later add `use_instructions.R`, you might consider renaming to `R/instructions_recipes.R` for consistency—but not required.

If you want, paste your preferred error-message style (short vs verbose) and whether `spec` should be deduplicated vs error on duplicates, and I’ll adjust the drafts accordingly.

## @mpdougherty

These additions were added in commit: ec892083f4cc55e4c4b59b9f2d1792427b8d2338. Now lets create a vignette for this R package to demonstrate to users how to use this new functionality. 

## Copilot

You don’t currently have a `vignettes/` directory in the repo, and your `DESCRIPTION` doesn’t yet declare vignette tooling (`knitr`, `rmarkdown`) or a `VignetteBuilder`, so the clean path is:

1) add `vignettes/` with an `.Rmd` vignette (or `.qmd` if you prefer Quarto—but CRAN-style vignettes are most commonly `.Rmd`), and  
2) update `DESCRIPTION` to include vignette dependencies.

Below are **draft files** you can add (manual copy/paste). They demonstrate the new instruction-module workflow without touching the user’s real `dev/` folder by writing into a temp directory in examples.

---

````markdown name=vignettes/instructions.Rmd
---
title: "Composing reproducible chat instructions"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Composing reproducible chat instructions}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include=FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  error = FALSE,
  message = FALSE,
  warning = FALSE
)
```

This vignette shows how to:

- discover which instruction modules are shipped with `{ethicalai}`,
- use recommended compositions (“recipes”),
- write selected instruction files into a local folder (e.g. `dev/instructions/`) so future chat sessions are reproducible.

## Why instruction modules?

A reproducible chat session needs two things:

1. **Stable guidance** (what rules the assistant should follow), and
2. **A local record** of exactly which guidance was used.

`use_instructions()` supports (2) by copying the chosen instruction modules into a known folder.

## List available modules

Use `instructions_available()` to see what modules are installed with the package:

```{r}
library(ethicalai)

instructions_available()
```

If you want the resolved file paths:

```{r}
instructions_available(include_path = TRUE)
```

## Use recommended compositions (recipes)

`instructions_recipes()` provides opinionated, maintained “known good” compositions.

```{r}
recipes <- instructions_recipes()
names(recipes)

recipes$base
recipes$r_package
recipes$quarto_user_manual
```

## Write instructions into a project folder

In a real project, you’ll usually write to `dev/instructions/`:

```r
use_instructions(c("chat-manual", "goals", "r-package"))
```

For this vignette, we write into a temporary directory so nothing in your working directory is modified:

```{r}
tmp <- tempdir()
dest <- file.path(tmp, "dev", "instructions")

paths <- use_instructions(
  spec = c("chat-manual", "goals", "quarto-book", "user-manual"),
  dest_dir = dest,
  quiet = TRUE
)

paths
list.files(dest)
```

You can inspect the written files to confirm what guidance was used:

```{r}
cat(readLines(file.path(dest, "chat-manual.md"), warn = FALSE)[1:10], sep = "\n")
```

## A practical workflow

A typical “start of day” workflow might look like:

1. Choose a composition:
   - for Quarto technical manuals: `c("chat-manual", "goals", "quarto-book", "user-manual")`
   - for golem apps: `c("chat-manual", "goals", "r-package", "shiny-golem")`
2. Write them into the project:
   - `use_instructions(spec)`
3. Keep them under version control (recommended):
   - commit `dev/instructions/*.md` so reviewers can see exactly what guidance governed the chat session.

## Notes on validation

- `use_instructions()` validates that requested modules exist.
- If you typo a module name, the error will include the list of available modules so you can correct quickly.
````

---

### `DESCRIPTION` updates needed for vignettes

Add these fields:

- `Suggests: knitr, rmarkdown` (in addition to testthat)
- `VignetteBuilder: knitr`

You can patch it like this:

```diff name=DESCRIPTION
 Suggests: 
     testthat (>= 3.0.0),
+    knitr,
+    rmarkdown
 Config/testthat/edition: 3
+VignetteBuilder: knitr
```

(If you already keep `Suggests:` alphabetical, reorder accordingly.)

---

### Optional: `.gitignore` inside `vignettes/`
If you build vignettes locally, it will generate `vignettes/*.html` and `vignettes/*.R`. Many packages ignore those:

```gitignore name=vignettes/.gitignore
*.html
*.R
```

---

If you’d rather write the vignette as a **Quarto `.qmd`** (since your team is already using Quarto books), say so and I’ll provide a QMD-based vignette draft plus the minimal setup needed to build it during `R CMD build/check`.

## @mpdougherty

I'd like to use the `usethis::use_vignette` function to make the config changes to the package. Yes, I'd prefer to use .qmd. 

## Copilot

Use `usethis::use_vignette()` is a good approach, but there’s one practical wrinkle: **`use_vignette()` scaffolds an `.Rmd` vignette**, not a `.qmd`. You can still use it to set up all the package plumbing (DESCRIPTION fields, `vignettes/` dir, etc.), then **replace the `.Rmd` with a `.qmd`**.

Below is a clean, low-friction way to do that, plus a draft `.qmd` vignette you can drop in.

---

## 1) Scaffold with usethis (sets up package config)
Run in the package root:

```r
usethis::use_vignette("instructions")
```

This will typically:
- create `vignettes/instructions.Rmd`
- add `knitr`/`rmarkdown` to `Suggests` (if missing)
- add `VignetteBuilder: knitr` to DESCRIPTION
- add `vignettes/.gitignore` (often)

## 2) Convert to Quarto `.qmd`
1. Delete or rename the generated file:
   - remove `vignettes/instructions.Rmd`
2. Create:
   - `vignettes/instructions.qmd` (content below)

### Important note about building
R package vignettes are conventionally built by **knitr/rmarkdown** during `R CMD build/check`. Quarto can also render `.qmd`, but only if Quarto is available in the build environment and you configure the build to render it. So you have two viable paths:

- **Path A (simplest/most standard):** keep the vignette as `.Rmd` for package builds, and optionally maintain a Quarto version elsewhere.
- **Path B (your preference):** use `.qmd`, and ensure your CI/build tooling runs `quarto render` (and that Quarto is installed in those environments). The vignette can still live in `vignettes/`, but it won’t be built by default `R CMD build` unless you add a build step.

Since you explicitly want `.qmd`, the draft below assumes **Path B** (Quarto-rendered docs).

---

## 3) Draft Quarto vignette: `vignettes/instructions.qmd`

```markdown name=vignettes/instructions.qmd
---
title: "Composing reproducible chat instructions"
format:
  html:
    toc: true
    code-fold: false
execute:
  echo: true
  message: false
  warning: false
---

This vignette shows how to:

- discover which instruction modules are shipped with `{ethicalai}`,
- use recommended compositions (“recipes”),
- write selected instruction files into a local folder (e.g. `dev/instructions/`) so future chat sessions are reproducible.

## Why instruction modules?

A reproducible chat session needs two things:

1. **Stable guidance** (what rules the assistant should follow), and
2. **A local record** of exactly which guidance was used.

`use_instructions()` supports (2) by copying the chosen instruction modules into a known folder.

## List available modules

```{r}
library(ethicalai)

instructions_available()
```

If you want the resolved file paths:

```{r}
instructions_available(include_path = TRUE)
```

## Use recommended compositions (recipes)

```{r}
recipes <- instructions_recipes()
names(recipes)

recipes$base
recipes$r_package
recipes$quarto_user_manual
```

## Write instructions into a project folder

In a real project, you’ll usually write to `dev/instructions/`:

```r
use_instructions(c("chat-manual", "goals", "r-package"))
```

For this vignette, we write into a temporary directory so nothing in your working directory is modified:

```{r}
tmp <- tempdir()
dest <- file.path(tmp, "dev", "instructions")

paths <- use_instructions(
  spec = c("chat-manual", "goals", "quarto-book", "user-manual"),
  dest_dir = dest,
  quiet = TRUE
)

paths
list.files(dest)
```

Inspect the first few lines of one of the written instruction files:

```{r}
cat(readLines(file.path(dest, "chat-manual.md"), warn = FALSE)[1:12], sep = "\n")
```

## A practical workflow

A typical “start of day” workflow might look like:

1. Choose a composition:
   - for Quarto technical manuals: `c("chat-manual", "goals", "quarto-book", "user-manual")`
   - for golem apps: `c("chat-manual", "goals", "r-package", "shiny-golem")`
2. Write them into the project:
   - `use_instructions(spec)`
3. Keep them under version control (recommended):
   - commit `dev/instructions/*.md` so reviewers can see exactly what guidance governed the chat session.

## Notes on validation

- `use_instructions()` validates that requested modules exist.
- If you typo a module name, the error includes the list of available modules.
```

---

## 4) Minimal “how to build” note (so users aren’t confused)
If you go with `.qmd`, I recommend adding a short note in your README or CONTRIBUTING along the lines of:

- To render the vignette locally: `quarto render vignettes/instructions.qmd`
- CI should install Quarto and run that render step.

If you tell me your CI approach (GitHub Actions? which workflow?), I can suggest the smallest build step to add so the `.qmd` vignette is consistently rendered.

## @mpdougherty

That vignette works well. I'd like to switch gears to working on functionality to save chat session history to document the use of ai in a project. See the last commit: b3dd71725fdd96eed661f0bffd0fb7636d3e9a05 for updates to the `extract_copilot_chat` and related functions. Can you see these changes?

## Copilot

Yes—I can see the changes in commit `b3dd71725fdd96eed661f0bffd0fb7636d3e9a05`.

## What’s in that commit
### New function: `cleanup_session_backups()`
- **Added** `R/cleanup_session_backups.R` and exported it. citeturn5view0
- Purpose: delete (or dry-run report) backup files in `dev/sessions/.backups/` older than `days_to_keep` (default 7).
- It identifies backups using the filename pattern: `_backup_YYYYMMDD_HHMMSS.md`. citeturn5view0
- Also added the generated Rd doc `man/cleanup_session_backups.Rd` and `NAMESPACE` export. citeturn5view0

### `extract_copilot_chat()` in this commit
In the diff for `b3dd717…`, `extract_copilot_chat()` itself **did not change** (the file content shown is unchanged relative to what we saw previously): it still:
- validates the zip path and `.zip` extension,
- writes into `dev/sessions/` by default,
- optionally creates backups under `dev/sessions/.backups/` when overwriting,
- extracts the first root-level `.md` file from the Copilot export zip and copies it to a date/topic-named file. citeturn5view1

## One quick observation (for your “AI use audit trail” goal)
`cleanup_session_backups()` uses file modification time (`file.info()$mtime`) to decide “age.” That’s usually fine, but if backups are copied/moved between machines it can be misleading. If you later want “audit-grade” retention logic, you could parse the timestamp embedded in the filename instead. (Not necessary unless you hit that edge case.)

If you point me at the *specific commit* where `extract_copilot_chat` was updated (you mentioned “last commit … for updates to extract_copilot_chat and related functions,” but this particular commit is primarily the cleanup function), I can review that delta too.

## @mpdougherty

That summary of changes looks good. Now I'd like to follow the above procedure to create a vignette to demonstrate to users how to use these functions. 

## Copilot

Below is a Quarto `.qmd` vignette draft that demonstrates the **chat session capture + backup + cleanup** workflow using:

- `extract_copilot_chat()`
- `cleanup_session_backups()`

It’s written so it can render without needing a real Copilot export zip by **generating a minimal synthetic zip** in a temp directory (so users can run the examples). It also avoids writing into the project by default (uses `tempdir()`), but includes a “real project” section that points to `dev/sessions`.

---

```markdown name=vignettes/chat-session-history.qmd
---
title: "Saving Copilot chat session history (audit trail)"
format:
  html:
    toc: true
execute:
  echo: true
  message: false
  warning: false
---

This vignette demonstrates a reproducible workflow for capturing GitHub Copilot Chat session history into a project directory to support a transparent audit trail of AI usage.

Functions covered:

- `extract_copilot_chat()` — extract the main chat markdown from a Copilot export `.zip` into a dated session file
- `cleanup_session_backups()` — remove older backups to prevent `dev/sessions/.backups/` bloat

## Overview of the workflow

A typical workflow looks like:

1. Export your Copilot Chat session as a `.zip` file (from your tooling / UI).
2. Run `extract_copilot_chat()` to write (or update) a `dev/sessions/YYYY-MM-DD[_topic].md` file.
3. Re-run the export + `extract_copilot_chat()` throughout the day; the function will create timestamped backups.
4. Periodically run `cleanup_session_backups()` to delete old backups.

## Setup

```{r}
library(ethicalai)
```

## A runnable example (using a synthetic export zip)

To make this vignette runnable without requiring an actual Copilot export, we generate a tiny zip that mimics the expected structure:

- a markdown file in the **root** of the zip archive (this is what `extract_copilot_chat()` looks for)

```{r}
tmp <- tempdir()

# Create a minimal Copilot-like export zip in a temp folder
export_dir <- file.path(tmp, "copilot_export_example")
dir.create(export_dir, showWarnings = FALSE, recursive = TRUE)

md_path <- file.path(export_dir, "copilot-chat.md")
writeLines(c(
  "# Example Copilot Chat Export",
  "",
  "This is a synthetic chat export used for the vignette.",
  "",
  "- Question: How do I do X?",
  "- Answer: Here's how..."
), md_path)

zip_path <- file.path(tmp, "copilot_export_example.zip")

# zip() wants paths relative to working directory unless you use -j; simplest is setwd temporarily
old_wd <- getwd()
setwd(export_dir)
utils::zip(zipfile = zip_path, files = basename(md_path))
setwd(old_wd)

file.exists(zip_path)
```

## Extract a session transcript

In a real project, you’ll typically use `sessions_dir = "dev/sessions"` (the default).
For this vignette, we write into a temp directory so we don’t modify your project.

```{r}
sessions_dir <- file.path(tmp, "dev", "sessions")

session_path <- extract_copilot_chat(
  zip_path = zip_path,
  session_date = "2026-04-14",
  topic = "documentation",
  sessions_dir = sessions_dir,
  backup = TRUE,
  quiet = TRUE
)

session_path
readLines(session_path, warn = FALSE)[1:6]
```

## Updating the same session (and creating backups)

If you export again later and re-run `extract_copilot_chat()` for the same `session_date` + `topic`,
the existing file will be overwritten and a backup will be created in `dev/sessions/.backups/`.

To demonstrate that, we update the synthetic markdown inside the zip and extract again.

```{r}
# Update the synthetic markdown and re-zip
writeLines(c(
  "# Example Copilot Chat Export (updated)",
  "",
  "This simulates a later export from the same day.",
  "",
  "- New item added later in the session."
), md_path)

old_wd <- getwd()
setwd(export_dir)
utils::zip(zipfile = zip_path, files = basename(md_path))
setwd(old_wd)

# Extract again -> should create a backup of the previous session file
session_path2 <- extract_copilot_chat(
  zip_path = zip_path,
  session_date = "2026-04-14",
  topic = "documentation",
  sessions_dir = sessions_dir,
  backup = TRUE,
  quiet = TRUE
)

identical(session_path, session_path2)
```

List session files and backups:

```{r}
list.files(sessions_dir, full.names = FALSE)

backup_dir <- file.path(sessions_dir, ".backups")
list.files(backup_dir, full.names = FALSE)
```

## Cleaning up old backups

Over time, repeated exports can create many backups. Use `cleanup_session_backups()` to
delete backups older than `days_to_keep`.

First, a dry run (no deletion):

```{r}
cleanup_session_backups(
  sessions_dir = sessions_dir,
  days_to_keep = 7,
  dry_run = TRUE
)
```

If you want to actually delete eligible backups:

```{r, eval=FALSE}
cleanup_session_backups(
  sessions_dir = "dev/sessions",
  days_to_keep = 7,
  dry_run = FALSE
)
```

## Recommended project conventions

For a consistent audit trail across projects:

- Store session transcripts in `dev/sessions/`
- Use `YYYY-MM-DD[_topic].md` naming (what `extract_copilot_chat()` produces)
- Commit `dev/sessions/*.md` to version control when appropriate for your org
- Decide on a backup retention policy (e.g., keep 7 or 30 days)
- Periodically run `cleanup_session_backups()` (or schedule it)

## Troubleshooting

Common issues:

- **“Zip file not found”**: confirm the path is correct and accessible.
- **“No .md file found in zip archive root”**: `extract_copilot_chat()` expects a markdown file at the root of the zip.
- **Multiple root `.md` files**: the function will use the first one (and warn).
```

---

### How to scaffold via `usethis::use_vignette()` (same procedure as before)
You can do:

```r
usethis::use_vignette("chat-session-history")
```

Then replace the generated `vignettes/chat-session-history.Rmd` with the `.qmd` above (and ensure your build process renders `.qmd` vignettes using Quarto).

---

If you tell me how you intend users to build/package docs (GitHub Actions? pkgdown? Quarto render step?), I can suggest the smallest, most reliable build step to ensure this `.qmd` vignette is always rendered.

## @mpdougherty

I forgot to mention that I changed my mind about using Quarto for vignettes. I'd prefer to use .Rmd to maintain compatibility with the traditional R CMD build process. 

## Copilot

Use `.Rmd` vignettes is the safest choice for **standard `R CMD build/check`**. Here’s the same vignette rewritten as an **`html_vignette` `.Rmd`** that remains runnable by generating a synthetic Copilot export zip in a temp directory.

You can scaffold with:

```r
usethis::use_vignette("chat-session-history")
```

…and then replace the file contents with the draft below.

---

````markdown name=vignettes/chat-session-history.Rmd
---
title: "Saving Copilot chat session history (audit trail)"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Saving Copilot chat session history (audit trail)}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include=FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  error = FALSE,
  message = FALSE,
  warning = FALSE
)
```

This vignette demonstrates a reproducible workflow for capturing GitHub Copilot Chat
session history into a project directory to support a transparent audit trail of AI usage.

Functions covered:

- `extract_copilot_chat()` — extract the main chat markdown from a Copilot export `.zip` into a dated session file
- `cleanup_session_backups()` — remove older backups to prevent `dev/sessions/.backups/` bloat

## Overview of the workflow

A typical workflow looks like:

1. Export your Copilot Chat session as a `.zip` file.
2. Run `extract_copilot_chat()` to write (or update) a `dev/sessions/YYYY-MM-DD[_topic].md` file.
3. Re-run the export + `extract_copilot_chat()` throughout the day; the function will create timestamped backups.
4. Periodically run `cleanup_session_backups()` to delete old backups.

## Setup

```{r}
library(ethicalai)
```

## A runnable example (using a synthetic export zip)

To make this vignette runnable without requiring an actual Copilot export, we generate
a tiny zip that mimics the expected structure:

- a markdown file in the **root** of the zip archive (this is what `extract_copilot_chat()` looks for)

```{r}
tmp <- tempdir()

# Create a minimal Copilot-like export zip in a temp folder
export_dir <- file.path(tmp, "copilot_export_example")
dir.create(export_dir, showWarnings = FALSE, recursive = TRUE)

md_path <- file.path(export_dir, "copilot-chat.md")
writeLines(c(
  "# Example Copilot Chat Export",
  "",
  "This is a synthetic chat export used for the vignette.",
  "",
  "- Question: How do I do X?",
  "- Answer: Here's how..."
), md_path)

zip_path <- file.path(tmp, "copilot_export_example.zip")

# zip() works most reliably with relative paths, so temporarily setwd()
old_wd <- getwd()
setwd(export_dir)
utils::zip(zipfile = zip_path, files = basename(md_path))
setwd(old_wd)

file.exists(zip_path)
```

## Extract a session transcript

In a real project, you’ll typically use `sessions_dir = "dev/sessions"` (the default).
For this vignette, we write into a temp directory so we don’t modify your project.

```{r}
sessions_dir <- file.path(tmp, "dev", "sessions")

session_path <- extract_copilot_chat(
  zip_path = zip_path,
  session_date = "2026-04-14",
  topic = "documentation",
  sessions_dir = sessions_dir,
  backup = TRUE,
  quiet = TRUE
)

session_path
readLines(session_path, warn = FALSE)[1:6]
```

## Updating the same session (and creating backups)

If you export again later and re-run `extract_copilot_chat()` for the same
`session_date` + `topic`, the existing file will be overwritten and a backup will be
created in `dev/sessions/.backups/`.

To demonstrate that, we update the synthetic markdown inside the zip and extract again.

```{r}
# Update the synthetic markdown and re-zip
writeLines(c(
  "# Example Copilot Chat Export (updated)",
  "",
  "This simulates a later export from the same day.",
  "",
  "- New item added later in the session."
), md_path)

old_wd <- getwd()
setwd(export_dir)
utils::zip(zipfile = zip_path, files = basename(md_path))
setwd(old_wd)

# Extract again -> should create a backup of the previous session file
session_path2 <- extract_copilot_chat(
  zip_path = zip_path,
  session_date = "2026-04-14",
  topic = "documentation",
  sessions_dir = sessions_dir,
  backup = TRUE,
  quiet = TRUE
)

identical(session_path, session_path2)
```

List session files and backups:

```{r}
list.files(sessions_dir, full.names = FALSE)

backup_dir <- file.path(sessions_dir, ".backups")
list.files(backup_dir, full.names = FALSE)
```

## Cleaning up old backups

Over time, repeated exports can create many backups. Use `cleanup_session_backups()`
to delete backups older than `days_to_keep`.

First, a dry run (no deletion):

```{r}
cleanup_session_backups(
  sessions_dir = sessions_dir,
  days_to_keep = 7,
  dry_run = TRUE
)
```

To actually delete eligible backups in a real project:

```{r, eval=FALSE}
cleanup_session_backups(
  sessions_dir = "dev/sessions",
  days_to_keep = 7,
  dry_run = FALSE
)
```

## Recommended project conventions

For a consistent audit trail across projects:

- Store session transcripts in `dev/sessions/`
- Use `YYYY-MM-DD[_topic].md` naming (what `extract_copilot_chat()` produces)
- Commit `dev/sessions/*.md` to version control when appropriate for your org
- Decide on a backup retention policy (e.g., keep 7 or 30 days)
- Periodically run `cleanup_session_backups()` (or schedule it)

## Troubleshooting

Common issues:

- **“Zip file not found”**: confirm the path is correct and accessible.
- **“No .md file found in zip archive root”**: `extract_copilot_chat()` expects a markdown file at the root of the zip.
- **Multiple root `.md` files**: the function will use the first one (and warn).
````

---

### One small tweak you may want (optional)
In the runnable example we hard-code `session_date = "2026-04-14"` to keep outputs stable. You can change that to `Sys.Date()` if you want the vignette to reflect “today,” but deterministic output is usually nicer for examples/tests.

If you paste the commit SHA after you add this vignette (and the `DESCRIPTION` updates from `use_vignette()`), I can confirm everything is wired correctly (Suggests, VignetteBuilder, file path, and that no examples write into the package root).
